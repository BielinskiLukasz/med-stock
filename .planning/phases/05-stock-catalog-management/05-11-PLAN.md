---
phase: 05-stock-catalog-management
plan: 11
type: execute
wave: 3
depends_on: [05-10]
files_modified:
  - src/lib/stockOps.ts
  - src/routes/medicines/[id].tsx
autonomous: true
gap_closure: true
gap_ids: [G-05-8]
requirements: [CAT-03]

must_haves:
  truths:
    - "A delete button appears in the catalog header of the detail view"
    - "Clicking delete opens an AlertDialog; if active stock entries exist the dialog shows a guard message with only a Cancel option"
    - "When no active stock exists the dialog shows a confirm message; confirming calls deleteCatalogEntry and navigates to /medicines"
    - "deleteCatalogEntry deletes the catalog row in a db.transaction; it throws if active stock entries exist"
  artifacts:
    - src/lib/stockOps.ts (deleteCatalogEntry function)
    - src/routes/medicines/[id].tsx (catalog delete button, AlertDialog, handler, navigate on success)
  key_links:
    - "stockEntries (already loaded via useLiveQuery) drives the guard check in the dialog — no extra DB query needed"
    - "deleteCatalogEntry uses db.transaction('rw', db.medicine_catalog) to ensure atomicity"
    - "navigate('/medicines') called inside handleCatalogDeleteConfirm on success"
---

<objective>
Implement catalog deletion: a Trash2 button in the catalog header opens an AlertDialog that either guards deletion (when active stock exists) or confirms it (when the catalog is empty). Confirmed deletion calls a new deleteCatalogEntry function and navigates back to the list.

Purpose: Users have no way to remove an unwanted catalog entry from the UI. This was the only feature entirely absent from the detail view. The guard prevents orphaned stock entries by blocking deletion while active stock remains.
Output: New deleteCatalogEntry function in stockOps.ts and corresponding UI in [id].tsx.
</objective>

<execution_context>
@C:/my-code/vibe-coding/med-stock/.claude/gsd-core/workflows/execute-plan.md
@C:/my-code/vibe-coding/med-stock/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-stock-catalog-management/05-UAT.md
@.planning/phases/05-stock-catalog-management/05-10-SUMMARY.md
</context>

<tasks>

<task type="tracer">
  <name>Tracer: deleteCatalogEntry function in stockOps.ts (G-05-8)</name>
  <files>src/lib/stockOps.ts</files>
  <action>
    Read the current file before editing.

    Add a new exported async function `deleteCatalogEntry(catalogId: number): Promise<void>` at the end of the file.

    Inside the function: first count active stock entries for this catalog using `db.medicines.where('catalogId').equals(catalogId).filter(m => m.deletedAt === null).count()`. If the count is greater than 0, throw a new Error with the message 'Cannot delete catalog with active stock entries'.

    If the count is 0, execute `db.transaction('rw', db.medicine_catalog, async () => { await db.medicine_catalog.delete(catalogId) })`.

    Import db and the medicine_catalog table access is already available via the existing db import. No new imports are needed.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>deleteCatalogEntry is exported from stockOps.ts. TypeScript compiles. Calling it with a catalogId that has active stock throws; calling it with a catalog that has no active stock deletes the catalog row.</done>
</task>

<task type="auto">
  <name>Catalog delete button + AlertDialog + navigation in [id].tsx (G-05-8)</name>
  <files>src/routes/medicines/[id].tsx</files>
  <action>
    Read the current file before editing. This file has been edited by 05-07 and 05-10. Build on the current state.

    Imports: add deleteCatalogEntry to the existing import from '@/lib/stockOps'. Trash2 is already imported from 'lucide-react'. AlertDialog and related components are already imported.

    State: add `const [catalogDeleteOpen, setCatalogDeleteOpen] = useState(false)`.

    Handler: add an async function `handleCatalogDeleteConfirm`. Inside: call `await deleteCatalogEntry(catalogId)`, wrap in try/catch — on success call `toast.success('Catalog deleted')` and `void navigate('/medicines')`; on error call `toast.error('Failed to delete catalog. Please try again.')` and log to console.error.

    Catalog header button: in the catalog header div (the `<div className="flex items-center gap-1">` that holds the StatusBadge and the pencil edit button), add a second button after the pencil button. Use the same styling pattern as the pencil button: `type="button"`, `className="p-1 rounded-md hover:bg-gray-100 transition-colors"`, `aria-label="Delete catalog"`, `onClick={() => setCatalogDeleteOpen(true)}`. Render `<Trash2 className="h-4 w-4 text-red-500" />` inside.

    AlertDialog for catalog delete: add a second AlertDialog after the existing stock-entry delete AlertDialog. Use `open={catalogDeleteOpen}` and `onOpenChange={setCatalogDeleteOpen}`.

    Inside the dialog, branch on whether active stock exists (use the already-loaded `stockEntries` array):
    - When `stockEntries.length > 0`: title is "Cannot delete catalog", description explains the catalog has active stock entries that must be deleted first. Footer has only a Cancel button.
    - When `stockEntries.length === 0`: title is "Delete catalog?", description explains this permanently removes the catalog entry. Footer has Cancel and a destructive AlertDialogAction that calls `void handleCatalogDeleteConfirm()`.
  </action>
  <verify>
    <automated>npx vitest run 2>/dev/null; npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>Trash2 button appears in catalog header. Clicking it when active stock exists shows a guard dialog with no delete option. Clicking it when no active stock exists shows a confirm dialog; confirming deletes the catalog and navigates to /medicines. TypeScript clean. Tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI guard | stockEntries.length check in dialog is informational; deleteCatalogEntry re-enforces the guard server-side before executing |
| DB transaction | deletion wrapped in db.transaction — no partial state |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-11-01 | Tampering | catalog delete with active stock | medium | mitigate | deleteCatalogEntry re-checks active stock count inside the function before deleting; the UI guard alone is not relied on |
| T-05-11-02| Denial of Service | orphaned stock after catalog delete | high | mitigate | active-stock guard in deleteCatalogEntry prevents orphaned medicines — stock must be deleted to Trash first |
</threat_model>

<verification>
`npx tsc --noEmit` clean. `npx vitest run` green.

Manual:
1. Open a catalog detail view with active stock entries — Trash2 button visible in header. Click it — guard dialog appears, no delete option.
2. Delete all stock entries (move to Trash) for a catalog. Click Trash2 — confirm dialog appears. Click Delete — catalog is removed and app navigates to /medicines. The catalog no longer appears in the list.
3. Confirm the deleted catalog's ID no longer exists in medicine_catalog (check via dev tools IndexedDB inspector).
</verification>

<success_criteria>
Gap G-05-8 closed. Catalog deletion works end-to-end with active-stock guard. TypeScript clean. All tests green.
</success_criteria>

<output>
Create `.planning/phases/05-stock-catalog-management/05-11-SUMMARY.md` when done
</output>
