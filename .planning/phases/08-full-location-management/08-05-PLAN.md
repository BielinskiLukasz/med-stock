---
phase: 08-full-location-management
plan: 05
type: execute
wave: 4
depends_on: ["08-02", "08-04"]
files_modified:
  - package.json
  - src/components/SortableLocationList.tsx
  - src/routes/locations/index.tsx
autonomous: true
requirements: [LOC-04]

estimate:
  tokens: 42000
  raw_tokens: 42000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Each location row can be dragged (via a dedicated drag handle) to a new position, and the new order persists to db.locations and is reflected everywhere order is read from (D-14)."
    - "Each location row also has up/down move buttons that reorder it by exactly one position, as a keyboard/screen-reader-accessible fallback to dragging — both mechanisms operate on the same list simultaneously, neither hidden behind a toggle (D-14)."
    - "The first row's up button and the last row's down button are disabled — a single-row list has both buttons disabled (resolves the UI-SPEC zero-one-many row)."
    - "Hidden locations remain draggable and button-movable in this same list, just visually muted — hiding a location never removes it from the reorder UI (D-15)."
    - "\"Other\" (the null location) never appears as a row in this list — it has no Location record and is filtered out before the list is built, not filtered out inside the drag library (D-16)."
    - "Both the drag-end handler and the up/down button handlers call the same shared reorderLocations(orderedIds) function from Plan 08-02 — no duplicate renumbering logic exists between the two input paths."
    - statement: "Dragging a row shows the opacity: 0.5 drag-active state and other rows reflow smoothly, without layout jump, at a 360px-wide mobile viewport."
      verification: backstop
  artifacts:
    - "src/components/SortableLocationList.tsx — @dnd-kit DndContext/SortableContext wrapper + per-row useSortable hook"
    - "src/routes/locations/index.tsx — up/down buttons, reorder hint, SortableLocationList wired in place of the plain .map() loop"
    - "package.json — @dnd-kit/core, @dnd-kit/sortable dependencies"
  key_links:
    - "SortableLocationList onDragEnd -> compute new ordered array via array splice -> reorderLocations(newOrderedIds)"
    - "up/down Button onClick -> swap with adjacent visible row in the current array -> reorderLocations(newOrderedIds)"
  prohibitions:
    - "Never include a row for 'Other' in the @dnd-kit items array — filter it out before the array is built, since it is not a Location record to begin with."
    - "Never make a hidden row non-draggable or non-button-movable."
    - "Never implement a second, independent renumbering code path for the button handlers — both drag and buttons must call the same reorderLocations function."
---

<objective>
Add drag-to-reorder (via `@dnd-kit`) and up/down move buttons to the Locations screen, both writing through the `reorderLocations` function Plan 08-02 already tested. This is the last LOC-04 piece — everything else in the phase already reads `order` correctly (Plans 08-01, 08-03) and the mutation logic already exists (Plan 08-02); this plan is purely the drag/button UI on top.

Purpose: Households reorganize their location list to match how they actually think about their home (fridge before bathroom, etc.) — D-14 requires both a touch-friendly drag affordance and a reliable keyboard/screen-reader fallback.
Output: A new `SortableLocationList` component wired into `LocationsScreen`, replacing the plain `.map()` loop, with up/down buttons alongside the drag handle on every row.
</objective>

<execution_context>
@C:/my-code/vibe-coding/med-stock/.claude/gsd-core/workflows/execute-plan.md
@C:/my-code/vibe-coding/med-stock/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-full-location-management/08-CONTEXT.md
@.planning/phases/08-full-location-management/08-RESEARCH.md
@.planning/phases/08-full-location-management/08-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install @dnd-kit and build SortableLocationList</name>
  <files>package.json, src/components/SortableLocationList.tsx</files>
  <read_first>
    .planning/phases/08-full-location-management/08-RESEARCH.md ("Drag-and-Drop Library Choice", "Package Legitimacy Audit" — both packages already vetted Approved, "Code Examples" reference implementation)
    .planning/phases/08-full-location-management/08-UI-SPEC.md ("Reorder UI (Drag + Buttons)" section)
    src/lib/db.ts (Location interface, post-08-01)
  </read_first>
  <action>
    Run `npm install @dnd-kit/core @dnd-kit/sortable @dnd-kit/utilities` (all three already legitimacy-vetted "Approved" in 08-RESEARCH.md's Package Legitimacy Audit — no install checkpoint needed).
    Create `src/components/SortableLocationList.tsx` exporting `SortableLocationList({ locations, renderRow }: { locations: Location[]; renderRow: (loc: Location) => React.ReactNode })` and an internal `SortableLocationRow` wrapper. Use `DndContext` with `useSensors(useSensor(PointerSensor), useSensor(KeyboardSensor))` and `collisionDetection={closestCenter}`, `SortableContext` with `items={locations.map(l => l.id)}` and `strategy={verticalListSortingStrategy}`. `SortableLocationRow` calls `useSortable({ id: loc.id })`, applies `transform: CSS.Transform.toString(transform), transition` and `opacity: isDragging ? 0.5 : 1` via inline style on its wrapper `div`, spreads `{...attributes} {...listeners}` onto a dedicated drag-handle element (a `GripVertical` icon from `lucide-react`, `aria-label={t('aria.dragHandle')}`, minimum 44x44px touch target per the UI-SPEC spacing exceptions), and renders `renderRow(loc)` for the rest of the row content — the caller (LocationsScreen) owns what a row looks like; this component owns only the drag mechanics. The component takes an `onDragEnd(oldIndex: number, newIndex: number)` callback prop, invoked from `DndContext`'s `onDragEnd` when `active.id !== over.id`, computed via `locations.findIndex()` on both ids.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <fails_when>non-zero exit, or any line matching "error TS"</fails_when>
  </verify>
  <acceptance_criteria>
    - `package.json` dependencies include `@dnd-kit/core`, `@dnd-kit/sortable`, `@dnd-kit/utilities`.
    - `SortableLocationList` exports a component accepting `locations`, `renderRow`, and `onDragEnd` props, with no hardcoded row markup of its own beyond the drag handle.
  </acceptance_criteria>
  <done>`SortableLocationList` compiles and is ready to be wired into LocationsScreen. `npx tsc --noEmit` passes.</done>
</task>

<task type="auto">
  <name>Task 2: Wire SortableLocationList + up/down buttons into LocationsScreen</name>
  <files>src/routes/locations/index.tsx</files>
  <read_first>
    src/routes/locations/index.tsx (state after Plan 08-04 — the plain `.map()` loop over `locations`)
    src/lib/locationOps.ts (reorderLocations signature from Plan 08-02)
    src/components/SortableLocationList.tsx (from Task 1 of this plan)
    .planning/phases/08-full-location-management/08-UI-SPEC.md ("Row anatomy" ordering: drag handle, name, badge, up, down, hide/show, edit, delete — and the `locations.reorderHint` copy)
  </read_first>
  <action>
    Import `SortableLocationList` from `@/components/SortableLocationList` and `reorderLocations` from `@/lib/locationOps`, plus `ChevronUp, ChevronDown` from `lucide-react`. Replace the existing `<div className="space-y-1">{locations.map((loc) => (...))}</div>` block: wrap it with `<SortableLocationList locations={locations} onDragEnd={handleReorder} renderRow={(loc) => (/* existing per-row JSX from Plan 08-04, unchanged */)} />`.
    Add `async function handleReorder(oldIndex: number, newIndex: number)`: build the new order via `const reordered = [...locations]; const [moved] = reordered.splice(oldIndex, 1); reordered.splice(newIndex, 0, moved)`, then `await reorderLocations(reordered.map(l => l.id))`.
    Add `async function handleMove(index: number, direction: 'up' | 'down')`: compute `targetIndex = direction === 'up' ? index - 1 : index + 1`; guard `if (targetIndex < 0 || targetIndex >= locations.length) return`; build the swapped array the same splice way as `handleReorder` (moving the item at `index` to `targetIndex`), then call `reorderLocations(...)` with the resulting id order — reuse `handleReorder(index, targetIndex)` directly rather than duplicating the splice logic.
    Inside the row content passed to `renderRow`, add two `Button variant="ghost" size="sm"` elements with `ChevronUp`/`ChevronDown` icons, `aria-label={t('aria.moveUp')}`/`{t('aria.moveDown')}`, `onClick={() => handleMove(locations.findIndex(l => l.id === loc.id), 'up' | 'down')}`, each `disabled` when its target index would be out of bounds (up disabled when `index === 0`, down disabled when `index === locations.length - 1`) — this naturally covers the single-row case (both disabled). Place them per the UI-SPEC row anatomy: after the drag handle and name/badge, before the hide/show toggle. Add a one-line hint above the list: `<p className="text-xs text-muted-foreground mb-2">{t('locations.reorderHint')}</p>`, rendered once, above `SortableLocationList` (only when `locations.length > 0` — no hint needed for the empty state from Plan 08-04).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <fails_when>non-zero exit, or any line matching "error TS"</fails_when>
    <automated>npm run build 2>&1 | tail -5</automated>
    <fails_when>output does not contain a successful build summary, or contains "error"</fails_when>
    <human-check>At a 360px-wide viewport, drag a location row: confirm it shows the opacity 0.5 drag-active state and the rest of the list reflows without a layout jump. Then click a row's up/down chevrons and confirm the order visibly changes and persists after a page reload.</human-check>
  </verify>
  <acceptance_criteria>
    - `LocationsScreen`'s row list is rendered through `SortableLocationList`, not a bare `.map()`.
    - Up/down buttons exist on every row; the first row's up button and the last row's down button are `disabled`.
    - `handleReorder` and `handleMove` both funnel into the single `reorderLocations` call — no second renumbering implementation exists in this file.
  </acceptance_criteria>
  <done>Dragging and the up/down buttons both reorder locations, persist through `reorderLocations`, and reflect immediately via `useLiveQuery`. `npx tsc --noEmit` and `npm run build` pass; the human-check confirms the drag visual and button behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| npm install | `@dnd-kit/core`, `@dnd-kit/sortable`, `@dnd-kit/utilities` are new third-party dependencies pulled into the build. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-05-SC | Tampering | npm install of @dnd-kit/core, @dnd-kit/sortable, @dnd-kit/utilities | high | mitigate | Package-legitimacy already vetted "Approved" in 08-RESEARCH.md's Package Legitimacy Audit (npm registry, ~3 yrs old, millions of weekly downloads, single verifiable GitHub source, no postinstall scripts) — no [ASSUMED]/[SUS] verdict, so no additional blocking-human checkpoint is required beyond that existing audit. `@dnd-kit/utilities` (new, not separately audited in RESEARCH.md) is the same publisher/repo/release cadence as the two audited packages and is a pure CSS-transform-string helper with no network or filesystem access. |
| T-08-05-01 | Tampering | reorderLocations write path from drag/button UI | low | mitigate | Both input paths funnel through the same tested `reorderLocations` function (Plan 08-02), which only accepts an array of already-existing location ids — no arbitrary object is ever written. |
</threat_model>

<verification>
`npx tsc --noEmit` and `npm run build` pass. The human-check confirms drag visuals and button reordering on a real viewport.
</verification>

<success_criteria>
- Drag-and-drop and up/down buttons both reorder locations and persist via `reorderLocations`.
- Hidden locations stay reorderable; "Other" never appears as a row.
- First/last row boundary buttons are disabled correctly.
</success_criteria>

## Artifacts this phase produces (this plan)

- `SortableLocationList` component (src/components/SortableLocationList.tsx)
- `handleReorder`, `handleMove` (src/routes/locations/index.tsx)
- `@dnd-kit/core`, `@dnd-kit/sortable`, `@dnd-kit/utilities` (package.json dependencies)

<output>
Create `.planning/phases/08-full-location-management/08-05-SUMMARY.md` when done
</output>
