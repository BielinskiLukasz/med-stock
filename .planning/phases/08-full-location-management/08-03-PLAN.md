---
phase: 08-full-location-management
plan: 03
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - src/components/MedicineForm.tsx
  - src/components/StockFields.tsx
  - src/components/MoveStockSheet.tsx
  - src/components/FilterBottomSheet.tsx
  - src/lib/dataOps.ts
  - src/lib/dataOps.test.ts
autonomous: true
requirements: [LOC-02, LOC-04]

estimate:
  tokens: 46000
  raw_tokens: 46000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The Add/Edit form's location dropdown (MedicineForm.tsx) never lists a hidden location and sorts by order, not name (D-06, D-17)."
    - "The Stock Fields location dropdown (StockFields.tsx) never lists a hidden location and sorts by order, not name (D-06, D-17)."
    - "The Move/Split Stock sheet's target-location dropdown (MoveStockSheet.tsx) never lists a hidden location and sorts by order, not name (D-06, D-17)."
    - "The Filter sheet's location filter (FilterBottomSheet.tsx) continues to list hidden locations alongside visible ones — existing stock already sitting in a hidden location must stay filterable — and sorts by order, not name (D-07, D-17)."
    - "A JSON backup exported after this phase round-trips hidden and order through BackupSchema without loss."
    - "Importing a pre-v6 JSON backup that has no hidden/order fields on its locations array succeeds — those fields default to hidden: false and order: 0 rather than failing Zod validation."
  artifacts:
    - "src/components/MedicineForm.tsx, StockFields.tsx, MoveStockSheet.tsx — orderBy('order') + hidden filter on the location useLiveQuery"
    - "src/components/FilterBottomSheet.tsx — orderBy('order'), no hidden filter"
    - "src/lib/dataOps.ts — BackupSchema.locations entries carry hidden/order with backward-compatible defaults"
  key_links:
    - "MedicineForm/StockFields/MoveStockSheet useLiveQuery(() => db.locations.orderBy('order').filter(l => !l.hidden).toArray()) -> Select dropdown options"
    - "FilterBottomSheet useLiveQuery(() => db.locations.orderBy('order').toArray()) -> filter pill list (hidden locations included)"
  prohibitions:
    - "Never exclude hidden locations from the Filter sheet — existing stock already sitting in a hidden location must stay findable/filterable (D-07)."
    - "Never include a hidden location in the Add/Edit form or Move/Split Stock sheet dropdowns (D-06)."
    - "Never let the BackupSchema import/export round-trip silently drop the new hidden/order fields, and never let an old (pre-v6) backup lacking those fields fail validation outright."
---

<objective>
Wire the two hidden-location filtering rules (D-06 exclude, D-07 keep) into the four dropdown/filter call sites Dexie's `db.locations` feeds, and make the JSON backup schema forward- and backward-compatible with the new `hidden`/`order` fields. This is pure "read the new fields, filter/sort accordingly" work — no new business logic, and it depends only on Plan 08-01's schema landing, not on Plan 08-02's mutation functions, so it runs in parallel with 08-02.

Purpose: LOC-02's user-facing guarantee ("hidden locations disappear from assignment dropdowns but stay visible in the filter sheet") and LOC-04's ("order drives every dropdown, everywhere") land here, across every one of the remaining `db.locations.orderBy('name')` call sites Plan 08-01 didn't already touch.
Output: Four components reading `order`/`hidden` correctly, plus a backup schema that survives round-tripping through both directions.
</objective>

<execution_context>
@C:/my-code/vibe-coding/med-stock/.claude/gsd-core/workflows/execute-plan.md
@C:/my-code/vibe-coding/med-stock/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-full-location-management/08-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Filter hidden locations out of the three assignment dropdowns (D-06, D-17)</name>
  <files>src/components/MedicineForm.tsx, src/components/StockFields.tsx, src/components/MoveStockSheet.tsx</files>
  <read_first>
    src/components/MedicineForm.tsx (location useLiveQuery at line 75-78, SelectContent at lines 203-215)
    src/components/StockFields.tsx (location useLiveQuery at line 57-60, SelectContent at lines 117-130)
    src/components/MoveStockSheet.tsx (location useLiveQuery at line 54, SelectContent at lines 144-152)
    .planning/phases/08-full-location-management/08-CONTEXT.md (D-06, D-17, canonical_refs "UI call sites" list)
  </read_first>
  <action>
    In all three files, change the location `useLiveQuery` call from `db.locations.orderBy('name').toArray()` to `db.locations.orderBy('order').filter(l => !l.hidden).toArray()`. In each file's `SelectItem` that renders `{t(LOCATION_KEYS[loc.name] ?? loc.name)}` inside the location dropdown, add `className="truncate"` to the `SelectItem` so a very long location name (no `maxLength` is enforced on the name input) elides with an ellipsis instead of overflowing the dropdown — this is the same resolution the UI-SPEC applies to the list-row name, extended here to dropdown items per its explicit note that "one resolution covers both the list row and dropdown item cases." StockFields.tsx and MoveStockSheet.tsx already have a defensive `.filter(loc => loc.name !== 'Other')` before their `.map()` — leave that filter in place unchanged (harmless legacy guard; "Other" was never a real row per D-16).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <fails_when>non-zero exit, or any line matching "error TS"</fails_when>
    <automated>npm run build 2>&1 | tail -5</automated>
    <fails_when>output does not contain a successful build summary, or contains "error"</fails_when>
  </verify>
  <acceptance_criteria>
    - Grep for `orderBy('name')` in MedicineForm.tsx, StockFields.tsx, MoveStockSheet.tsx returns zero matches; all three now call `orderBy('order')`.
    - All three files' location query includes `.filter(l => !l.hidden)` (or equivalent `.where('hidden').equals(false)` semantics — either is acceptable as long as hidden rows are excluded).
  </acceptance_criteria>
  <done>MedicineForm, StockFields, and MoveStockSheet all sort location options by `order` and never surface a hidden location.</done>
</task>

<task type="auto">
  <name>Task 2: Sort the Filter sheet by order while keeping hidden locations visible (D-07, D-17)</name>
  <files>src/components/FilterBottomSheet.tsx</files>
  <read_first>
    src/components/FilterBottomSheet.tsx (location useLiveQuery at line 56, location filter render loop at lines 121-146)
    .planning/phases/08-full-location-management/08-CONTEXT.md (D-07)
  </read_first>
  <action>
    Change the location `useLiveQuery` from `db.locations.orderBy('name').toArray()` to `db.locations.orderBy('order').toArray()`. Do NOT add a `hidden` filter here — existing stock sitting in a hidden location must remain filterable (D-07). No other change to this file; the pill-button rendering loop and the `locations?.length === 0` empty state stay exactly as they are.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <fails_when>non-zero exit, or any line matching "error TS"</fails_when>
  </verify>
  <acceptance_criteria>
    - Grep for `orderBy('name')` in FilterBottomSheet.tsx returns zero matches.
    - No `.filter(l => !l.hidden)` or `.where('hidden')` clause is added to FilterBottomSheet.tsx's location query (hidden locations must still appear in this specific sheet).
  </acceptance_criteria>
  <done>FilterBottomSheet sorts by order and still lists hidden locations.</done>
</task>

<task type="auto">
  <name>Task 3: Round-trip hidden/order through BackupSchema (JSON export/import)</name>
  <files>src/lib/dataOps.ts, src/lib/dataOps.test.ts</files>
  <read_first>
    src/lib/dataOps.ts (BackupSchema.locations definition at lines 48-54, the packCount backward-compat precedent at line 26)
    src/lib/dataOps.test.ts (existing `locations: []` usages — none currently seed populated location objects)
    .planning/phases/08-full-location-management/08-CONTEXT.md ("CSV import / JSON export... the new hidden/order fields will need to round-trip through BackupSchema export/import without breaking existing backups")
  </read_first>
  <action>
    In `BackupSchema.locations`'s per-item object schema (src/lib/dataOps.ts), add two fields matching the same backward-compat pattern already used for `packCount` on the medicines schema: `hidden: z.boolean().optional().default(false)` and `order: z.number().optional().default(0)`. Do the same for `LegacyBackupSchema.locations`'s per-item object schema, since old-format backups round-trip through that schema too. No changes needed to `exportToJSON`/`importFromJSON` themselves — they already serialize/deserialize whatever the schema accepts, so widening the schema is sufficient. Add a test in dataOps.test.ts asserting `BackupSchema.safeParse` succeeds on a `locations` array item lacking `hidden`/`order` (simulating a pre-v6 backup) and that the parsed result defaults them to `false`/`0`; add a second test asserting a location item that DOES include `hidden: true, order: 3` parses through unchanged.
  </action>
  <verify>
    <automated>npx vitest run src/lib/dataOps.test.ts</automated>
    <fails_when>non-zero exit, or the summary line does not report every test in the file as passed</fails_when>
  </verify>
  <acceptance_criteria>
    - `BackupSchema.parse({ ...validShape, locations: [{ id: 1, name: 'X', isDefault: false }] })` (no hidden/order) succeeds and the result's location has `hidden === false` and `order === 0`.
    - `BackupSchema.parse({ ...validShape, locations: [{ id: 1, name: 'X', isDefault: false, hidden: true, order: 3 }] })` succeeds and preserves `hidden: true, order: 3` exactly.
  </acceptance_criteria>
  <done>Old and new backup shapes both parse successfully; hidden/order survive a full export→import round trip.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| JSON import → app state | `importFromJSON` accepts arbitrary user-selected files; Zod validation is the only gate before data reaches Dexie. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-03-01 | Tampering | BackupSchema.locations hidden/order fields | medium | mitigate | `z.boolean()`/`z.number()` on both fields rejects any non-boolean/non-number value in a crafted or corrupted backup file before it reaches `db.locations.bulkAdd` — malformed types never enter the database. |
| T-08-03-02 | Information Disclosure | hidden locations still visible in FilterBottomSheet | low | accept | Deliberate per D-07 — this is a household-local single-user PWA with no shared/multi-tenant access; "hidden" means "decluttered from assignment", not "access-restricted". |
</threat_model>

<verification>
`npx tsc --noEmit` and `npm run build` pass after Tasks 1-2. `npx vitest run src/lib/dataOps.test.ts` passes after Task 3.
</verification>

<success_criteria>
- All four dropdown/filter call sites sort by `order`.
- MedicineForm, StockFields, MoveStockSheet exclude hidden locations; FilterBottomSheet does not.
- BackupSchema and LegacyBackupSchema both accept locations with or without hidden/order, defaulting sanely when absent.
</success_criteria>

## Artifacts this phase produces (this plan)

- `BackupSchema.locations[].hidden: boolean` (optional, default false, dataOps.ts)
- `BackupSchema.locations[].order: number` (optional, default 0, dataOps.ts)
- `LegacyBackupSchema.locations[].hidden`/`.order` (same defaults, dataOps.ts)

<output>
Create `.planning/phases/08-full-location-management/08-03-SUMMARY.md` when done
</output>

