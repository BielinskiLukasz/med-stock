---
context: phase
phase: 08-full-location-management
task: null
total_tasks: null
status: ready_to_plan
last_updated: 2026-09-17T15:52:59.320Z
---

## Critical Anti-Patterns

_None discovered this session — no blocking rows to acknowledge._

<current_state>
Phase 8 (Full Location Management) discussion is fully complete. `08-CONTEXT.md` and `08-DISCUSSION-LOG.md` are written and committed. STATE.md reflects the session. This is a clean phase-boundary pause — no plan exists yet, nothing is in progress, nothing is uncommitted. The next step is simply to plan the phase.

This session also cleaned up a stale `.planning/HANDOFF.json` left over from Phase 07's final gap-closure cycle — the repo had already moved 8+ commits past what it described, so it was discarded rather than resumed from.
</current_state>

<completed_work>

Completed Tasks:
- Resumed session (`/gsd-resume-work`) — discarded the stale Phase 07 handoff artifact
- Ran `/gsd-discuss-phase 8` — discussed all 4 gray areas: Delete with references, Hide scope & visibility, Reorder interaction, Rename predefined names
- Wrote `08-CONTEXT.md` (17 decisions, D-01 through D-17) and `08-DISCUSSION-LOG.md`
- Committed context (`22accbb`), STATE.md session record (`b6f3cc6`), and the stale-handoff cleanup (`73b0dd7`)
</completed_work>

<remaining_work>

- Plan Phase 8: `/gsd-plan-phase 8`
- Execute Phase 8 plan(s): `/gsd-execute-phase 8`
</remaining_work>

<decisions_made>

See `.planning/phases/08-full-location-management/08-CONTEXT.md` for the full list with rationale. Headline decisions:
- `isDefault` no longer blocks rename/delete — predefined locations become fully editable like custom ones
- Renamed predefined locations become plain strings (fall out of `LOCATION_KEYS`) — no new i18n override mechanism
- `hidden: boolean` applies to ALL locations; hidden ones excluded from Add/Edit + Move/Split dropdowns but stay visible in the Filter sheet
- Delete-with-references: inline reassign-or-clear dialog, active stock only (Trash entries excluded), no minimum-location floor
- New `order: number` field via `db.version(6)`; existing locations get alphabetical initial order (visually silent upgrade)
- Reorder UI: drag handle + up/down buttons (no drag library exists in the repo yet — new dependency decision for planning)
- Rename/add blocked on case-insensitive name collisions; names stored exactly as typed, no title-casing
</decisions_made>

<blockers>
None.
</blockers>

## Required Reading (in order)
1. `.planning/phases/08-full-location-management/08-CONTEXT.md` — the 17 locked implementation decisions; planner reads this directly
2. `.planning/ROADMAP.md` (Phase 8 section) — goal, success criteria, LOC-01–04 requirement IDs
3. `src/lib/db.ts` — current `Location` interface and schema versions 1–5 (v6 must be additive, never edit v5)
4. `src/lib/locationOps.ts` — current `addCustomLocation`/`renameLocation`/`deleteLocation`, all of which need changes

## Critical Anti-Patterns (do NOT repeat these)
_None discovered this session._

## Infrastructure State
- No dev server or background processes running
- Working tree clean at session end (verified via `git status`)

<context>
Phase 8 requires making ALL locations (predefined + custom) fully controllable: rename, hide, delete (with reassign-or-clear safety), reorder (persisted, drag + buttons). The current schema only has `isDefault` (a boolean that currently blocks rename/delete on the 7 seeded locations) — Phase 8 removes that restriction entirely and adds two new fields (`hidden`, `order`) via a new `db.version(6)` migration. Six different files currently query `db.locations.orderBy('name')` directly; all six need to switch to the new `order` field — this fan-out is the main mechanical risk, similar to the "grep not enumerated list" lesson from Phase 07's gap-closure cycles. No drag/sortable library exists yet, so that's a new dependency to evaluate during planning/research.
</context>

<next_action>
Start with: `/gsd-plan-phase 8` — it will read `08-CONTEXT.md` for the locked decisions and `has_research: false` so it may also want to research drag/sortable library options for a mobile-first PWA list (per Claude's Discretion note in CONTEXT.md).
</next_action>
