---
phase: 08-full-location-management
plan: 04
type: execute
wave: 3
depends_on: ["08-01", "08-02"]
files_modified:
  - src/routes/locations/index.tsx
autonomous: true
requirements: [LOC-02, LOC-03]

estimate:
  tokens: 44000
  raw_tokens: 44000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Every row on the Locations screen shows a hide/show toggle (Eye/EyeOff icon) that calls toggleLocationHidden — available on predefined and custom locations alike, with no isDefault branch (D-05)."
    - "A hidden row displays a 'Hidden' badge (t('locations.hidden')) and is visually muted (opacity-60), while staying in the same inline list — not a separate section (D-08)."
    - "Deleting a location with zero active references still opens a confirm dialog before deleting — there is no silent/instant delete path (D-11)."
    - "Deleting a location with one or more active references opens a dialog with a live count and a reassign-target Select (excluding the location being deleted and excluding hidden locations) or a 'Clear (set to Other)' option; the confirm button stays disabled until a target is chosen (D-09, resolves the UI-SPEC's unresolved 'partial' row)."
    - "Confirming the reassign dialog calls deleteLocationWithReassign(id, target) and both the reassignment and the delete land atomically (D-09)."
    - "The Add/Edit-location and inline rename inputs enforce maxLength={60} — a defensive resolution of the UI-SPEC's unresolved 'long-text' row, since no length limit existed before this phase."
    - "The Add/rename catch blocks distinguish a duplicate-name error from other errors: when addCustomLocation/renameLocation throws 'Location name already exists', the screen shows t('locations.errorDuplicate') instead of the generic errorAdd/errorRename message."
    - "When zero locations exist, the screen renders t('locations.emptyHeading') / t('locations.emptyBody') instead of an empty list, and the 'Add location' button remains visible so recovery is one tap away (D-12, resolves the UI-SPEC empty-state row)."
    - "The location name span uses a single-line truncate with a native title attribute carrying the full name, so an overflowing name is still recoverable on hover/long-press (resolves the UI-SPEC overflow/long-text row)."
  artifacts:
    - "src/routes/locations/index.tsx — hide/show toggle, delete-with-reassign AlertDialog + Select, empty state, maxLength, error branching"
  key_links:
    - "handleToggleHidden(id, current) -> toggleLocationHidden(id, !current) -> useLiveQuery re-render"
    - "AlertDialog confirm -> countActiveLocationReferences(loc.name) decides zero-ref vs reassign-ref dialog body -> deleteLocationWithReassign(loc.id, reassignTarget)"
  prohibitions:
    - "Never delete a location without a confirm dialog, even with zero active references (D-11)."
    - "Never default the reassign target to 'Other' when the user has not explicitly chosen one — the confirm button must stay disabled until a selection is made."
    - "Never let the reassign Select list the location currently being deleted, or any hidden location, as a reassignment target."
---

<objective>
Rewrite the Locations screen's row actions and delete flow: add the hide/show toggle (LOC-02) and replace the simple delete confirm with the reassign-or-clear flow (LOC-03), on top of the `toggleLocationHidden`/`countActiveLocationReferences`/`deleteLocationWithReassign` functions Plan 08-02 already tested.

Purpose: This is the screen where a household member actually declutters and safely removes locations — every D-05/D-08/D-09/D-11/D-12 decision from CONTEXT.md becomes visible here.
Output: `LocationsScreen` with hide/show toggles, a reassign-or-clear delete dialog, an empty state, and the duplicate-name error branch.
</objective>

<execution_context>
@C:/my-code/vibe-coding/med-stock/.claude/gsd-core/workflows/execute-plan.md
@C:/my-code/vibe-coding/med-stock/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-full-location-management/08-CONTEXT.md
@.planning/phases/08-full-location-management/08-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Hide/show toggle, "Hidden" badge, empty state, maxLength, duplicate-error branch</name>
  <files>src/routes/locations/index.tsx</files>
  <read_first>
    src/routes/locations/index.tsx (post-08-01 state — Edit button already ungated, orderBy('order') already in place)
    src/lib/locationOps.ts (toggleLocationHidden signature from Plan 08-02)
    .planning/phases/08-full-location-management/08-UI-SPEC.md ("Location Row Interaction Design", "Copywriting Contract", "UI Considerations" — the empty/error/overflow/long-text rows)
  </read_first>
  <action>
    Import `EyeOff` and `Eye` from `lucide-react`, and `toggleLocationHidden` from `@/lib/locationOps` (alongside the existing `addCustomLocation, renameLocation` import — drop nothing yet, Task 2 replaces `deleteLocation`). Add `async function handleToggleHidden(id: number, currentlyHidden: boolean)` calling `toggleLocationHidden(id, !currentlyHidden)` wrapped in the same try/catch + `setError` pattern as `handleDelete`.
    In the row render: add a `Button variant="ghost" size="sm"` with `<Eye>`/`<EyeOff>` icon (swap based on `loc.hidden`), `aria-label={t(loc.hidden ? 'aria.showLocation' : 'aria.hideLocation')}`, calling `handleToggleHidden(loc.id, loc.hidden)` — place it before the Edit button, available unconditionally on every row (predefined and custom alike, no `isDefault` branch, per D-05). When `loc.hidden` is true, render `<span className="text-xs text-muted-foreground ml-2">{t('locations.hidden')}</span>` next to the name, and apply `opacity-60` to the row's outer `<div>` (currently `className="flex items-center justify-between py-2 border-b last:border-0"`).
    Change the location-name `<span className="text-sm">` to `<span className="text-sm truncate" title={LOCATION_KEYS[loc.name] ? t(LOCATION_KEYS[loc.name]) : loc.name}>` (same truncate + native-title pattern the UI-SPEC applies everywhere long text can overflow).
    Add `maxLength={60}` to both the add-location `<Input>` (`id="new-location-name"`) and the rename `<Input>` (the one bound to `editValue`).
    Add an empty-state block: when `locations.length === 0`, render a heading `t('locations.emptyHeading')` and body `t('locations.emptyBody')` in place of the (now-empty) `<div className="space-y-1">` row list — the existing "Add location" button above it stays untouched and visible regardless.
    In `handleAdd`'s and `handleRename`'s catch blocks, branch on the thrown error: if `err instanceof Error && err.message === 'Location name already exists'`, call `setError(t('locations.errorDuplicate'))`; otherwise keep the existing `setError(t('locations.errorAdd'))` / `setError(t('locations.errorRename'))` fallback.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <fails_when>non-zero exit, or any line matching "error TS"</fails_when>
  </verify>
  <acceptance_criteria>
    - The hide/show toggle button renders on every row regardless of `loc.isDefault` (grep the row-render JSX: no `!loc.isDefault &&` wraps the new toggle button).
    - `t('locations.hidden')` badge and `opacity-60` styling appear conditionally on `loc.hidden`.
    - `locations.length === 0` renders `t('locations.emptyHeading')`/`t('locations.emptyBody')`.
    - `handleAdd`/`handleRename` catch blocks check for the `'Location name already exists'` message before falling back to the generic error.
  </acceptance_criteria>
  <done>Hide/show works on every row, hidden rows are visibly muted with a badge, the empty state renders, names truncate with a title tooltip, inputs cap at 60 chars, and duplicate-name errors show distinct copy. `npx tsc --noEmit` passes.</done>
</task>

<task type="auto">
  <name>Task 2: Delete-with-reassign dialog replacing the simple delete confirm</name>
  <files>src/routes/locations/index.tsx</files>
  <read_first>
    src/routes/locations/index.tsx (state after Task 1 — the still-isDefault-guarded AlertDialog delete block from Plan 08-01)
    src/lib/locationOps.ts (countActiveLocationReferences, deleteLocationWithReassign signatures from Plan 08-02)
    .planning/phases/08-full-location-management/08-UI-SPEC.md ("Delete-with-Reassign Dialog" section — exact copy keys and Select behavior)
  </read_first>
  <action>
    Import `countActiveLocationReferences, deleteLocationWithReassign` from `@/lib/locationOps` in place of `deleteLocation`; remove the `deleteLocation` import entirely. Import `Select, SelectContent, SelectItem, SelectTrigger, SelectValue` from `@/components/ui/select` and `Label` from `@/components/ui/label`.
    Add component state: `const [deleteTargetId, setDeleteTargetId] = useState<number | null>(null)`, `const [activeRefCount, setActiveRefCount] = useState(0)`, `const [reassignTo, setReassignTo] = useState<string | null | undefined>(undefined)` (undefined = "nothing chosen yet", distinct from `null` which means "Other").
    On `AlertDialogTrigger` click (or an `onOpenChange` handler on the `AlertDialog`), call `countActiveLocationReferences(loc.name)` and store the result in `activeRefCount`, and reset `reassignTo` to `undefined` and `deleteTargetId` to `loc.id`.
    Render two dialog bodies conditionally on `activeRefCount === 0` vs `> 0`: the zero-reference body keeps the existing `t('locations.deleteConfirmTitle')`/`t('locations.deleteConfirmBody')` copy and its confirm button calls `deleteLocationWithReassign(loc.id, null)` (no active refs means the clear-to-null branch is a no-op reassignment, so passing `null` is safe and simpler than a special "no-op" path). The active-reference body shows `t('locations.deleteReassignTitle')` as the title and, as the body, `t('locations.deleteReassignBody').replace('{n}', String(activeRefCount))` (the same manual `{n}`-interpolation workaround Phase 7 used for `ImportJSONSection`, per the UI-SPEC's interpolation note) — followed by a `Label` reading `t('locations.reassignLabel')` and a `Select` bound to `reassignTo`, whose `SelectContent` lists every OTHER non-hidden location (`locations.filter(l => l.id !== loc.id && !l.hidden)`, each `SelectItem value={l.name}`) plus one final `SelectItem value="__OTHER__"` reading `t('locations.reassignOther')` mapping to `null`. The footer confirm button reads `t('locations.confirmReassignDelete')`, uses `variant="destructive"`, is `disabled={reassignTo === undefined}` (guarding the "no default selection" case per the UI-SPEC's resolved unresolved-row), and on click calls `deleteLocationWithReassign(loc.id, reassignTo === '__OTHER__' ? null : reassignTo ?? null)` then closes the dialog. Both dialog variants' `AlertDialogCancel` stay `t('form.cancel')`. Keep the existing `handleDelete`'s catch-block error copy (`t('locations.errorDelete')`) wrapping whichever confirm path is used. Remove the outer `{!loc.isDefault && (...)}` guard around this entire delete block — delete is now available on every row (D-09 through D-12 apply uniformly, no isDefault branch).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <fails_when>non-zero exit, or any line matching "error TS"</fails_when>
    <automated>npm run build 2>&1 | tail -5</automated>
    <fails_when>output does not contain a successful build summary, or contains "error"</fails_when>
  </verify>
  <acceptance_criteria>
    - The delete AlertDialog is no longer wrapped in `{!loc.isDefault && (...)}` — it renders for every row.
    - The reassign `Select`'s options exclude the location being deleted and exclude every `hidden: true` location.
    - The confirm button in the reassign-body variant is `disabled` whenever `reassignTo === undefined`.
    - `deleteLocation` no longer appears anywhere in `src/routes/locations/index.tsx`.
  </acceptance_criteria>
  <done>Deleting a zero-reference location still confirms first; deleting a referenced location shows the live count, blocks confirm until a target is chosen, and calls `deleteLocationWithReassign` atomically. `npx tsc --noEmit` and `npm run build` pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| None crossed | Client-side UI wiring against already-vetted `locationOps.ts` functions (Plan 08-02); no new input source. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-04-01 | Tampering | Delete-with-reassign confirm button | medium | mitigate | `disabled={reassignTo === undefined}` prevents submitting the reassign dialog with no explicit target, closing the one way this screen could otherwise silently default-reassign to the wrong place. |
| T-08-04-02 | Information Disclosure | "Hidden" badge visible to any screen viewer | low | accept | Deliberate per D-08 — single-user local household PWA, "hidden" is a declutter affordance, not an access control. |
</threat_model>

<verification>
`npx tsc --noEmit` and `npm run build` pass after both tasks.
</verification>

<success_criteria>
- Hide/show toggle works on every row with correct badge/muting.
- Delete flow branches correctly on active-reference count, blocks confirm until a target is chosen, and never lists the deleted or a hidden location as a reassignment target.
- Empty state, maxLength, and duplicate-error copy are all in place.
</success_criteria>

## Artifacts this phase produces (this plan)

- Hide/show toggle button + "Hidden" badge (src/routes/locations/index.tsx)
- Delete-with-reassign `AlertDialog` + `Select` (src/routes/locations/index.tsx)
- Empty-state block (src/routes/locations/index.tsx)

<output>
Create `.planning/phases/08-full-location-management/08-04-SUMMARY.md` when done
</output>

