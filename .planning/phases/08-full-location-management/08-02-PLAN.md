---
phase: 08-full-location-management
plan: 02
type: tdd
wave: 2
depends_on: ["08-01"]
files_modified:
  - src/lib/locationOps.ts
  - src/lib/db.test.ts
autonomous: true
requirements: [LOC-01, LOC-02, LOC-03, LOC-04]

estimate:
  tokens: 58000
  raw_tokens: 58000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "addCustomLocation and renameLocation both reject a name that collides case-insensitively (after trimming) with any other existing location, and both leave the offending name untouched on rejection (D-03)."
    - "Location names are stored exactly as typed, trimmed only — no title-casing transformation, unlike the medicine catalog's title-case convention (D-04)."
    - "addCustomLocation assigns a new location the next order value after the current maximum, so it never collides with an existing order and never sorts unpredictably (Pitfall 3)."
    - "deleteLocationWithReassign, when the target location has one or more active (deletedAt === null) stock entries, reassigns all of them to the chosen target location or clears them to null (\"Other\") and deletes the location row, all inside one db.transaction (D-09)."
    - "Soft-deleted (Trash) stock entries are never counted or touched by countActiveLocationReferences or deleteLocationWithReassign — only active entries (deletedAt === null) participate (D-10)."
    - "A location with zero active references still requires deleteLocationWithReassign to run its normal transaction — there is no silent/instant delete path, and no minimum-location floor blocks deleting the last remaining location (D-11, D-12)."
    - "toggleLocationHidden flips a location's hidden flag independent of isDefault — predefined and custom locations use the identical toggle (D-05)."
    - "reorderLocations accepts the full list of location ids in their new desired order and renumbers every one of them to contiguous integers 1..N — no gaps, no duplicate order values survive a reorder (D-13 Strategy A)."
    - "Calling deleteLocationWithReassign a second time on an already-deleted locationId throws \"Location not found\" — the transaction's db.locations.get() returns undefined, so no duplicate reassignment or double-delete side effect occurs on repeat calls."
    - "The bulk reassign/clear update and the location delete inside deleteLocationWithReassign are atomic — Dexie's db.transaction('rw', ...) guarantees both succeed or both roll back together; there is no window where medicines are reassigned but the location row still exists, or vice versa."
    - "reorderLocations produces order values that are always unique contiguous integers after every mutation (add, delete, reorder) — two locations never compare equal on order, so display order is always deterministic."
    - "Reordering an empty or single-item location list is a no-op; \"Other\" (null location) is never passed into reorderLocations because it has no Location row to begin with (D-16)."
    - "Renaming a predefined location permanently falls it out of LOCATION_KEYS lookup — confirmed here at the locationOps layer by the collision/rename logic making no distinction between isDefault and custom rows (D-02, cross-checked with Plan 08-01)."
  artifacts:
    - "src/lib/locationOps.ts — addCustomLocation (collision + order), renameLocation (collision), countActiveLocationReferences, deleteLocationWithReassign, toggleLocationHidden, reorderLocations"
    - "src/lib/db.test.ts — describe blocks for every function above, replacing the obsolete isDefault-guard tests for add/delete"
  key_links:
    - "deleteLocationWithReassign -> db.medicines.where('location').equals(loc.name).filter(m => m.deletedAt === null).modify(...) -> db.locations.delete(locationId), all inside one db.transaction('rw', db.locations, db.medicines, ...)"
    - "reorderLocations -> db.locations.bulkUpdate(orderedIds.map((id, i) => ({ key: id, changes: { order: i + 1 } })))"
  prohibitions:
    - "Never store the literal string 'Other' in Location.name or Medicine.location — deleteLocationWithReassign's clear branch must set location: null, never the string 'Other'."
    - "Never title-case, trim-only-partially, or otherwise transform a location name beyond a full trim() in addCustomLocation or renameLocation."
    - "Never allow two locations to coexist with the same case-insensitive, trimmed name."
    - "Never query db.medicines.where('deletedAt').equals(null) — null is not a valid IndexedDB key; use .filter(m => m.deletedAt === null) when counting or reassigning active stock."
    - "Never block deleteLocationWithReassign because it would leave zero locations in the table — no minimum-location floor exists."
    - "Never leave duplicate or non-contiguous order values after reorderLocations completes — every affected row must be renumbered."
---

<objective>
Harden `locationOps.ts` into the full mutation surface Phase 8 needs: collision-safe add/rename, atomic delete-with-reassign, a hide/show toggle, and contiguous-order reordering. This is the business-logic core behind LOC-01 (collision safety), LOC-02 (hide/show), LOC-03 (delete-with-reassign), and LOC-04 (reorder) — pure, testable, deterministic I/O with no UI involved, so it gets a dedicated TDD plan per the tdd_mode_active guidance for this phase.

Purpose: Every downstream UI plan (08-03 dropdown filtering, 08-04 hide/delete screen, 08-05 reorder UI) calls straight into these functions — get the contracts and edge cases right here, under test, before any component wires them.
Output: `locationOps.ts` exporting `addCustomLocation`, `renameLocation`, `countActiveLocationReferences`, `deleteLocationWithReassign`, `toggleLocationHidden`, `reorderLocations` — all covered by `db.test.ts`.
</objective>

<execution_context>
@C:/my-code/vibe-coding/med-stock/.claude/gsd-core/workflows/execute-plan.md
@C:/my-code/vibe-coding/med-stock/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-full-location-management/08-CONTEXT.md
@.planning/phases/08-full-location-management/08-RESEARCH.md
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: Collision-safe addCustomLocation + renameLocation, with order assignment on add</name>
  <files>src/lib/locationOps.ts, src/lib/db.test.ts</files>
  <read_first>
    src/lib/locationOps.ts (post-08-01 state — renameLocation with the isDefault guard already removed)
    src/lib/db.test.ts (existing addCustomLocation/renameLocation describe blocks)
    .planning/phases/08-full-location-management/08-CONTEXT.md (D-03, D-04, canonical_refs "Common Pitfalls" pitfall 1 and 3)
  </read_first>
  <behavior>
    - `addCustomLocation('Pantry')` then `addCustomLocation('pantry')` — the second call rejects with an Error whose message signals a duplicate (e.g. "Location name already exists"), and no second row is created.
    - `addCustomLocation('  Fridge  ')` stores the name as `'Fridge'` (trimmed, not title-cased, not lower/upper-cased).
    - `addCustomLocation` on a table whose existing max `order` is 5 creates the new row with `order: 6`; on an empty table it creates `order: 1`.
    - `renameLocation(idOfLocationA, 'Bathroom Cabinet')` rejects when a different location already named `'Bathroom Cabinet'` (any case) exists; renaming a location to its own current name (case-identical or not) still succeeds since it is not colliding with a *different* row.
  </behavior>
  <action>
    Rewrite `addCustomLocation(name)`: trim the input; if empty, keep the existing "Location name cannot be empty" throw. Query `db.locations.toCollection().filter(loc => loc.name.toLowerCase() === trimmed.toLowerCase())` — if any row matches, throw `new Error('Location name already exists')`. Compute the next order value: `const maxOrder = await db.locations.orderBy('order').reverse().first(); const nextOrder = (maxOrder?.order ?? 0) + 1`. Call `db.locations.add({ name: trimmed, isDefault: false, hidden: false, order: nextOrder })`.
    Rewrite `renameLocation(locationId, newName)`: trim the input, keep the empty-name throw. Before the existing transaction, query the same case-insensitive collision check but EXCLUDE the row being renamed: `db.locations.toCollection().filter(loc => loc.id !== locationId && loc.name.toLowerCase() === trimmed.toLowerCase())` — if any match, throw `new Error('Location name already exists')`. Keep the existing transaction body (bulk-update medicines, update the location) unchanged.
  </action>
  <verify>
    <automated>npx vitest run src/lib/db.test.ts -t "addCustomLocation|renameLocation"</automated>
    <fails_when>non-zero exit, or the summary line reports fewer passed tests than exist for these two describe blocks</fails_when>
  </verify>
  <acceptance_criteria>
    - A case-insensitive duplicate name (differing only in case and/or leading/trailing whitespace) is rejected by both `addCustomLocation` and `renameLocation`, proven by a test that adds/renames to an existing name in a different case and asserts a throw.
    - A test asserts `addCustomLocation` assigns a strictly increasing `order` relative to the current max.
    - A test asserts renaming a location to a name matching its own current name (not colliding with a *different* row) still succeeds.
  </acceptance_criteria>
  <done>Collision checks and order assignment pass under `npx vitest run src/lib/db.test.ts`.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: countActiveLocationReferences + deleteLocationWithReassign (replaces deleteLocation)</name>
  <files>src/lib/locationOps.ts, src/lib/db.test.ts</files>
  <read_first>
    src/lib/locationOps.ts (current deleteLocation implementation to replace)
    src/lib/db.test.ts (existing deleteLocation describe block — including the now-obsolete "throws when trying to delete a default location" test)
    .planning/phases/08-full-location-management/08-CONTEXT.md (D-09, D-10, D-11, D-12)
    src/lib/historyOps.ts (lines with permanentDeleteMedicine — the multi-step atomic transaction pattern to mirror)
  </read_first>
  <behavior>
    - `countActiveLocationReferences('Kitchen Drawer')` counts only medicines where `location === 'Kitchen Drawer' && deletedAt === null`; a soft-deleted medicine with that location is excluded from the count.
    - `deleteLocationWithReassign(id, 'Bathroom Cabinet')` on a location with 2 active + 1 soft-deleted medicine referencing it: the 2 active medicines' `location` becomes `'Bathroom Cabinet'`, the soft-deleted one's `location` is untouched, and the location row is gone.
    - `deleteLocationWithReassign(id, null)` clears matching active medicines' `location` to `null` (never the string `'Other'`).
    - `deleteLocationWithReassign` on a location with zero active references still deletes it successfully (no special-cased throw for the zero-reference case).
    - Calling `deleteLocationWithReassign` twice with the same (now-deleted) id throws `'Location not found'` on the second call.
    - Deleting the last remaining location in the table succeeds (no minimum-floor check anywhere in the function).
  </behavior>
  <action>
    Add `countActiveLocationReferences(locationName: string): Promise<number>` — returns `db.medicines.where('location').equals(locationName).filter(m => m.deletedAt === null).count()`.
    Replace `deleteLocation` entirely with `deleteLocationWithReassign(locationId: number, reassignTo: string | null): Promise<void>`, following the exact `db.transaction('rw', db.locations, db.medicines, async () => {...})` shape already used by `renameLocation`: fetch the location, throw `'Location not found'` if absent (mirrors the existing guard), then `db.medicines.where('location').equals(loc.name).filter(m => m.deletedAt === null).modify({ location: reassignTo })`, then `db.locations.delete(locationId)`. Do not reintroduce any `isDefault` guard and do not add any check on remaining location count.
    Remove the old `deleteLocation` export and its now-incorrect db.test.ts describe block (the "throws when trying to delete a default location" test is obsolete per D-01/D-09-12 removing that guard entirely) — replace with new tests covering the behaviors above, including one seeding a soft-deleted (`deletedAt` set to a real ISO string) medicine to prove it is excluded.
  </action>
  <verify>
    <automated>npx vitest run src/lib/db.test.ts -t "deleteLocationWithReassign|countActiveLocationReferences"</automated>
    <fails_when>non-zero exit, or the summary line reports fewer passed tests than the describe blocks for these two functions contain</fails_when>
  </verify>
  <acceptance_criteria>
    - No call site in `src/` still imports or calls `deleteLocation` (grep for `deleteLocation\b` outside of `deleteLocationWithReassign` returns zero hits after this task; existing call sites are only in db.test.ts, updated by this task, and LocationsScreen, updated by Plan 08-04).
    - A test proves a soft-deleted medicine referencing the target location is excluded from both the count and the reassignment.
    - A test proves the second call on an already-deleted id throws `'Location not found'`.
  </acceptance_criteria>
  <done>Delete-with-reassign, its reference counter, and their edge cases (soft-delete exclusion, zero-reference, double-call, last-location) all pass under `npx vitest run src/lib/db.test.ts`.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 3: toggleLocationHidden + reorderLocations (contiguous renumbering)</name>
  <files>src/lib/locationOps.ts, src/lib/db.test.ts</files>
  <read_first>
    src/lib/locationOps.ts (state after Tasks 1-2)
    .planning/phases/08-full-location-management/08-CONTEXT.md (D-05, D-13, D-14, D-15, D-16)
    .planning/phases/08-full-location-management/08-RESEARCH.md ("Order Field Renumbering Strategy" section — Strategy A, contiguous integers)
  </read_first>
  <behavior>
    - `toggleLocationHidden(id, true)` sets that location's `hidden` to `true` and leaves every other field (including `isDefault`) untouched; `toggleLocationHidden(id, false)` reverses it. Works identically whether `isDefault` is true or false.
    - `reorderLocations([idC, idA, idB])` on three locations currently at order 1,2,3 (idA=1, idB=2, idC=3) results in idC=order 1, idA=order 2, idB=order 3 — contiguous, matching the array's new position order.
    - `reorderLocations` on an empty array or a single-element array does not throw and leaves order values as-is (no-op).
    - After any `reorderLocations` call, every location's `order` value is unique — no two rows share the same order.
  </behavior>
  <action>
    Add `toggleLocationHidden(locationId: number, hidden: boolean): Promise<void>` — a direct `db.locations.update(locationId, { hidden })` call, no transaction needed (single-table, single-row write).
    Add `reorderLocations(orderedIds: number[]): Promise<void>` — inside `db.transaction('rw', db.locations, async () => { const updates = orderedIds.map((id, i) => ({ key: id, changes: { order: i + 1 } })); await db.locations.bulkUpdate(updates) })`. If `orderedIds.length <= 1`, return immediately without touching the database (no-op, matches the "empty/single-element" edge-probe resolution — D-16's "Other" never appears in this array because callers filter it out before it reaches this function; nothing in `reorderLocations` needs to special-case it).
  </action>
  <verify>
    <automated>npx vitest run src/lib/db.test.ts -t "toggleLocationHidden|reorderLocations"</automated>
    <fails_when>non-zero exit, or the summary line reports fewer passed tests than the describe blocks for these two functions contain</fails_when>
    <automated>npx vitest run src/lib/db.test.ts</automated>
    <fails_when>non-zero exit, or the summary line does not report every test in the file as passed (full-file regression check after three tasks of edits to the same file)</fails_when>
  </verify>
  <acceptance_criteria>
    - A test proves `toggleLocationHidden` works on an `isDefault: true` row exactly like a custom row.
    - A test proves `reorderLocations` produces contiguous, unique order values after a 3-item reorder.
    - A test proves an empty or single-element array is a safe no-op.
  </acceptance_criteria>
  <done>toggleLocationHidden and reorderLocations pass under test, and the full `npx vitest run src/lib/db.test.ts` file passes end to end after all three tasks in this plan.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| None crossed | Pure client-side Dexie mutation logic; the only "input" is data the app itself already holds (location names typed by the household member using their own device). No network or auth boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-02-01 | Tampering | deleteLocationWithReassign bulk `.modify()` | high | mitigate | The reassign/clear update and the location delete run inside one `db.transaction('rw', ...)` — Dexie guarantees all-or-nothing, preventing a half-applied state where medicines point at a location id that no longer exists. |
| T-08-02-02 | Repudiation | reorderLocations bulkUpdate | low | accept | Reorder operations are not audit-logged (unlike medicine history) — an existing, accepted gap for this local-only feature, not introduced by this plan. |
| T-08-02-03 | Denial of Service | reorderLocations renumbering all rows on every call | low | accept | Bounded to the household's location count (single digits to low tens in practice); renumbering ≤15 rows on IndexedDB is negligible cost (RESEARCH.md Assumption A2). |
</threat_model>

<verification>
`npx vitest run src/lib/db.test.ts` passes in full after all three tasks. `npx tsc --noEmit` passes (no callers broken by the removed `deleteLocation` export — Plan 08-04 is the only consumer and lands in a later wave, so at this point in the phase no source file outside db.test.ts should reference it).
</verification>

<success_criteria>
- `locationOps.ts` exports exactly: `addCustomLocation`, `renameLocation`, `countActiveLocationReferences`, `deleteLocationWithReassign`, `toggleLocationHidden`, `reorderLocations` (no `deleteLocation`).
- Every behavior listed above is proven by a passing vitest test in `db.test.ts`.
</success_criteria>

## Artifacts this phase produces (this plan)

- `countActiveLocationReferences(locationName: string): Promise<number>` (new function, locationOps.ts)
- `deleteLocationWithReassign(locationId: number, reassignTo: string | null): Promise<void>` (new function, locationOps.ts — replaces `deleteLocation`)
- `toggleLocationHidden(locationId: number, hidden: boolean): Promise<void>` (new function, locationOps.ts)
- `reorderLocations(orderedIds: number[]): Promise<void>` (new function, locationOps.ts)

<output>
Create `.planning/phases/08-full-location-management/08-02-SUMMARY.md` when done
</output>
