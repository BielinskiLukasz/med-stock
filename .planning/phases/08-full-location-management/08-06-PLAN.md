---
phase: 08-full-location-management
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/MedicineForm.tsx
  - src/components/StockFields.tsx
autonomous: true
gap_closure: true
requirements: [LOC-01]

estimate:
  tokens: 20000
  raw_tokens: 20000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Quick-add-location in MedicineForm.tsx creates a new Location exclusively via addCustomLocation(trimmed), inheriting D-03's case-insensitive, trimmed-name collision rejection."
    - "Quick-add-location in StockFields.tsx creates a new Location exclusively via addCustomLocation(trimmed), inheriting D-03's case-insensitive, trimmed-name collision rejection."
    - "When a quick-add attempt collides case-insensitively with an existing location name, both components show t('locations.errorDuplicate') via toast.error rather than the generic toasts.locationFailed fallback."
    - "New locations created via either quick-add path receive the correct max(order)+1 value from addCustomLocation — no hardcoded sentinel order value remains in either component."
  artifacts:
    - "src/components/MedicineForm.tsx — handleAddLocation delegates to addCustomLocation"
    - "src/components/StockFields.tsx — handleAddLocation delegates to addCustomLocation"
  key_links:
    - "MedicineForm.handleAddLocation -> locationOps.addCustomLocation -> db.locations.add (collision-checked, order-correct)"
    - "StockFields.handleAddLocation -> locationOps.addCustomLocation -> db.locations.add (collision-checked, order-correct)"
  prohibitions:
    - "Never insert a Location row via db.locations.add() directly from a UI component — all creation must route through addCustomLocation() (D-03)."
    - "Never let a duplicate-name error surface as the generic toasts.locationFailed message when the underlying cause is a name collision — use locations.errorDuplicate specifically for that case."
---

<objective>
Close the CR-01 blocker confirmed by 08-VERIFICATION.md: `MedicineForm.tsx` and `StockFields.tsx` both create locations via a direct `db.locations.add()` call instead of the vetted `addCustomLocation()` helper (built and unit-tested in Plan 08-02), bypassing the D-03 case-insensitive collision check everywhere else in the phase already enforces. Route both quick-add-location handlers through `addCustomLocation()` and distinguish the duplicate-name error case in each component's existing toast-based error handling, matching the pattern already used in `src/routes/locations/index.tsx`.

This is a narrow, single-concern gap-closure fix — not a re-plan of LOC-01 through LOC-04. Edge-case coverage for those requirements (idempotency, concurrency, ordering, empty-input, adjacency) was already addressed by the original phase-8 plan set (08-01 through 08-05) and its verification; this plan's `must_haves` are scoped exclusively to the quick-add-location collision-safety gap CR-01 identifies. The one other open item from 08-VERIFICATION.md (drag-active visual state at 360px) is a human-verification backstop already flagged as such in Plan 08-05's SUMMARY — it is not a code gap and is out of scope here.

Purpose: Every location-creation code path in the app must enforce the same collision-safety invariant, or a user can silently create duplicate location rows ("pantry" alongside "Pantry") that fragment stock filtering, aggregation, and the Locations management screen.
Output: `MedicineForm.tsx` and `StockFields.tsx` both create locations exclusively through `addCustomLocation()`, with duplicate-name errors surfaced distinctly from generic failures.
</objective>

<execution_context>
@C:/my-code/vibe-coding/med-stock/.claude/gsd-core/workflows/execute-plan.md
@C:/my-code/vibe-coding/med-stock/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-full-location-management/08-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Route MedicineForm quick-add-location through addCustomLocation</name>
  <reversibility rating="reversible">Swapping one function call for another already-vetted helper in the same module; trivially revertible via git, no data migration or one-way state change involved.</reversibility>
  <files>src/components/MedicineForm.tsx</files>
  <read_first>
    src/lib/locationOps.ts — addCustomLocation(name: string): Promise<number> contract: trims the name, throws Error('Location name already exists') on a case-insensitive collision with any existing location (D-03), and assigns order as max(existing order)+1 before inserting.
    src/routes/locations/index.tsx (handleAdd, the try/catch around addCustomLocation) — the reference error-branching pattern this task adapts: catch(err), check `err instanceof Error && err.message === 'Location name already exists'` to distinguish the duplicate case from a generic failure.
    src/components/MedicineForm.tsx (current handleAddLocation and the `locations` useLiveQuery a few lines below it) — the function being replaced and the sibling query that still needs the `db` import.
  </read_first>
  <action>
    Add a new import statement bringing in `addCustomLocation` from `@/lib/locationOps` (this import does not currently exist in this file). Keep the existing `import { db } from '@/lib/db'` — `db` is still used by the `locations` useLiveQuery dropdown query below.
    Replace the entire body of `handleAddLocation`: remove the current direct Dexie insert together with its hardcoded numeric order value and the stale deferral comment sitting above it, and replace that insert with a single `await addCustomLocation(trimmed)` call — drop the `isDefault`/`hidden`/`order` object literal entirely, since `addCustomLocation` already sets all three fields correctly. Keep the `form.setValue('location', trimmed)`, `setNewLocationInput('')`, and `setShowQuickAddLocation(false)` lines that already follow the insert, unchanged.
    In the `catch (err)` block, keep the existing `console.error('Failed to add location:', err)` line, then branch: when `err instanceof Error && err.message === 'Location name already exists'`, call `toast.error(t('locations.errorDuplicate'))`; otherwise keep the existing `toast.error(t('toasts.locationFailed'))` fallback. This mirrors the branching shape already used in `handleAdd` in `src/routes/locations/index.tsx`, adapted from `setError` to `toast.error` since this component surfaces errors via sonner toasts rather than local error state.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <fails_when>non-zero exit, or any line matching "error TS"</fails_when>
    <automated>grep -c "addCustomLocation" src/components/MedicineForm.tsx</automated>
    <fails_when>output is 0 (import or call site missing)</fails_when>
  </verify>
  <acceptance_criteria>
    - `handleAddLocation` calls `addCustomLocation(trimmed)` and no longer constructs its own object literal for a direct Dexie insert.
    - The `addCustomLocation` import resolves from `@/lib/locationOps`.
    - The catch block calls `toast.error(t('locations.errorDuplicate'))` specifically when the caught error's message equals `'Location name already exists'`, and falls back to `toast.error(t('toasts.locationFailed'))` for any other error.
    - The `db` import remains present and used (the `locations` useLiveQuery dropdown query is unchanged).
    - `npx tsc --noEmit` passes clean.
  </acceptance_criteria>
  <done>Quick-add-location in the Add/Edit Medicine form is collision-safe: adding a name that collides case-insensitively with an existing location throws and surfaces `locations.errorDuplicate`; a genuinely new name is created through `addCustomLocation` with the correct next-order value.</done>
</task>

<task type="auto">
  <name>Task 2: Route StockFields quick-add-location through addCustomLocation</name>
  <reversibility rating="reversible">Same class of change as Task 1 — a like-for-like function-call swap, trivially revertible.</reversibility>
  <files>src/components/StockFields.tsx</files>
  <read_first>
    src/lib/locationOps.ts — addCustomLocation(name: string): Promise<number> contract (same as Task 1).
    src/routes/locations/index.tsx (handleAdd, the try/catch around addCustomLocation) — the reference error-branching pattern this task adapts.
    src/components/StockFields.tsx (current handleAddLocation and the `locations` useLiveQuery above it) — the function being replaced and the sibling query that still needs the `db` import.
  </read_first>
  <action>
    Add a new import statement bringing in `addCustomLocation` from `@/lib/locationOps` (this import does not currently exist in this file). Keep the existing `import { db } from '@/lib/db'` — `db` is still used by the `locations` useLiveQuery dropdown query above.
    Replace the entire body of `handleAddLocation`: remove the current direct Dexie insert together with its hardcoded numeric order value and the stale deferral comment sitting above it, and replace that insert with a single `await addCustomLocation(trimmed)` call — drop the `isDefault`/`hidden`/`order` object literal entirely. Keep the `form.setValue('location', trimmed)`, `setNewLocationInput('')`, and `setShowQuickAddLocation(false)` lines that already follow the insert, unchanged.
    In the `catch (err)` block, keep the existing `console.error('Failed to add location:', err)` line, then branch: when `err instanceof Error && err.message === 'Location name already exists'`, call `toast.error(t('locations.errorDuplicate'))`; otherwise keep the existing `toast.error(t('toasts.locationFailed'))` fallback.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <fails_when>non-zero exit, or any line matching "error TS"</fails_when>
    <automated>grep -c "addCustomLocation" src/components/StockFields.tsx</automated>
    <fails_when>output is 0 (import or call site missing)</fails_when>
    <automated>npm run build 2>&1 | tail -5</automated>
    <fails_when>output does not contain a successful build summary, or contains "error"</fails_when>
  </verify>
  <acceptance_criteria>
    - `handleAddLocation` calls `addCustomLocation(trimmed)` and no longer constructs its own object literal for a direct Dexie insert.
    - The `addCustomLocation` import resolves from `@/lib/locationOps`.
    - The catch block calls `toast.error(t('locations.errorDuplicate'))` specifically when the caught error's message equals `'Location name already exists'`, and falls back to `toast.error(t('toasts.locationFailed'))` for any other error.
    - The `db` import remains present and used (the `locations` useLiveQuery dropdown query is unchanged).
    - `npx tsc --noEmit` and `npm run build` both pass clean.
  </acceptance_criteria>
  <done>Quick-add-location in the Add/Edit Stock fields is collision-safe: adding a name that collides case-insensitively with an existing location throws and surfaces `locations.errorDuplicate`; a genuinely new name is created through `addCustomLocation` with the correct next-order value. Both `MedicineForm.tsx` and `StockFields.tsx` now route every location-creation path through the same vetted helper the Locations screen already uses.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| None crossed | Client-side UI wiring against the already-vetted `addCustomLocation()` function (Plan 08-02); no new input source, no network/auth boundary — local-only IndexedDB PWA per CLAUDE.md. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-06-01 | Tampering | `MedicineForm.tsx` / `StockFields.tsx` `handleAddLocation` | low | mitigate | Route both quick-add-location handlers through `addCustomLocation()`'s case-insensitive collision check (D-03), closing the path that let a user silently create a duplicate `Location` row and fragment stock filtering/aggregation. No network/auth trust boundary is crossed — this is local-data integrity in a single-user, no-backend PWA, so severity is assessed as low rather than an ASVS-relevant access-control gap. |
</threat_model>

<verification>
`npx tsc --noEmit` and `npm run build` both pass after both tasks. `grep -c "addCustomLocation" src/components/MedicineForm.tsx` and the same command against `src/components/StockFields.tsx` both return a non-zero count.
</verification>

<success_criteria>
- Both quick-add-location handlers create locations exclusively through `addCustomLocation()` — no direct Dexie insert remains in either component's `handleAddLocation`.
- Attempting to quick-add a name that collides case-insensitively with an existing location surfaces `locations.errorDuplicate` via `toast.error` in both components.
- `npx tsc --noEmit` and `npm run build` both pass clean.
</success_criteria>

## Artifacts this phase produces (this plan)

This plan creates no new files, types, or exported symbols. It corrects two existing call sites (`src/components/MedicineForm.tsx`, `src/components/StockFields.tsx`) to import and call the already-existing `addCustomLocation` export from `src/lib/locationOps.ts` (built in Plan 08-02) instead of inserting directly into `db.locations`, and adds one new error-branch (`toast.error(t('locations.errorDuplicate'))`, using an i18n key that already exists in both `en.ts`/`pl.ts`) to each component's existing catch block.

<output>
Create `.planning/phases/08-full-location-management/08-06-SUMMARY.md` when done
</output>
