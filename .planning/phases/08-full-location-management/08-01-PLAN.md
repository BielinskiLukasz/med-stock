---
phase: 08-full-location-management
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/db.ts
  - src/lib/locationOps.ts
  - src/lib/db.test.ts
  - src/routes/locations/index.tsx
  - src/i18n/types.ts
  - src/i18n/en.ts
  - src/i18n/pl.ts
autonomous: true
requirements: [LOC-01]

estimate:
  tokens: 52000
  raw_tokens: 52000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Opening the app against an existing (pre-v6) database automatically upgrades every location row to carry hidden=false and a sequential order value, with zero data loss and no manual step."
    - "A brand-new install seeds all 7 predefined locations with hidden=false and order values 1 through 7, in the same alphabetical order they display today (Bathroom Cabinet=1 ... Travel Kit=7)."
    - "Renaming a predefined (isDefault=true) location succeeds — it is no longer blocked — and every medicine currently referencing its old name string is updated to the new name inside the same transaction."
    - "The Locations screen renders rows sorted by the new order field (db.locations.orderBy('order')), not alphabetically by name."
    - "Renaming a predefined location permanently falls it out of LOCATION_KEYS translation lookup — it displays as the plain stored string in both EN and PL from then on, identical to how custom locations already display (D-02)."
  artifacts:
    - "src/lib/db.ts — Location.hidden: boolean, Location.order: number, db.version(6)"
    - "src/lib/locationOps.ts — renameLocation() without the isDefault guard"
    - "src/routes/locations/index.tsx — orderBy('order'), Edit button rendered unconditionally"
    - "src/i18n/types.ts, en.ts, pl.ts — full locations.*/aria.* key set for the phase"
  key_links:
    - "db.version(6).upgrade(tx => ...) reads+writes db.locations within the same versionchange transaction, so every pre-existing row has hidden/order populated before any query in this app ever observes it"
    - "LocationsScreen useLiveQuery(() => db.locations.orderBy('order').toArray()) re-renders immediately once the migration commits, via Dexie's live-query reactivity"
  prohibitions:
    - "Never modify db.version(5)'s stores() string in place — schema changes only land via a new db.version(6) block, exactly like the v2-v5 precedent in this same file."
    - "Never assign Location.order values inside a second, separate db.transaction() call chained after .upgrade() completes — Dexie's Version builder does not expose a public .then() for post-upgrade work; read and write order inside the same upgrade transaction via the tx argument."
    - "Never leave a location row without both hidden and order after the v6 upgrade runs — every existing row must be visited by the upgrade's toCollection() pass, not just newly-added ones."
---

<objective>
Land the one-way `db.version(6)` schema migration (`Location.hidden`, `Location.order`) and prove it end-to-end with the thinnest real capability this phase unlocks: renaming a predefined location. This is the tracer slice — every later plan in Phase 8 (hide/show, delete-with-reassign, reorder, dropdown filtering) depends on these two new fields existing and being populated correctly on every location row, old and new.

Purpose: De-risk the irreversible part of the phase first, on the freshest context, before any business-logic hardening or UI work is built on top of it.
Output: `db.ts` at schema v6 with a working upgrade path, `renameLocation()` usable on predefined locations, `LocationsScreen` sorted by the new field, and the full i18n key set the rest of the phase will consume.
</objective>

<execution_context>
@C:/my-code/vibe-coding/med-stock/.claude/gsd-core/workflows/execute-plan.md
@C:/my-code/vibe-coding/med-stock/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-full-location-management/08-CONTEXT.md
@.planning/phases/08-full-location-management/08-UI-SPEC.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: db.version(6) migration + predefined-location rename, end-to-end</name>
  <reversibility rating="one-way">CONTEXT.md D-13 already locked this exact migration shape (Location.order + Location.hidden via a new db.version(6), never modifying v5) during /gsd-discuss-phase — the human already walked through this door, so no re-ask checkpoint is inserted here per the "already decided" rule. Recorded for traceability: undoing this after it ships requires a second migration (v7) and breaks BackupSchema round-trip for any JSON backup exported after this lands.</reversibility>
  <files>src/lib/db.ts, src/lib/locationOps.ts, src/routes/locations/index.tsx, src/lib/db.test.ts</files>
  <read_first>
    src/lib/db.ts (full file — Location interface at lines 48-52, the v1-v5 version() chain at lines 81-209, db.on('populate') seed at lines 212-222 — copy the exact index strings, do not paraphrase them)
    src/lib/locationOps.ts (full file, 30 lines)
    src/routes/locations/index.tsx (full file, 177 lines — the isDefault-guarded block at lines 138-169)
    src/lib/db.test.ts (full file — existing renameLocation/deleteLocation describe blocks)
    .planning/phases/08-full-location-management/08-CONTEXT.md (D-01, D-02, D-03, D-04, D-13, D-17, canonical_refs)
  </read_first>
  <behavior>
    - Behavior 1 (fresh install): after `db.on('populate')` seeds the 7 predefined locations, every seeded row has `hidden === false` and `order` equal to its 1-based position in the existing bulkAdd array order (Bathroom Cabinet=1 ... Travel Kit=7).
    - Behavior 2 (upgrade path): a database that already exists at v5 shape (locations rows with only `id`/`name`/`isDefault`, no `hidden`/`order`) — when opened by the app now bound through v6 — ends up with every existing location row carrying `hidden === false` and a unique, contiguous `order` value 1..N assigned in alphabetical-by-name order, with no rows lost or duplicated.
    - Behavior 3 (rename unlock): calling `renameLocation(locationId, newName)` on a location with `isDefault: true` no longer throws; the location's `name` updates and every `medicines` row whose `location` matched the old name now has the new name, inside one transaction.
  </behavior>
  <action>
    In db.ts: add `hidden: boolean` and `order: number` to the `Location` interface (replace the stale "D-18: predefined locations cannot be renamed/deleted" comment — that invariant no longer holds after this plan and Plan 08-02/08-04). Add `db.version(6).stores({ locations: '++id, name, isDefault' })` (index string unchanged — hidden/order are not indexed) followed by `.upgrade(tx => ...)`. Inside that single upgrade callback: read all location rows sorted by name via `tx.table('locations').toCollection().sortBy('name')`, then for each row at array index `i` call `tx.table('locations').update(row.id, { hidden: false, order: i + 1 })` — do this entirely inside the versionchange transaction the `tx` argument represents; do not open a second `db.transaction()` afterward. Update the `db.on('populate')` bulkAdd array so every one of the 7 predefined-location objects gains `hidden: false` and `order: 1` through `order: 7` matching their existing array position (Bathroom Cabinet through Travel Kit).
    In locationOps.ts: in `renameLocation`, delete the line `if (loc.isDefault) throw new Error('Cannot rename default location')`. Leave everything else in `renameLocation` and `addCustomLocation` unchanged — the case-insensitive collision check (D-03) and order assignment on add are deliberately deferred to Plan 08-02.
    In routes/locations/index.tsx: change the `useLiveQuery` call from `db.locations.orderBy('name').toArray()` to `db.locations.orderBy('order').toArray()`. Restructure the per-row conditional block: currently `{!loc.isDefault && (<div>...Edit button...AlertDialog delete...</div>)}` wraps both actions together — split it so the Edit `<Button>` (calls `startEdit(loc.id, loc.name)`) renders unconditionally for every row, while the `AlertDialog` delete block stays wrapped in `{!loc.isDefault && (...)}` for now (Plan 08-04 replaces that entire delete block with the reassign-or-clear flow and removes this guard permanently — this is a deliberate incremental step, not a scope cut).
  </action>
  <verify>
    <automated>npx vitest run src/lib/db.test.ts</automated>
    <fails_when>non-zero exit, or the summary line does not contain "passed" for every test in the file</fails_when>
    <automated>npx tsc --noEmit</automated>
    <fails_when>non-zero exit, or any line matching "error TS"</fails_when>
  </verify>
  <acceptance_criteria>
    - `Location` interface in db.ts declares both `hidden: boolean` and `order: number`.
    - `db.version(6)` exists in db.ts and is the last version in the chain; `db.version(5)`'s `.stores()` call is byte-for-byte unchanged from before this task.
    - `renameLocation` in locationOps.ts contains no `isDefault` guard (grep for `Cannot rename default location` returns zero matches in locationOps.ts).
    - `routes/locations/index.tsx` calls `db.locations.orderBy('order')`, not `.orderBy('name')`.
    - db.test.ts contains a passing test asserting rename succeeds on an `isDefault: true` location and updates referencing medicines, and a passing migration test asserting a pre-v6-shaped database gets `hidden`/`order` populated correctly on open.
  </acceptance_criteria>
  <done>Fresh installs seed hidden/order correctly, existing databases upgrade in place without data loss, predefined locations can be renamed end-to-end, and the Locations screen sorts by the new order field. All in `npx vitest run src/lib/db.test.ts` and `npx tsc --noEmit`.</done>
</task>

<task type="auto">
  <name>Task 2: Extend i18n dictionary with the full Phase 8 key set</name>
  <files>src/i18n/types.ts, src/i18n/en.ts, src/i18n/pl.ts</files>
  <read_first>
    src/i18n/types.ts (full file — the `locations` block at lines 113-123, the `aria` block at lines 286-296)
    src/i18n/en.ts (locations block at lines 111-121, aria block near end of file)
    src/i18n/pl.ts (locations block at lines 111-121, aria block near end of file)
    .planning/phases/08-full-location-management/08-UI-SPEC.md ("New translation keys required (EN + PL)" table — the single source of truth for every key/value pair added here)
  </read_first>
  <action>
    Add these 12 keys to the `locations` object in TranslationDict (types.ts) as `string`, then the matching EN/PL values from the UI-SPEC table into en.ts/pl.ts's `locations` blocks: `hidden`, `hide`, `show`, `emptyHeading`, `emptyBody`, `errorDuplicate`, `deleteReassignTitle`, `deleteReassignBody`, `reassignLabel`, `reassignOther`, `confirmReassignDelete`, `reorderHint`. Add these 5 keys to the `aria` object (types.ts) and en.ts/pl.ts: `hideLocation`, `showLocation`, `moveUp`, `moveDown`, `dragHandle`. Use the exact EN and PL copy strings from the UI-SPEC table verbatim — do not paraphrase (this is the same "extend the dict atomically before touching route files" pattern used throughout Phase 7, recorded in STATE.md's Decisions log). None of these 17 keys are consumed yet in this plan — later Phase 8 plans (08-03, 08-04, 08-05) wire them into components.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <fails_when>non-zero exit, or any line matching "error TS" (a mismatch between types.ts's new keys and en.ts/pl.ts's values surfaces immediately as a structural-typing error here)</fails_when>
  </verify>
  <acceptance_criteria>
    - `TranslationDict['locations']` in types.ts has exactly 12 new string fields beyond the 9 that existed before this task.
    - `TranslationDict['aria']` in types.ts has exactly 5 new string fields beyond the 9 that existed before this task.
    - en.ts and pl.ts both satisfy the extended `TranslationDict` shape with no missing keys (proven by `npx tsc --noEmit` passing, since `en` and `pl` are typed as `TranslationDict`).
  </acceptance_criteria>
  <done>All 17 new keys exist in types.ts, en.ts, and pl.ts with the UI-SPEC's exact copy; `npx tsc --noEmit` passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| None crossed | This plan is entirely client-side IndexedDB schema/data work with no network or auth boundary — no untrusted external input reaches the upgrade callback or renameLocation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-01-01 | Tampering | db.ts db.version(6).upgrade() callback | medium | mitigate | The callback only reads/writes the local `locations` table it already owns and derives `hidden`/`order` purely from data already in that table (no external input) — matches the additive-only, self-contained pattern already proven in v2-v5. |
| T-08-01-02 | Repudiation | renameLocation() on isDefault rows | low | accept | Location renames were never audit-logged (unlike medicine history) even before this phase — this pre-existing gap is not introduced or worsened by removing the isDefault guard. |
| T-08-01-03 | Denial of Service | db.version(6) upgrade scanning locations table | low | accept | The upgrade only iterates `db.locations` (bounded to a handful of rows in practice), never `db.medicines` — upgrade cost is O(location count), independent of stock table size. |
</threat_model>

<verification>
`npx vitest run src/lib/db.test.ts` passes with the new/updated tests described above. `npx tsc --noEmit` passes with zero errors after both tasks. `npm run build` succeeds (sanity check that the full app still compiles and bundles with the new schema and dict shape).
</verification>

<success_criteria>
- A fresh app install has 7 predefined locations with hidden=false and order 1-7.
- An existing (pre-v6) database upgrades in place with no data loss, gaining hidden=false and sequential order on every location row.
- A predefined location can be renamed via `renameLocation()`, and the Locations screen displays rows in `order` sequence.
- The full 17-key i18n vocabulary for Phase 8 exists in types.ts/en.ts/pl.ts, ready for Plans 08-03/08-04/08-05 to consume.
</success_criteria>

## Artifacts this phase produces (this plan)

- `Location.hidden: boolean` (new field, db.ts)
- `Location.order: number` (new field, db.ts)
- `db.version(6)` schema + upgrade migration (db.ts)
- `TranslationDict['locations'].hidden/hide/show/emptyHeading/emptyBody/errorDuplicate/deleteReassignTitle/deleteReassignBody/reassignLabel/reassignOther/confirmReassignDelete/reorderHint` (types.ts, en.ts, pl.ts)
- `TranslationDict['aria'].hideLocation/showLocation/moveUp/moveDown/dragHandle` (types.ts, en.ts, pl.ts)

<output>
Create `.planning/phases/08-full-location-management/08-01-SUMMARY.md` when done
</output>

