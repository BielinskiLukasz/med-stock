---
phase: 07-i18n-polish-language
plan: 07
type: execute
wave: 1
depends_on: []
gap_closure: true
gap_ids: [G-07-4, G-07-17, G-07-17b, G-07-20]
files_modified:
  - src/lib/historyOps.ts
  - src/lib/historyOps.test.ts
  - src/components/CatalogAutocomplete.tsx
  - src/components/FilterBottomSheet.tsx
  - src/components/ImportJSONSection.tsx
  - src/i18n/types.ts
  - src/i18n/en.ts
  - src/i18n/pl.ts
autonomous: true
requirements: [I18N-02]

estimate:
  tokens: 45000
  raw_tokens: 22000
  tasks: 3
  confidence: high

must_haves:
  truths:
    - "FilterBottomSheet location option buttons render translated location names; predefined names use LOCATION_KEYS lookup; user-created names pass through unchanged"
    - "FilterBottomSheet empty state 'No locations added yet.' renders via t('filter.noLocations')"
    - "CatalogAutocomplete category spans render t(CATEGORY_KEYS[cat.category] ?? cat.category) instead of the raw canonical English DB string"
    - "permanentDeleteMedicine cascades to delete the medicine_catalog row when no other medicines reference that catalogId"
    - "permanentDeleteMedicine preserves the catalog row when at least one other medicine still references the same catalogId"
    - "ImportJSONSection idle description paragraph renders via t('data.importJSONDescription')"
    - "ImportJSONSection AlertDialog confirm body renders via split translation keys with dynamic counts interpolated between key segments"
  artifacts:
    - src/lib/historyOps.ts
    - src/lib/historyOps.test.ts
    - src/components/FilterBottomSheet.tsx
    - src/components/CatalogAutocomplete.tsx
    - src/components/ImportJSONSection.tsx
    - src/i18n/types.ts
    - src/i18n/en.ts
    - src/i18n/pl.ts
  key_links:
    - "LOCATION_KEYS imported from @/i18n in FilterBottomSheet.tsx and used as t(LOCATION_KEYS[name] ?? name)"
    - "CATEGORY_KEYS imported from @/i18n in CatalogAutocomplete.tsx and used as t(CATEGORY_KEYS[cat.category] ?? cat.category)"
    - "db.medicine_catalog added to transaction scope in permanentDeleteMedicine"
    - "TranslationDict compiles with new filter.noLocations and data.importJSONDescription / importConfirmBody* keys"
---

<objective>
Close four diagnosed UAT gaps from Phase 07 — the final i18n / data-consistency fixes before the phase is verified complete.

Purpose: Gaps G-07-4, G-07-17, and G-07-20 leave English strings visible when Polish is active. Gap G-07-17b leaves stale catalog suggestions in the autocomplete after medicines are permanently deleted.

Output:
- FilterBottomSheet renders location names through LOCATION_KEYS (G-07-4)
- CatalogAutocomplete renders category labels through CATEGORY_KEYS (G-07-17)
- permanentDeleteMedicine cascades to medicine_catalog when the last stock entry is removed (G-07-17b)
- ImportJSONSection idle description and dialog confirm body use t() calls with new translation keys (G-07-20)
</objective>

<execution_context>
@C:/my-code/vibe-coding/med-stock/.claude/gsd-core/workflows/execute-plan.md
@C:/my-code/vibe-coding/med-stock/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@C:/my-code/vibe-coding/med-stock/.planning/PROJECT.md
@C:/my-code/vibe-coding/med-stock/.planning/ROADMAP.md
@C:/my-code/vibe-coding/med-stock/.planning/STATE.md
@C:/my-code/vibe-coding/med-stock/.planning/phases/07-i18n-polish-language/07-UAT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Cascade-delete catalog entry in permanentDeleteMedicine (G-07-17b)</name>
  <files>src/lib/historyOps.ts, src/lib/historyOps.test.ts</files>
  <behavior>
    - Test A: when the last medicine referencing a catalogId is permanently deleted, the medicine_catalog row for that catalogId is also deleted — verified by db.medicine_catalog.get(catalogId) returning undefined
    - Test B: when other medicines still reference the same catalogId, the catalog row is preserved — verified by db.medicine_catalog.get(catalogId) returning the original entry
    - Existing tests must still pass: history entry is written, medicine record is removed, history is preserved after permanent delete
  </behavior>
  <action>
    RED phase — add two tests to the existing `describe('permanentDeleteMedicine', ...)` block in historyOps.test.ts:

    Test A "cascades to catalog when last medicine is deleted": add a catalog entry via db.medicine_catalog.add with name, category, form: null, notes: null, createdAt and updatedAt as ISO strings; add a medicine with that returned catalogId; call permanentDeleteMedicine; assert db.medicine_catalog.get(catalogId) is undefined.

    Test B "preserves catalog when other medicines still reference it": add a catalog entry; add two medicines both using that catalogId; permanently delete only the first medicine; assert db.medicine_catalog.get(catalogId) is defined.

    Run `npx vitest run src/lib/historyOps.test.ts` — both new tests must FAIL (permanentDeleteMedicine does not yet cascade).

    GREEN phase — update permanentDeleteMedicine in historyOps.ts:
    - Add db.medicine_catalog to the transaction table list: change `db.transaction('rw', db.medicines, db.history, async () => {` to `db.transaction('rw', db.medicines, db.history, db.medicine_catalog, async () => {`
    - After `await db.medicines.delete(medicine.id)`, count remaining entries: `const remaining = await db.medicines.where('catalogId').equals(medicine.catalogId).count()`
    - If remaining === 0: `await db.medicine_catalog.delete(medicine.catalogId)`

    Run `npx vitest run src/lib/historyOps.test.ts` — all tests must pass, including the existing three permanentDeleteMedicine tests.

    Note: the existing tests use baseMedicine with catalogId: 1 and never create a catalog entry. After the cascade change, the code will count medicines with catalogId=1 (finds 0), then call db.medicine_catalog.delete(1) where id=1 does not exist — Dexie's delete on a missing key is a silent no-op, so those tests are unaffected.
  </action>
  <verify>
    <automated>npx vitest run src/lib/historyOps.test.ts</automated>
  </verify>
  <done>All permanentDeleteMedicine tests pass including the two new cascade tests. npm run build succeeds (TypeScript strict mode satisfied — medicine.catalogId is number, db.medicine_catalog.delete accepts number).</done>
</task>

<task type="auto">
  <name>Task 2: Translate location names in FilterBottomSheet and category labels in CatalogAutocomplete (G-07-4, G-07-17)</name>
  <files>src/components/CatalogAutocomplete.tsx, src/components/FilterBottomSheet.tsx, src/i18n/types.ts, src/i18n/en.ts, src/i18n/pl.ts</files>
  <action>
    i18n files — add filter.noLocations key:

    In src/i18n/types.ts, inside the `filter` object type (after the existing `location: string` line), add:
      `noLocations: string`

    In src/i18n/en.ts, inside the `filter` object (after `location: 'Location'`), add:
      `noLocations: 'No locations added yet.'`

    In src/i18n/pl.ts, inside the `filter` object (after `location: 'Lokalizacja'`), add:
      `noLocations: 'Brak dodanych lokalizacji.'`

    CatalogAutocomplete.tsx — add CATEGORY_KEYS (G-07-17):

    The current import on line 6 is `import { useLang } from '@/i18n'`. Change it to:
      `import { useLang, CATEGORY_KEYS } from '@/i18n'`

    On line 69, the span renders `{cat.category}`. The JSX is wrapped in `{cat.category && (...)}` so TypeScript narrows cat.category to string inside. Replace `{cat.category}` with:
      `{t(CATEGORY_KEYS[cat.category] ?? cat.category)}`

    FilterBottomSheet.tsx — add LOCATION_KEYS and translate location names (G-07-4):

    Line 13 imports `import { useLang, CATEGORY_KEYS } from '@/i18n'`. Add LOCATION_KEYS:
      `import { useLang, CATEGORY_KEYS, LOCATION_KEYS } from '@/i18n'`

    Line 138 renders `{location.name}` inside the location button. Replace with:
      `{t(LOCATION_KEYS[location.name] ?? location.name)}`

    This preserves user-created location names (those absent from LOCATION_KEYS fall back to location.name verbatim, per D-06/D-07).

    Line 143 renders hardcoded `'No locations added yet.'`. Replace the paragraph with:
      `<p className="text-xs text-gray-500">{t('filter.noLocations')}</p>`
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>TypeScript reports zero errors. With Polish active, CatalogAutocomplete category spans show translated category names (e.g. "Inne" for "Other"). FilterBottomSheet location buttons show translated predefined location names (e.g. "Szafka w łazience" for "Bathroom Cabinet"). User-created location names are unchanged. The empty state renders the Polish string "Brak dodanych lokalizacji."</done>
</task>

<task type="auto">
  <name>Task 3: Add ImportJSONSection translation keys and replace hardcoded English strings (G-07-20)</name>
  <files>src/components/ImportJSONSection.tsx, src/i18n/types.ts, src/i18n/en.ts, src/i18n/pl.ts</files>
  <action>
    i18n files — add four new keys to the data section:

    In src/i18n/types.ts, inside the `data` object type (after the existing `importCSVDescription: string` line), add:
      `importJSONDescription: string`
      `importConfirmBodyPre: string`
      `importConfirmBodyMid: string`
      `importConfirmBodyPost: string`

    In src/i18n/en.ts, inside the `data` object (after `importCSVDescription: '...'`), add:
      `importJSONDescription: 'Restore your inventory from a backup file. This will replace all medicines, locations, and history with the contents of the backup.'`
      `importConfirmBodyPre: 'This will replace all '`
      `importConfirmBodyMid: ' medicines, '`
      `importConfirmBodyPost: ' locations, and full change history. This cannot be undone. Import anyway?'`

    In src/i18n/pl.ts, inside the `data` object (after `importCSVDescription: '...'`), add:
      `importJSONDescription: 'Przywróć stan magazynu z pliku kopii zapasowej. Zastąpi to wszystkie leki, lokalizacje oraz historię zmian zawartością pliku.'`
      `importConfirmBodyPre: 'Spowoduje to zastąpienie wszystkich '`
      `importConfirmBodyMid: ' leków, '`
      `importConfirmBodyPost: ' lokalizacji oraz całej historii zmian. Tej operacji nie można cofnąć. Importować?'`

    ImportJSONSection.tsx — replace hardcoded strings:

    Lines 87-90: the idle description paragraph currently hardcodes the English description string. Replace the entire `<p>` element with:
      `<p className="text-sm text-muted-foreground">{t('data.importJSONDescription')}</p>`

    Lines 113-119: the AlertDialogDescription currently concatenates: `'This will replace all ' + medicineCount + ' medicines, ' + locationCount + ' locations, and full change history. This cannot be undone. Import anyway?'`

    Replace the entire AlertDialogDescription content with the split-key pattern (t() has no interpolation support — three keys bracket the two dynamic counts):
      `{t('data.importConfirmBodyPre')}{medicineCount}{t('data.importConfirmBodyMid')}{locationCount}{t('data.importConfirmBodyPost')}`
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Build succeeds with zero TypeScript errors — TranslationDict structural type validates that en.ts and pl.ts both define the four new data keys. With Polish active, the ImportJSONSection idle description renders in Polish. The confirm dialog body shows the Polish sentence with the live medicine and location counts interpolated at the correct positions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DB transaction → catalog table | permanentDeleteMedicine now writes to a second table; incorrect scope could leave orphaned catalog rows or silently delete active ones |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-07-01 | Tampering | permanentDeleteMedicine in historyOps.ts | high | mitigate | Count remaining medicines for catalogId INSIDE the same transaction before deleting catalog row; atomicity prevents a TOCTOU race between count and delete |
| T-07-07-02 | Tampering | LOCATION_KEYS lookup in FilterBottomSheet | low | accept | LOCATION_KEYS[name] ?? name fallback means user-created location names always render as-is; no user data is lost or transformed |
</threat_model>

<verification>
Sequential verification across all three tasks:

1. `npx vitest run src/lib/historyOps.test.ts` — all permanentDeleteMedicine tests pass (cascade + preserve + existing three)
2. `npx tsc --noEmit` — zero TypeScript errors after i18n type additions and component import changes
3. `npm run build` — full tsc + vite build succeeds, confirming TranslationDict structural type validates both en.ts and pl.ts with all four new data keys

Manual smoke test (after build):
- Switch to Polish. Open Add medicine form. Confirm category labels in autocomplete suggestions are in Polish.
- Open Filter sheet. Confirm location buttons use Polish names. If no locations exist, confirm Polish empty state text.
- Navigate to Data screen. Confirm the JSON import description paragraph is in Polish. Trigger an import — confirm the confirm dialog body is in Polish with correct numeric counts.
- Delete all medicines for one catalog entry from Trash screen. Open Add medicine form. Confirm that deleted medicine no longer appears in autocomplete suggestions.
</verification>

<success_criteria>
- G-07-4 closed: FilterBottomSheet location buttons render Polish names for predefined locations; empty state uses t('filter.noLocations')
- G-07-17 closed: CatalogAutocomplete category span renders t(CATEGORY_KEYS[cat.category] ?? cat.category) — Polish category names visible in suggestions
- G-07-17b closed: permanentDeleteMedicine removes the catalog row when it is the last medicine for that catalogId; two new tests cover both the cascade and the preserve branches
- G-07-20 closed: ImportJSONSection idle description and dialog confirm body are fully translated via four new data.* keys; no hardcoded English strings remain in those two locations
- `npm run build` exits 0
- `npx vitest run src/lib/historyOps.test.ts` exits 0
</success_criteria>

<output>
Create `.planning/phases/07-i18n-polish-language/07-07-SUMMARY.md` when done
</output>
