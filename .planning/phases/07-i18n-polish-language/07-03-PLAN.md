---
phase: 07-i18n-polish-language
plan: 03
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - src/components/CatalogFields.tsx
  - src/components/MedicineForm.tsx
  - src/components/StockFields.tsx
  - src/components/StockEditSheet.tsx
  - src/components/MoveStockSheet.tsx
  - src/components/CatalogEditSheet.tsx
  - src/components/ChangeHistory.tsx
  - src/components/HistoryEntry.tsx
autonomous: true
requirements:
  - I18N-02
  - I18N-04

estimate:
  tokens: 75000
  raw_tokens: 38000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Form dropdowns for category show Polish category names when Polish is active (I18N-02, I18N-04, D-05)"
    - "Form dropdowns for medicine form type (Tablet, Capsule, etc.) show Polish names when Polish is active (I18N-02, D-05)"
    - "Quantity unit dropdowns and PAO unit dropdowns render translated labels in active language (I18N-02, D-08)"
    - "Form labels, placeholders, and button labels in CatalogFields, MedicineForm, StockFields render in active language (I18N-02)"
    - "Sheet titles (Edit Stock, Move Stock, Edit Catalog Entry) render in active language (I18N-02)"
    - "Toast messages from StockEditSheet, MoveStockSheet, CatalogEditSheet render in active language (I18N-02)"
    - "ChangeHistory section heading and 'No history yet' text render in active language (I18N-02)"
    - "HistoryEntry action strings (Medicine added, Moved to Trash Bin, Restored from Trash Bin) render in active language (I18N-02)"
    - "Stored DB category values and form type values are never modified — only display rendering changes (D-05, D-07)"
  artifacts:
    - "src/components/CatalogFields.tsx — category dropdown and form-type dropdown labels translated"
    - "src/components/MedicineForm.tsx — all form labels, location names, unit labels, button text translated"
    - "src/components/StockFields.tsx — quantity unit dropdown and PAO unit dropdown translated"
    - "src/components/StockEditSheet.tsx — sheet title and toast messages translated"
    - "src/components/MoveStockSheet.tsx — sheet title, labels, and toast messages translated"
    - "src/components/CatalogEditSheet.tsx — sheet title and toast messages translated"
    - "src/components/ChangeHistory.tsx — section heading and no-history text translated"
    - "src/components/HistoryEntry.tsx — action strings translated"
  key_links:
    - "MedicineForm imports FORM_TYPE_KEYS from '@/i18n' to translate MedicineFormEnum values"
    - "StockFields imports UNIT_KEYS from '@/i18n' for quantity unit labels and uses t('units.*') for PAO units"
    - "HistoryEntry receives lang from useLang() to format the timestamp locale-appropriately"
---

<objective>
Translate all form-entry components and stock management sheets. Every visible string in dropdown options, form labels, placeholders, button labels, sheet titles, and toast messages is replaced with t() calls. History view strings are also translated.

Purpose: Forms are the primary data-entry interaction surface. Translating them in a separate wave-2 plan (parallel to Plan 02) keeps the plan focused and avoids file-sharing conflicts.
Output: CatalogFields, MedicineForm, StockFields, StockEditSheet, MoveStockSheet, CatalogEditSheet, ChangeHistory, HistoryEntry — all strings translated.
</objective>

<execution_context>
@C:/my-code/vibe-coding/med-stock/.claude/gsd-core/workflows/execute-plan.md
@C:/my-code/vibe-coding/med-stock/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-i18n-polish-language/07-CONTEXT.md
@.planning/phases/07-i18n-polish-language/07-PATTERNS.md
@.planning/phases/07-i18n-polish-language/07-UI-SPEC.md
@.planning/phases/07-i18n-polish-language/07-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Translate form field components — CatalogFields, MedicineForm, StockFields (D-05, D-08)</name>

  <read_first>
    - src/components/CatalogFields.tsx (full file) — categories dropdown (CATEGORIES.map), form-type dropdown (MedicineFormEnum values), form labels 'Name *', 'Category', 'Form', 'Notes', placeholders, 'No category', 'No type'
    - src/components/MedicineForm.tsx (lines 1-220) — form labels, location section (predefined names, 'No location (Other)', 'Add new location...', placeholder), categories dropdown, toast.error for location add failure
    - src/components/MedicineForm.tsx (lines 220-420) — quantity, unit, expiry date, opened date, PAO, pack count labels and buttons
    - src/components/StockFields.tsx (full file) — quantity unit dropdown (QUANTITY_UNITS.map), PAO unit options ('days', 'weeks', 'months'), all field labels
    - src/i18n/index.ts — CATEGORY_KEYS, FORM_TYPE_KEYS, UNIT_KEYS, useLang exports from Plan 01
    - src/lib/db.ts (lines 6-20) — MedicineForm const values to confirm FORM_TYPE_KEYS coverage
    - .planning/phases/07-i18n-polish-language/07-CONTEXT.md §D-05, D-06, D-07, D-08 — what translates, what stays as stored value
  </read_first>

  <action>
In src/components/CatalogFields.tsx:
  - Add import of useLang, CATEGORY_KEYS, FORM_TYPE_KEYS from '@/i18n'.
  - Call const { t } = useLang() inside CatalogFields.
  - Form label 'Name *' becomes t('form.name') with the asterisk preserved inline.
  - Input placeholder 'e.g. Ibuprofen 400mg' — keep unchanged (it is an example hint, not a translatable UI label; the executor may choose to translate it if a form.namePlaceholder key is defined).
  - FormLabel 'Category' becomes t('form.category'); the 'No category' SelectItem label becomes t('form.noCategory').
  - In the CATEGORIES.map, render t(CATEGORY_KEYS[cat] ?? 'categories.other') as the displayed label; the SelectItem value attribute remains the canonical English category string (D-05 — stored value unchanged).
  - FormLabel 'Form' becomes t('form.formType'); the 'No type' option becomes t('form.noType').
  - In the MedicineFormEnum options map (Object.values(MedicineFormEnum)), render t(FORM_TYPE_KEYS[val] ?? 'formTypes.other') as the display label; the SelectItem value attribute remains the canonical MedicineForm value (D-05).
  - FormLabel 'Notes' becomes t('form.notes'); textarea placeholder becomes t('form.notesPlaceholder') if defined.

In src/components/MedicineForm.tsx:
  - Add import of useLang, CATEGORY_KEYS, FORM_TYPE_KEYS, LOCATION_KEYS from '@/i18n'.
  - Call const { t } = useLang() inside the MedicineForm component.
  - Apply the same category and form-type translation pattern from CatalogFields (above) to MedicineForm's own category and form type dropdowns.
  - Location section: the 'No location (Other)' SelectItem label becomes t('form.noLocation'); 'Add new location...' option label becomes t('form.addNewLocation'); the new-location input placeholder becomes t('form.newLocationPlaceholder'); 'Add' button label for submitting the new location becomes t('form.addLocation').
  - For predefined location SelectItems fetched from the DB: display t(LOCATION_KEYS[loc.name] ?? loc.name) — if LOCATION_KEYS has a key for the name, translate; otherwise show as-is (D-06, D-07).
  - Quantity label 'Quantity' becomes t('form.quantity'); quantity unit label becomes t('form.quantityUnit').
  - Expiry date label becomes t('form.expiryDate'); opened date label becomes t('form.openedDate').
  - PAO label becomes t('form.pao'); PAO unit label becomes t('form.paoUnit').
  - PAO unit options ('days', 'weeks', 'months') SelectItem labels become t('units.days'), t('units.weeks'), t('units.months'); values stay as canonical strings (D-08).
  - Pack count label becomes t('form.packCount'); pack count placeholder becomes t('form.packCountPlaceholder') if defined.
  - Submit/save button label: 'Save' → t('form.save'), 'Save changes' → t('form.saveChanges'), 'Cancel' → t('form.cancel').
  - Toast error 'Failed to add location. Please try again.' → toast.error(t('toasts.locationFailed')).
  - FormLabel 'Notes', notes textarea placeholder — translate as above.

In src/components/StockFields.tsx:
  - Add import of useLang, UNIT_KEYS from '@/i18n'.
  - Call const { t } = useLang() inside StockFields.
  - All field labels (Quantity, Unit, Expiry Date, Opened Date, PAO, Pack Count, Location, Notes) → translate via t('form.*') keys.
  - In QUANTITY_UNITS.map, display label becomes t(UNIT_KEYS[unit] ?? 'units.units'); value attribute stays as the canonical unit string (D-08).
  - PAO unit options ('days', 'weeks', 'months') → t('units.days'), t('units.weeks'), t('units.months'); values stay as canonical strings.
  - Toast error 'Failed to add location. Please try again.' → t('toasts.locationFailed').
  - All button labels within StockFields (Add, Cancel, Save) → translate via t('form.*').
  </action>

  <verify>
    <automated>npm run build 2>&1 | tail -5</automated>
    <automated>npx vitest run 2>&1 | tail -5</automated>
  </verify>

  <acceptance_criteria>
    - `npm run build` exits 0 with zero TypeScript errors
    - `npx vitest run` exits 0 with all prior tests passing (MedicineForm.test.ts and StockFields.test.ts must still pass)
    - Category and form-type SelectItem values (the value prop) remain English canonical strings — only displayed labels change
    - Quantity unit and PAO unit SelectItem values remain English canonical strings
    - No location name stored value is altered
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Translate sheets + history — StockEditSheet, MoveStockSheet, CatalogEditSheet, ChangeHistory, HistoryEntry (I18N-02)</name>

  <read_first>
    - src/components/StockEditSheet.tsx (full file) — SheetTitle 'Edit Stock Entry', toast.error 'Failed to save. Please try again.', button labels
    - src/components/MoveStockSheet.tsx (full file) — SheetTitle 'Move Stock', label 'Target location', toast.error 'Failed to move stock...', button labels
    - src/components/CatalogEditSheet.tsx (full file) — SheetTitle 'Edit Catalog Entry', toast.error 'Failed to save...', button labels
    - src/components/ChangeHistory.tsx (full file) — section heading 'Change History', 'No history yet.' text, expand/collapse logic
    - src/components/HistoryEntry.tsx (full file) — formatEntry() function with action strings 'Medicine added', 'Moved to Trash Bin', 'Restored from Trash Bin', 'fields updated', 'changed from ... to ...' — all need translation
    - src/lib/utils.ts — formatDate from Plan 01 for timestamp formatting in HistoryEntry
    - src/i18n/index.ts — useLang, t() and history.* / toasts.* / form.* keys
    - .planning/phases/07-i18n-polish-language/07-CONTEXT.md §D-11, D-13 — date formatting scope
  </read_first>

  <action>
In src/components/StockEditSheet.tsx:
  - Add import of useLang from '@/i18n'.
  - Call const { t } = useLang() inside StockEditSheet.
  - SheetTitle content 'Edit Stock Entry' → t('form.editStock').
  - Button labels (Save, Cancel) → t('form.save'), t('form.cancel').
  - toast.error('Failed to save. Please try again.') → toast.error(t('toasts.saveFailed')).

In src/components/MoveStockSheet.tsx:
  - Add import of useLang from '@/i18n'.
  - Call const { t } = useLang() inside MoveStockSheet.
  - SheetTitle content 'Move Stock' → t('form.moveStock').
  - Label 'Target location' → t('form.targetLocation').
  - Button labels → t('form.save'), t('form.cancel').
  - toast.error('Failed to move stock. Please try again.') → toast.error(t('toasts.moveFailed')).

In src/components/CatalogEditSheet.tsx:
  - Add import of useLang from '@/i18n'.
  - Call const { t } = useLang() inside CatalogEditSheet.
  - SheetTitle content → t('form.editMedicine') or equivalent catalog-edit key.
  - Button labels → t('form.save'), t('form.cancel').
  - toast.error('Failed to save. Please try again.') → toast.error(t('toasts.saveFailed')).

In src/components/ChangeHistory.tsx:
  - Add import of useLang from '@/i18n'.
  - Call const { t } = useLang() inside ChangeHistory.
  - The section heading text 'Change History' → t('history.title').
  - The empty-history message 'No history yet.' → t('history.noHistory').

In src/components/HistoryEntry.tsx:
  - Refactor formatEntry() to accept both the entry and a translation function t (typed as (key: string) => string) plus lang (typed as Lang imported from '@/i18n').
  - Add import of useLang and Lang from '@/i18n'; add import of formatDate from '@/lib/utils'.
  - Call const { t, lang } = useLang() inside HistoryEntry; pass t and lang to a translated formatEntry call.
  - In the formatEntry body:
    Format the timestamp using new Date(entry.timestamp).toLocaleDateString with locale 'pl-PL' when lang is 'pl' and 'en-GB' when lang is 'en' (this is the timestamp — not a stored YYYY-MM-DD field, so Intl is appropriate here for the time portion).
    'Medicine added' → t('history.added').
    'Moved to Trash Bin' → t('history.deleted').
    'Restored from Trash Bin' → t('history.restored').
    For 'updated' with one changed field: replace '${field} changed from "${oldValue}" to "${newValue}"' template with a string that uses t('history.fieldChanged') as a prefix and interpolates field, oldValue, newValue — the surrounding verbiage ('changed from ... to ...') uses t() keys for the connecting words.
    For 'updated' with multiple fields: replace '${n} fields updated' with interpolated string using t('history.fieldsUpdated') as the format key.
  </action>

  <verify>
    <automated>npm run build 2>&1 | tail -5</automated>
    <automated>npx vitest run 2>&1 | tail -5</automated>
  </verify>

  <acceptance_criteria>
    - `npm run build` exits 0
    - `npx vitest run` exits 0 — all existing tests pass including any HistoryEntry or sheet tests
    - StockEditSheet SheetTitle, MoveStockSheet SheetTitle, CatalogEditSheet SheetTitle render in active language
    - ChangeHistory section heading and no-history text render in active language
    - HistoryEntry action labels (added, deleted, restored, updated) render in active language
    - Toast error messages from all three sheets render in active language
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Form input → DB | Form values (category, unit) pass through as canonical English strings; display layer is separate |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-06 | Tampering | Form SelectItem values | low | accept | SelectItem value props remain canonical English strings; only label text is translated. Zod validation in form schema is unaffected. |
| T-07-07 | Tampering | HistoryEntry formatEntry | low | accept | History entry content comes from IndexedDB immutable records; translation only affects rendering |
</threat_model>

<verification>
1. `npm run build` passes zero errors
2. `npx vitest run` all tests pass (including MedicineForm.test.ts and StockFields.test.ts)
3. Manual: open Add Medicine form in Polish mode — category dropdown, form-type dropdown, unit dropdowns show Polish labels; the stored value submitted to DB is still the English canonical key
4. Manual: open Edit Stock sheet in Polish mode — sheet title shows Polish, Save/Cancel buttons show Polish
5. Manual: expand Change History section in Polish mode — section heading and action strings show Polish
</verification>

<success_criteria>
- Form dropdowns render translated labels; stored category/unit/form-type values unchanged (D-05, D-08)
- All sheet titles and toast messages translated (I18N-02)
- ChangeHistory and HistoryEntry strings translated (I18N-02)
- Zero TypeScript errors; all existing tests pass
</success_criteria>

<output>
Create `.planning/phases/07-i18n-polish-language/07-03-SUMMARY.md` when done
</output>

## Artifacts This Phase Produces

No new exported symbols — all changes are internal call-site translations in existing components.
