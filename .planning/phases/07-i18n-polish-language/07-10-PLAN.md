---
phase: 07-i18n-polish-language
plan: 10
type: execute
wave: 1
depends_on: []
files_modified:
  - src/i18n/types.ts
  - src/i18n/en.ts
  - src/i18n/pl.ts
  - src/routes/medicines/index.tsx
  - src/components/CatalogFields.tsx
  - src/components/MedicineForm.tsx
autonomous: true
gap_closure: true
requirements:
  - I18N-02

estimate:
  tokens: 14000
  raw_tokens: 14000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The Medicines list screen's filter-sheet trigger button (medicines/index.tsx:160) renders its aria-label via t('aria.openFilters') in both English and Polish, so a screen reader announces the translated label on the app's most-viewed screen (WR-05, I18N-02)"
    - "The medicine-name input's example placeholder in both CatalogFields.tsx:54 and MedicineForm.tsx:119 renders via t('form.namePlaceholder') in both English and Polish, closing the last two hardcoded-English placeholder call sites identified this session (Gap 2, I18N-02)"
    - { statement: "A full-repo grep for a raw (non-t()-wrapped) English literal in aria-label=\"...\", placeholder=\"...\", or title=\"...\" JSX attributes across src/**/*.tsx returns zero matches after this plan, confirming no third hardcoded-English call site was missed or introduced", verification: backstop }
  artifacts:
    - src/i18n/types.ts
    - src/i18n/en.ts
    - src/i18n/pl.ts
    - src/routes/medicines/index.tsx
    - src/components/CatalogFields.tsx
    - src/components/MedicineForm.tsx
  key_links:
    - "medicines/index.tsx filter-sheet trigger button aria-label -> TranslationDict.aria.openFilters (new key, added to types.ts/en.ts/pl.ts)"
    - "CatalogFields.tsx + MedicineForm.tsx medicine-name Input placeholder -> TranslationDict.form.namePlaceholder (existing key, reused — value updated in en.ts/pl.ts to match the wording already present at both call sites, rather than minting a duplicate key)"
  prohibitions:
    - requirement_id: I18N-02
      category: scope
      status: resolved
      verification: judgment
      resolution: null
      reason: null
      statement: "MUST NOT introduce or leave behind a third hardcoded-English aria-label/placeholder/title JSX attribute literal while closing WR-05 and the placeholder gap — only the two named call sites (medicines/index.tsx:160 and the two identical CatalogFields.tsx:54/MedicineForm.tsx:119 placeholders) are touched; the full-repo grep probe re-run as this plan's own final acceptance check is what keeps this fail-closed rather than attested."

---

<objective>
Close the two remaining i18n gaps blocking Phase 7's I18N-02 requirement ("all UI strings display in the active language"):

1. **WR-05 (Critical, from 07-VERIFICATION.md's re-verification):** `src/routes/medicines/index.tsx:160` has `aria-label="Open filters"` hardcoded English on the filter-sheet trigger button of the Medicines list screen — the app's primary, most-viewed screen. This is the one call site the 07-09 gap-closure plan's WR-03 scope missed (7 of 8 aria-labels were fixed; this is the 8th).

2. **Gap 2 (found this session, not in 07-VERIFICATION.md, user-approved for folding into this plan):** `placeholder="e.g. Ibuprofen 400mg"` is hardcoded English in both `src/components/CatalogFields.tsx:54` and `src/components/MedicineForm.tsx:119` (the medicine-name input's example hint). A full-repo grep this session confirmed these two placeholders plus the one aria-label above are the ONLY three remaining hardcoded-English `aria-label`/`placeholder`/`title` literals in `src/**/*.tsx`.

Both fixes extend the SAME `t()`/`useLang()`/`TranslationDict` pattern established in Plans 07-01 through 07-09 (07-CONTEXT.md D-01/D-03) — no new architecture, no new dependency, purely additive/corrective dictionary values plus literal-to-`t()`-call swaps.

Purpose: Unblock I18N-02 so Phase 7 can pass re-verification with true "full string coverage" — this is the phase's third and (per 07-VERIFICATION.md's Next Steps) final gap-closure cycle before Phase 8 can start.

Output: `aria.openFilters` added as a new `TranslationDict` key (types.ts/en.ts/pl.ts); the existing, previously-orphaned `form.namePlaceholder` key's EN/PL values corrected to match the current two call sites' wording; three call sites (medicines/index.tsx:160, CatalogFields.tsx:54, MedicineForm.tsx:119) wired through `t()`.
</objective>

<execution_context>
@C:/my-code/vibe-coding/med-stock/.claude/gsd-core/workflows/execute-plan.md
@C:/my-code/vibe-coding/med-stock/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/07-i18n-polish-language/07-CONTEXT.md
@.planning/phases/07-i18n-polish-language/07-VERIFICATION.md
@src/i18n/types.ts
@src/i18n/en.ts
@src/i18n/pl.ts
</context>

<flagged_assumptions>
Per the spec-less probe fallback protocol, the deterministic edge-probe run against I18N-01..05
in this prompt's `<specless_probe_fallback>` block returned 4 applicable items (adjacency, empty,
encoding, ordering, all scoped to I18N-02), all `unresolved`. None apply to this plan's actual
scope — swapping 3 known hardcoded string literals for `t()` calls against a fixed, enumerated
list of 2 gaps, not the general I18N-02 feature set (which Plans 07-01 through 07-09 already
built):

- **adjacency** — not applicable; this plan introduces no list/collection merge-or-collide logic.
- **empty** — partially resolved as the backstop truth above (the repo-wide zero-matches grep is
  the closest analog: "what is the result for zero remaining hardcoded literals" is exactly what
  that check answers). Beyond that, no new empty/null-input code path is added.
- **encoding** — not applicable; no byte/code-point/grapheme comparison logic is touched. The one
  encoding-adjacent fact worth naming: `aria.openFilters`'s Polish value and `form.namePlaceholder`'s
  Polish value both contain non-ASCII Polish diacritics (matching every other `pl.ts` entry) —
  this is pre-existing file-wide UTF-8 handling, unchanged by this plan.
- **ordering** — not applicable; this plan adds no sortable/comparable output, only static display
  strings.

These are carried forward as explicit flagged assumptions (never silently dropped) rather than
force-fit into truths that don't defensibly apply to this narrow 2-gap scope.
</flagged_assumptions>

<threat_model>
## Trust Boundaries

None newly introduced. This plan touches only client-side display-string rendering (translation
dictionary lookups and JSX `aria-label`/`placeholder` attribute content) inside an already-trusted,
already-rendered component tree. No new external input, no new network call, no new persistence
path, no authentication surface. ASVS Level 1 applies; nothing in this plan approaches "high
severity."

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-10-01 | Tampering | New/corrected `TranslationDict` values (`types.ts`/`en.ts`/`pl.ts`) | low | accept | Additive/corrective, compile-time-checked string constants only — no user input, no DB write, no new package. `tsc -b` (via `npm run build`) enforces both language dicts satisfy `TranslationDict` before the build can pass, so a missing key in either file fails the build rather than shipping silently. |
| T-07-10-02 | Information Disclosure | `t()` missing-key fallback (pre-existing, unchanged by this plan) | low | accept | Unchanged pre-existing behavior inherited from Plan 07-01: an unrecognized dictionary key renders its raw dot-path string instead of throwing or blanking. This plan only corrects/adds correctly-typed keys; it does not touch the fallback mechanism itself. |

**Package legitimacy gate:** Not applicable — this plan adds zero npm/pip/cargo packages.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Close WR-05 — add aria.openFilters and wire the Medicines list filter button</name>
  <reversibility rating="reversible">Additive TranslationDict key plus a one-line JSX attribute swap behind the existing t()/useLang() interface — a single git revert undoes it with no data or contract impact.</reversibility>
  <read_first>src/i18n/types.ts, src/i18n/en.ts, src/i18n/pl.ts, src/routes/medicines/index.tsx, .planning/phases/07-i18n-polish-language/07-VERIFICATION.md (WR-05 gap description and its "Next Steps" prescription)</read_first>
  <files>src/i18n/types.ts, src/i18n/en.ts, src/i18n/pl.ts, src/routes/medicines/index.tsx</files>
  <action>
    Step 1 — extend `TranslationDict` in `src/i18n/types.ts`: inside the existing top-level
    `aria: { ... }` object type (the block currently ending with `editCatalog: string`), add one
    new field on its own line: `openFilters: string`.

    Step 2 — add the English value to `src/i18n/en.ts`: inside the existing `aria: { ... }` object
    (currently ending with `editCatalog: 'Edit catalog',`), add `openFilters: '...'` on its own
    line, using the exact same wording already present as the hardcoded attribute value at
    `medicines/index.tsx:160` today (read that line via `read_first` and copy it verbatim — do not
    reword it).

    Step 3 — add the Polish value to `src/i18n/pl.ts`: inside the existing `aria: { ... }` object
    (currently ending with `editCatalog: 'Edytuj lek',`), add `openFilters: 'Otwórz filtry'` on its
    own line, matching the register of the other `aria.*` Polish entries in this file.

    Step 4 — wire the call site: in `src/routes/medicines/index.tsx`, change the filter-sheet
    trigger `<button>`'s `aria-label` attribute (line 160) from its current hardcoded string
    literal to `aria-label={t('aria.openFilters')}`. `useLang()` and `t` are already imported and
    destructured at the top of this file (line 19) — no new import needed.
  </action>
  <verify>
    <automated>npm run build && [ "$(grep -c "t('aria.openFilters')" "src/routes/medicines/index.tsx")" -ge 1 ]</automated>
    <fails_when>npm run build exits non-zero (a missing key in en.ts or pl.ts breaks TranslationDict structural typing, or a JSX/TS syntax error was introduced), or the grep -c count is 0 (the aria.openFilters call site is missing from medicines/index.tsx)</fails_when>
  </verify>
  <acceptance_criteria>
    - src/i18n/types.ts's `aria` type has a new `openFilters: string` field
    - src/i18n/en.ts's `aria` object has `openFilters` populated with the exact wording that was previously hardcoded at medicines/index.tsx:160
    - src/i18n/pl.ts's `aria` object has `openFilters` populated with a Polish translation
    - src/routes/medicines/index.tsx line 160's aria-label attribute calls t('aria.openFilters') instead of a string literal
    - npm run build exits 0
  </acceptance_criteria>
  <done>WR-05 is closed: the Medicines list screen's filter-sheet trigger button renders a translated aria-label in both languages.</done>
</task>

<task type="auto">
  <name>Task 2: Close Gap 2 — correct and wire form.namePlaceholder at both name-input call sites</name>
  <reversibility rating="reversible">Corrects an existing dictionary key's value and swaps 2 hardcoded literal placeholders for that existing t() call — no interface change, trivially revertable.</reversibility>
  <read_first>src/i18n/en.ts, src/i18n/pl.ts, src/components/CatalogFields.tsx, src/components/MedicineForm.tsx</read_first>
  <files>src/i18n/en.ts, src/i18n/pl.ts, src/components/CatalogFields.tsx, src/components/MedicineForm.tsx</files>
  <action>
    Context: `src/i18n/types.ts` already declares `form.namePlaceholder: string` and both
    `en.ts`/`pl.ts` already populate it, but with a shorter example than the one actually used at
    the two real call sites, and no call site currently references the key — it has been orphaned
    since it was added (flagged as WR-08 in 07-REVIEW.md and explicitly deferred by 07-09-PLAN.md).
    Reuse this existing key rather than minting a new one: its purpose (the medicine-name input's
    example placeholder) is exactly what both call sites need.

    Step 1 — in `src/i18n/en.ts`, update the existing `form.namePlaceholder` field's value to match
    the wording already hardcoded at both `CatalogFields.tsx:54` and `MedicineForm.tsx:119` today
    (read both via `read_first` — they are identical; copy that exact wording verbatim as the new
    value, replacing the current shorter example).

    Step 2 — in `src/i18n/pl.ts`, update the existing `form.namePlaceholder` field's value to
    `'np. Ibuprofen 400mg'`, following this file's existing `np.` prefix convention for "e.g."
    (matching the pattern already used by the current, shorter Polish value of this same key).

    Step 3 — in `src/components/CatalogFields.tsx`, change the medicine-name `Input`'s
    `placeholder` attribute (line 54) from its current hardcoded string literal to
    `placeholder={t('form.namePlaceholder')}`. `useLang()`/`t` are already imported and
    destructured in this file (line 42) — no new import needed.

    Step 4 — in `src/components/MedicineForm.tsx`, change the medicine-name `Input`'s `placeholder`
    attribute (line 119) from its current hardcoded string literal to
    `placeholder={t('form.namePlaceholder')}`. `useLang()`/`t` are already imported and destructured
    in this file (line 63) — no new import needed.

    Do not add a second/duplicate key for this purpose, and do not touch `form.name`,
    `form.nameRequired`, or any other existing `form.*` field — only `form.namePlaceholder`'s value
    and these two call sites change.
  </action>
  <verify>
    <automated>npm run build && [ "$(grep -c "t('form.namePlaceholder')" "src/components/CatalogFields.tsx")" -ge 1 ] && [ "$(grep -c "t('form.namePlaceholder')" "src/components/MedicineForm.tsx")" -ge 1 ] && ! grep -rnE 'aria-label="[A-Za-z]|placeholder="[A-Za-z]|title="[A-Za-z]' src --include='*.tsx'</automated>
    <fails_when>npm run build exits non-zero, or either grep -c count is 0 (form.namePlaceholder not wired at that call site), or the final repo-wide grep finds any remaining match (a third hardcoded aria-label/placeholder/title literal was missed or introduced)</fails_when>
  </verify>
  <acceptance_criteria>
    - src/i18n/en.ts's form.namePlaceholder value matches the wording previously hardcoded at CatalogFields.tsx:54 and MedicineForm.tsx:119
    - src/i18n/pl.ts's form.namePlaceholder value is "np. Ibuprofen 400mg"
    - src/components/CatalogFields.tsx line 54's placeholder attribute calls t('form.namePlaceholder') instead of a string literal
    - src/components/MedicineForm.tsx line 119's placeholder attribute calls t('form.namePlaceholder') instead of a string literal
    - No other form.* key or call site is modified
    - A full-repo grep for aria-label="[A-Za-z], placeholder="[A-Za-z], or title="[A-Za-z] across src/**/*.tsx returns zero matches
    - npm run build exits 0
  </acceptance_criteria>
  <done>Gap 2 is closed: the medicine-name input's example placeholder renders translated text in both CatalogFields.tsx and MedicineForm.tsx, and the full-repo hardcoded-string probe confirms no third call site remains.</done>
</task>

</tasks>

<verification>
Run in order after both tasks complete:

1. `npm run build` — exits 0 (TypeScript strict-mode compile across all 6 modified files; `TranslationDict` structural typing enforced for both `en.ts` and `pl.ts`).
2. Repo-wide probe (this plan's own final acceptance check, per the session's verification-command note): `grep -rnE 'aria-label="[A-Za-z]|placeholder="[A-Za-z]|title="[A-Za-z]' src --include='*.tsx'` returns **zero matches** — confirms WR-05 and Gap 2 are both closed and no third call site was missed or introduced. (A JSX expression like `aria-label={t('aria.openFilters')}` does not match this pattern, since the character immediately after the opening `"`/`{` is not a bare letter following a literal quote.)
3. `npx vitest run` — exits 0, no regression in existing suites (this plan touches no test-covered logic; this is a regression guard only).
4. `npm run lint` — exits 0 (oxlint across all modified files).

All 4 must pass before this plan is considered complete. `npm run build` failing on a missing dictionary key is the primary safety net — `TranslationDict`'s structural type makes an incomplete translation a compile error, not a silent runtime fallback. The repo-wide grep in step 2 is the safety net against a repeat of this same phase's earlier miss (07-09 closed 7/8 aria-labels and missed one; this step makes "zero remaining hardcoded literals" a checked fact, not an assumption).
</verification>

<success_criteria>
- WR-05 (medicines/index.tsx:160 aria-label) and Gap 2 (CatalogFields.tsx:54 + MedicineForm.tsx:119 placeholders) are both closed.
- `src/i18n/types.ts`, `en.ts`, and `pl.ts` all compile together under `npm run build` with `aria.openFilters` present and `form.namePlaceholder` corrected, in both languages.
- No hardcoded English `aria-label`/`placeholder`/`title` string literal remains anywhere in `src/**/*.tsx` — confirmed by the repo-wide grep probe.
- `npx vitest run` and `npm run lint` both exit 0 with no new failures.
- Requirement I18N-02 ("All UI strings display in the active language") is unblocked; per 07-VERIFICATION.md this was the last documented gap standing between Phase 7 and a passing re-verification.
</success_criteria>

## Artifacts This Phase Produces

New/corrected `TranslationDict` values added or changed by this plan:

**New top-level `aria` namespace key (1 new key, WR-05):**
- `aria.openFilters` — Medicines list screen filter-sheet trigger button, `medicines/index.tsx:160`

**Corrected existing `form` namespace key (0 new keys, Gap 2):**
- `form.namePlaceholder` — medicine-name input example placeholder; value corrected in both `en.ts`/`pl.ts` to match the wording already used at `CatalogFields.tsx:54` and `MedicineForm.tsx:119`; this key existed since an earlier plan but was orphaned (no call site referenced it) until this plan wires it — no new key was minted for this purpose.

<output>
Create `.planning/phases/07-i18n-polish-language/07-10-SUMMARY.md` when done
</output>
