---
phase: 07-i18n-polish-language
plan: 08
type: execute
wave: 1
depends_on: []
gap_closure: true
gap_ids: [G-07-19, G-07-20b]
files_modified:
  - src/i18n/types.ts
  - src/i18n/en.ts
  - src/i18n/pl.ts
  - src/components/CSVColumnMapper.tsx
autonomous: true
requirements: [I18N-04]

estimate:
  tokens: 28000
  raw_tokens: 14000
  tasks: 1
  confidence: high

must_haves:
  truths:
    - All six previously hardcoded English strings in CSVColumnMapper render via t() calls
    - Switching to Polish shows Polish text in the CSV column-mapping screen
    - TypeScript build passes with zero errors (TranslationDict structural check)
  artifacts:
    - src/i18n/types.ts (csv section extended with mapperDescription, mapToField, skip, nameRequired, preview)
    - src/i18n/en.ts (English values for five new keys)
    - src/i18n/pl.ts (Polish values for five new keys)
    - src/components/CSVColumnMapper.tsx (useLang() added; all six hardcoded strings replaced)
  key_links:
    - CSVColumnMapper imports useLang from @/i18n/context
    - t('csv.cancel') reuses the existing key; no duplicate key added
    - TranslationDict in types.ts drives tsc validation of en.ts and pl.ts shape
---

<objective>
Internationalise CSVColumnMapper.tsx — the final component with zero i18n coverage in Phase 07.

Purpose: Six hardcoded English strings in CSVColumnMapper caused UAT gaps G-07-19 and G-07-20b.
Switching to Polish while in the CSV import flow showed a fully English column-mapping screen,
making the entire CSV import flow appear untranslated.

Output: Five new csv.* keys in types/en/pl, and CSVColumnMapper wired to useLang() so all
labels respond to the active language.
</objective>

<execution_context>
@C:/my-code/vibe-coding/med-stock/.claude/gsd-core/workflows/execute-plan.md
@C:/my-code/vibe-coding/med-stock/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-i18n-polish-language/07-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add csv mapper keys and translate CSVColumnMapper</name>
  <files>
    src/i18n/types.ts,
    src/i18n/en.ts,
    src/i18n/pl.ts,
    src/components/CSVColumnMapper.tsx
  </files>
  <action>
    Extend the csv section of TranslationDict and both translation files, then wire
    CSVColumnMapper to the i18n context.

    Step 1 — src/i18n/types.ts: Inside the `csv` section (after the existing `cancel: string`
    entry, before the closing `}`), add five new string fields in this order:
      mapperDescription: string
      mapToField: string
      skip: string
      nameRequired: string
      preview: string

    Step 2 — src/i18n/en.ts: Inside the `csv` section (parallel position), add the five keys:
      mapperDescription: 'Select which columns in your spreadsheet map to medicine fields. Mark columns as "Skip" if they\'re not needed.'
      mapToField: 'Map to field:'
      skip: '(skip)'
      nameRequired: "Required field 'name' must be mapped before preview is enabled"
      preview: 'Preview'

    Step 3 — src/i18n/pl.ts: Inside the `csv` section, add the five keys:
      mapperDescription: 'Wybierz, które kolumny arkusza odpowiadają polom leku. Oznacz kolumny jako „Pomiń", jeśli nie są potrzebne.'
      mapToField: 'Mapuj na pole:'
      skip: '(pomiń)'
      nameRequired: "Pole wymagane 'nazwa' musi być zmapowane przed podglądem"
      preview: 'Podgląd'

    Step 4 — src/components/CSVColumnMapper.tsx:
    - Add import for useLang: `import { useLang } from '@/i18n/context'`
    - Inside the component function body, before the return statement, add: `const { t } = useLang()`
    - Replace the six hardcoded strings:
      a. Description paragraph text (lines 31-33) → {t('csv.mapperDescription')}
      b. SelectValue placeholder="Map to field:" → placeholder={t('csv.mapToField')}
      c. SelectItem for SKIP_VALUE, child text "(skip)" → {t('csv.skip')}
      d. Validation paragraph text (line 67) → {t('csv.nameRequired')}
      e. Preview Button text → {t('csv.preview')}
      f. Cancel Button text → {t('csv.cancel')}  (reuses the key that already existed — per G-07-19 diagnosis)

    The description paragraph contains an HTML entity for quotes (&quot;Skip&quot;). After
    replacing with t('csv.mapperDescription') the quotes are part of the Polish/English string
    so no entity encoding is needed — plain JSX text node is correct.
  </action>
  <verify>
    <automated>npx vitest run && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>
    npm run build exits 0 with zero TypeScript errors.
    CSVColumnMapper.tsx contains useLang() and six t() calls.
    src/i18n/types.ts csv section has 11 keys (6 pre-existing + 5 new).
    en.ts and pl.ts csv sections each have 11 matching keys.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| translation strings → JSX render | String values from en.ts/pl.ts are developer-controlled static literals, not user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation |
|-----------|----------|-----------|----------|-------------|------------|
| T-07-08-01 | Tampering | translation strings | low | accept | Strings are static developer-authored literals in source-controlled files; no user input path |
| T-07-08-SC | Tampering | npm/pip/cargo installs | high | accept | No new packages installed in this plan; existing @/i18n/context is an in-repo module |
</threat_model>

<verification>
Run after execution:

1. TypeScript build: `npm run build` — must exit 0
2. Manual smoke test: open CSV import flow with Polish active, confirm column-mapper screen shows Polish labels
3. Confirm `grep -c "useLang" src/components/CSVColumnMapper.tsx` returns 1
</verification>

<success_criteria>
- npm run build passes with zero errors
- CSVColumnMapper renders Polish labels when Polish is active
- No hardcoded English strings remain in CSVColumnMapper (six t() calls replace six literals)
- Gaps G-07-19 and G-07-20b are resolved
</success_criteria>

<output>
Create `.planning/phases/07-i18n-polish-language/07-08-SUMMARY.md` when done.
</output>
