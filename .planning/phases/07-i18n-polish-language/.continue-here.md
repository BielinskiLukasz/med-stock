---
context: phase
phase: 07-i18n-polish-language
task: 8
total_tasks: 12
status: in_progress
last_updated: 2026-09-16T20:57:40.445Z
---

_No blocking constraints from this session — execution was clean throughout._

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Stale `.continue-here.md` trusted at face value | At the start of this session, the phase's `.continue-here.md` still described the pre-07-08 state ("one plan away", referencing plan 08) even though 07-08 AND a full gap-closure cycle (07-09) had already happened since it was written. It correctly had no blocking rows, so it didn't cause harm, but a resuming agent that skimmed only the `<current_state>` prose without cross-checking STATE.md/git log would have mis-oriented itself. | advisory | Before acting on this file's `<current_state>`/`<remaining_work>` prose, cross-check against `git log --oneline -10` and STATE.md's `stopped_at`/`completed_plans` fields — this file's frontmatter `last_updated` is the freshness signal; if it's older than the latest phase commit, treat the prose as historical context only, not current truth. |
| Fixed enumerated gap lists keep missing one instance | Both re-verification cycles this session (07-08→07-09, and 07-09's own follow-up) found that a plan explicitly enumerating "8 aria-label instances across 5 files" or similar fixed lists reliably misses one call site elsewhere in the codebase. | advisory | When authoring the next gap-closure plan for a hardcoded-string category (aria-label, placeholder, etc.), grep repo-wide for the pattern (e.g. `grep -rn 'aria-label="[A-Z]' src/`) instead of trusting a manually-enumerated list from a prior review. |

**Severity values:** `blocking` — resuming agent must pass understanding check before proceeding. `advisory` — important context, does not gate resumption.

<current_state>
Phase 07 (i18n / Polish Language) has all 9 plans executed (07-01 through 07-09), but is in its **second** `gaps_found` re-verification cycle. This session executed gap-closure plan 07-09 (closing CR-01, WR-01, WR-02, and 7/8 of WR-03 from the first re-verification's code review), then ran a fresh code review and a fresh phase-goal verification. The verifier found ONE new gap not covered by 07-09's scope: **WR-05** — `src/routes/medicines/index.tsx:160` has `aria-label="Open filters"` hardcoded in English, on the app's primary landing screen (Medicines list, first tab). This is a NEW gap, not something 07-09 failed to fix — it was simply outside the 8-instance/5-file list 07-09 was scoped to.

`I18N-02` has been reverted out of `Complete` in REQUIREMENTS.md accordingly. No plan yet exists to close WR-05 — that's the next step.
</current_state>

<completed_work>

This session:
- Executed 07-09-PLAN.md (4 tasks: tracer + 3 auto) — closed CR-01, WR-01, WR-02, 7/8 of WR-03. Commits `c48c9aa`, `02e22a7`, `1f2de39`, `fdf96a5`, `82e4a76`, `dbeb58a`.
- Post-wave gates: `npm run build`, `npx vitest run` (143/143), `npm run lint` all passed; schema-drift/codebase-drift/ui-safety capability gates all passed with `block: false`.
- Fresh code review of all 39 phase-07 touched files (`07-REVIEW.md`, commit `543d203`). Confirmed CR-01/WR-01/WR-02 resolved, WR-03 mostly resolved. Found 8 NEW findings: CR-02 (Dexie null-vs-undefined bug, unrelated to i18n, out of phase scope), WR-05 through WR-08, IN-04 through IN-06.
- Phase re-verification (`07-VERIFICATION.md`, commit `a1cdc18`): status `gaps_found`, 1 gap (WR-05).
- Reverted `I18N-02` from `Complete` (commit `425d4b3`) since the shared-ID gate had let 07-09 mark it complete before this gap surfaced.

Prior sessions (01-07 plans, then 07-08 gap closure): see STATE.md `## Decisions` section and earlier commits — all i18n foundation, screen translations, and the CSVColumnMapper gap closure are done and verified.
</completed_work>

<remaining_work>

- Run `/gsd-plan-phase 07 --gaps` to author a new gap-closure plan (likely `07-10-PLAN.md`) for WR-05: add an `aria.openFilters` key to `src/i18n/types.ts`/`en.ts`/`pl.ts`, and swap `medicines/index.tsx:160`'s hardcoded `aria-label="Open filters"` for `t('aria.openFilters')`.
  - Recommend the new plan grep repo-wide for any OTHER remaining hardcoded `aria-label` literals before finalizing scope, per the anti-pattern noted above.
- Run `/gsd-execute-phase 07 --gaps-only` to execute the new plan.
- Re-run phase verification. If it passes, the orchestrator's `update_roadmap` step (phase.complete) runs automatically and Phase 07 closes.
- After Phase 07 passes: manual/UAT check — toggle to Polish, visit the Medicines list, and confirm a screen reader announces the filter button's aria-label in Polish (07-VERIFICATION.md's one `human_verification` item).

Not part of Phase 07, but discovered and worth tracking separately:
- **CR-02** (Critical, unrelated to i18n): `src/routes/medicines/[id].tsx` and `[id].edit.tsx` check `catalog === null`/`medicine === null` for "not found," but Dexie's `Table.get()` resolves to `undefined` for a missing key — the branch is dead code, so a deleted/nonexistent id shows an infinite spinner with no escape. See `07-REVIEW.md` for detail. Needs its own backlog item/phase — do not fix inside a Phase 07 i18n gap-closure plan (scope creep).
- **WR-07** (explicitly deferred, re-flagged for awareness): `src/lib/utils.ts`'s `formatDate()` hardcodes `'No expiry'`/`'Bez daty ważności'` instead of reusing the `dates.noExpiry` dict key.
</remaining_work>

<decisions_made>

- Reverted `I18N-02` out of `Complete` a second time — the shared-ID gate marks a requirement complete when its last declaring plan finishes, but that doesn't account for gaps a re-verification finds outside any plan's declared scope. Requirement stays open until WR-05 closes and verification passes.
- CR-02 and WR-07 are explicitly OUT of this phase's scope — do not fix them while closing WR-05. Track separately.
</decisions_made>

<blockers>
None. WR-05 is a small, well-understood, mechanical fix (same pattern as 07-09's Task 4) — no architecture questions, no auth gates, no external dependencies.
</blockers>

## Required Reading (in order)

1. `.planning/phases/07-i18n-polish-language/07-VERIFICATION.md` — the current re-verification report; WR-05's `gaps:` entry has exact file/line and the `missing:` checklist for the fix.
2. `.planning/phases/07-i18n-polish-language/07-REVIEW.md` — the code review that found WR-05 (and the other new findings CR-02/WR-06/WR-07/WR-08, all out of scope for the next gap plan).
3. `.planning/phases/07-i18n-polish-language/07-09-PLAN.md` — the immediately-prior gap-closure plan; WR-05's fix follows the exact same `aria.*` key pattern this plan established in Task 4 (`src/i18n/types.ts`/`en.ts`/`pl.ts` + one call-site swap).

## Infrastructure State

- Dev server: not running.
- No background processes, no async jobs.
- Working tree is clean as of this handoff (all session changes committed).

<context>
This is a mechanical, low-risk gap. The `aria.*` namespace, the `t()`/`useLang()` pattern, and the exact style of fix were all just established in 07-09. The next plan is essentially "repeat Task 4's pattern for one more file." The main risk is a THIRD missed instance — hence the anti-pattern note above recommending a repo-wide grep before finalizing the next plan's scope, rather than another manually-enumerated list.
</context>

<next_action>
Run `/gsd-plan-phase 07 --gaps` to author the WR-05 gap-closure plan.
</next_action>
