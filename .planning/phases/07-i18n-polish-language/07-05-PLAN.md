---
phase: 07-i18n-polish-language
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - src/i18n/types.ts
  - src/i18n/en.ts
  - src/i18n/pl.ts
autonomous: true
gap_closure: true
requirements:
  - I18N-02
  - I18N-04

estimate:
  tokens: 38000
  raw_tokens: 22000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "LOCATION_KEYS maps all 7 predefined DB locations: Bathroom Cabinet, Bedroom Cabinet, Kitchen Drawer, Living Room Cabinet, Medicine Box, Refrigerator, Travel Kit (I18N-04, Gap 1)"
    - "TranslationDict has typed keys for all strings needed by the 7 gap components: form.boxesToMove, form.unitsPerBox, form.quantityToMove, form.boxValidationMin, form.boxValidationMax, form.quantityValidationMin, form.quantityValidationMax, form.saving (I18N-02, Gap 4)"
    - "TranslationDict has a catalog section with selectOrCreate, placeholderText, createEntry, noMedicinesYet (I18N-02, Gap 6)"
    - "TranslationDict.data has syncStep1, syncStep2, syncStep3, syncStep4, syncNote, exportDescription, importCSVDescription (I18N-02, Gap 7)"
    - "TranslationDict has a csv section with previewHeader, rowCount, importing, importComplete, back, cancel (I18N-02, Gap 8)"
    - "TranslationDict.toasts has csvImportNeedsCatalog (I18N-02, low-priority Gap 9)"
    - "npx tsc --noEmit passes — en.ts and pl.ts both satisfy the full updated TranslationDict type"
    - "TranslationDict key structure follows the flat namespace convention from D-03: screen/component namespaces (form.*, catalog.*, csv.*, data.syncStep*) — both en.ts and pl.ts satisfy the same type (D-03)"
  artifacts:
    - "src/i18n/types.ts — LOCATION_KEYS with 7 entries; TranslationDict extended with all new keys"
    - "src/i18n/en.ts — English values for all 30+ new keys"
    - "src/i18n/pl.ts — Polish values for all 30+ new keys"
  key_links:
    - "LOCATION_KEYS string keys must exactly match the predefined location name strings used in the DB seed data ('Living Room Cabinet' etc.) — Plan 07-06 components call t(LOCATION_KEYS[v]) with these exact values"
    - "catalog and csv are new top-level sections in TranslationDict — both en.ts and pl.ts must implement every field or the build fails"
---

<objective>
Extend the i18n dictionary (TranslationDict type, en.ts, pl.ts) with all translation keys missing from the currently-executed Phase 07 plans. This plan adds no component wiring — it only grows the dictionary so that Plan 07-06 can wire the components with full type safety.

Purpose: TypeScript strict mode means every t() call key must exist in TranslationDict; adding keys here lets Plan 07-06 proceed without structural changes to types.
Output: All three dictionary files updated and type-checked; `npx tsc --noEmit` passes before any component is touched.
</objective>

<execution_context>
@C:/my-code/vibe-coding/med-stock/.claude/gsd-core/workflows/execute-plan.md
@C:/my-code/vibe-coding/med-stock/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-i18n-polish-language/07-CONTEXT.md
@.planning/phases/07-i18n-polish-language/07-VERIFICATION.md
</context>

<tasks>

<task type="tracer">
  <name>Extend LOCATION_KEYS and locationNames — prove dictionary extension compiles (Gap 1)</name>

  <files>src/i18n/types.ts, src/i18n/en.ts, src/i18n/pl.ts</files>

  <read_first>
    - src/i18n/types.ts (full file) — read the LOCATION_KEYS Record (lines 250–254) to identify exact insertion point; read TranslationDict.locationNames section (lines 34–39) for the type to extend
    - src/i18n/en.ts (full file) — read locationNames object (lines 32–37) for insertion point
    - src/i18n/pl.ts (full file) — read locationNames object (lines 32–37) for insertion point
  </read_first>

  <action>
  Make three coordinated edits — all in one pass per file.

  In src/i18n/types.ts:

  1. In the LOCATION_KEYS Record (around line 250), add 4 new entries after the 'Kitchen Drawer' entry so the Record maps all 7 predefined locations:
     'Living Room Cabinet' maps to 'locationNames.livingRoomCabinet',
     'Medicine Box' maps to 'locationNames.medicineBox',
     'Refrigerator' maps to 'locationNames.refrigerator',
     'Travel Kit' maps to 'locationNames.travelKit'.

  2. In TranslationDict, inside the locationNames section (currently: bathroomCabinet, bedroomCabinet, kitchenDrawer, other), add 4 new string fields after kitchenDrawer and before other:
     livingRoomCabinet: string
     medicineBox: string
     refrigerator: string
     travelKit: string

  In src/i18n/en.ts:

  In the locationNames object, add 4 new entries after kitchenDrawer: 'Kitchen Drawer' and before other: 'Other':
     livingRoomCabinet: 'Living Room Cabinet',
     medicineBox: 'Medicine Box',
     refrigerator: 'Refrigerator',
     travelKit: 'Travel Kit',

  In src/i18n/pl.ts:

  In the locationNames object, add 4 new entries after kitchenDrawer: 'Szuflada kuchenna' and before other: 'Inne':
     livingRoomCabinet: 'Szafka w salonie',
     medicineBox: 'Apteczka',
     refrigerator: 'Lodówka',
     travelKit: 'Apteczka podróżna',

  Do NOT touch CATEGORY_KEYS, UNIT_KEYS, FORM_TYPE_KEYS, or any other section. The `other` key that already exists in locationNames maps to t('locationNames.other') for location: null display.
  </action>

  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>

  <acceptance_criteria>
    - src/i18n/types.ts LOCATION_KEYS contains 'Living Room Cabinet', 'Medicine Box', 'Refrigerator', 'Travel Kit' as keys (grep -c "livingRoomCabinet" src/i18n/types.ts outputs 2 — one in LOCATION_KEYS value, one in TranslationDict type)
    - src/i18n/en.ts contains livingRoomCabinet: 'Living Room Cabinet' (grep -c "livingRoomCabinet" src/i18n/en.ts outputs 1)
    - src/i18n/pl.ts contains livingRoomCabinet: 'Szafka w salonie' (grep -c "livingRoomCabinet" src/i18n/pl.ts outputs 1)
    - npx tsc --noEmit exits 0 with no type errors
  </acceptance_criteria>

  <done>LOCATION_KEYS has 7 entries. TypeScript build confirms en.ts and pl.ts satisfy the extended TranslationDict.locationNames type — the tracer pattern (extend type + both translations + build check) is proven.</done>
</task>

<task type="auto">
  <name>Add all remaining translation keys — form, catalog, data.sync, csv, toasts sections (Gaps 4–9)</name>

  <files>src/i18n/types.ts, src/i18n/en.ts, src/i18n/pl.ts</files>

  <read_first>
    - src/i18n/types.ts (full file, after Task 1) — locate insertion points for form section (after deleteConfirmBody), data section (after importConfirmAction), toasts section (after catalogDeleted); confirm no catalog or csv sections exist yet
    - src/i18n/en.ts (full file, after Task 1) — locate insertion points for form, data, toasts objects; confirm no catalog or csv keys exist
    - src/i18n/pl.ts (full file, after Task 1) — same as en.ts
  </read_first>

  <action>
  Add keys in three files, keeping all additions type-safe. All three files must stay in sync — every key added to TranslationDict must have a value in en.ts and pl.ts.

  === src/i18n/types.ts ===

  A. In the form section of TranslationDict, add 8 new string fields after deleteConfirmBody (which is the current last field in form):
     boxesToMove: string
     unitsPerBox: string
     quantityToMove: string
     boxValidationMin: string
     boxValidationMax: string
     quantityValidationMin: string
     quantityValidationMax: string
     saving: string

  B. In the data section of TranslationDict, add 7 new string fields after importConfirmAction (current last field in data):
     syncStep1: string
     syncStep2: string
     syncStep3: string
     syncStep4: string
     syncNote: string
     exportDescription: string
     importCSVDescription: string

  C. In the toasts section of TranslationDict, add 1 new string field after catalogDeleted (current last field):
     csvImportNeedsCatalog: string

  D. Add a new catalog section to TranslationDict after the detail section (current last section). The new section is:
     catalog: {
       selectOrCreate: string
       placeholderText: string
       createEntry: string
       noMedicinesYet: string
     }

  E. Add a new csv section to TranslationDict after the catalog section:
     csv: {
       previewHeader: string
       rowCount: string
       importing: string
       importComplete: string
       back: string
       cancel: string
     }

  === src/i18n/en.ts ===

  A. In the form object, add after deleteConfirmBody:
     boxesToMove: 'Boxes to move',
     unitsPerBox: 'per box',
     quantityToMove: 'Quantity to move',
     boxValidationMin: 'Must be at least 1 box',
     boxValidationMax: 'Cannot exceed available boxes',
     quantityValidationMin: 'Quantity must be at least 1',
     quantityValidationMax: 'Cannot exceed available quantity',
     saving: 'Moving\u2026',

  B. In the data object, add after importConfirmAction:
     syncStep1: '1. Use the "Export backup" button above to save a backup file to a shared folder (e.g., OneDrive, Google Drive, Dropbox).',
     syncStep2: '2. Share the link to the folder with your household member.',
     syncStep3: '3. Your household member opens this app on their device, downloads your backup file, and uses the "Import backup" button to restore it.',
     syncStep4: '4. To push your changes to your household member, export a new backup and they import it again. The most recent backup is the source of truth.',
     syncNote: 'Note: All changes are manual. There is no automatic sync \u2014 each household member must explicitly export and import when sharing updates.',
     exportDescription: 'Download your entire inventory as a JSON file. You can restore this backup on another device or share it with household members.',
     importCSVDescription: 'Bulk import medicines from a CSV spreadsheet. Map your spreadsheet columns to medicine fields, preview the data, and commit.',

  C. In the toasts object, add after catalogDeleted:
     csvImportNeedsCatalog: 'CSV import requires at least one medicine catalog. Add a medicine first, then retry.',

  D. Add a new catalog object after the detail object:
     catalog: {
       selectOrCreate: 'Select or Create Medicine',
       placeholderText: 'Start typing a medicine name\u2026',
       createEntry: 'Create',
       noMedicinesYet: 'No medicines yet. Type a name to create one.',
     },

  E. Add a new csv object after the catalog object:
     csv: {
       previewHeader: 'Preview (first 5 rows)',
       rowCount: 'rows',
       importing: 'Importing\u2026',
       importComplete: 'Import',
       back: 'Back',
       cancel: 'Cancel',
     },

  === src/i18n/pl.ts ===

  A. In the form object, add after deleteConfirmBody:
     boxesToMove: 'Opakowania do przeniesienia',
     unitsPerBox: 'na opakowanie',
     quantityToMove: 'Ilo\u015b\u0107 do przeniesienia',
     boxValidationMin: 'Musi wynosi\u0107 co najmniej 1 opakowanie',
     boxValidationMax: 'Nie mo\u017cna przekroczy\u0107 dost\u0119pnych opakowa\u0144',
     quantityValidationMin: 'Ilo\u015b\u0107 musi wynosi\u0107 co najmniej 1',
     quantityValidationMax: 'Nie mo\u017cna przekroczy\u0107 dost\u0119pnej ilo\u015bci',
     saving: 'Przenosz\u0119\u2026',

  Note on Polish Unicode escapes: the executor may write the Polish characters directly (no need for escape sequences if the file uses UTF-8 encoding, which it does). The actual characters are: Ilość, Musi wynosić, Nie można przekroczyć, dostępnych, opakowań, dostępnej, ilości, Przenoszę….

  B. In the data object, add after importConfirmAction:
     syncStep1: '1. U\u017cyj przycisku \u201eEksportuj kopi\u0119 zapasow\u0105\u201d powy\u017cej, aby zapisa\u0107 plik w folderze udost\u0119pnionym (np. OneDrive, Google Drive, Dropbox).',
     syncStep2: '2. Udost\u0119pnij link do folderu swojemu domownikowi.',
     syncStep3: '3. Tw\xf3j domownik otwiera t\u0119 aplikacj\u0119 na swoim urz\u0105dzeniu, pobiera plik kopii zapasowej i u\u017cywa przycisku \u201eImportuj kopi\u0119 zapasow\u0105\u201d, aby go przywr\xf3ci\u0107.',
     syncStep4: '4. Aby przes\u0142a\u0107 swoje zmiany do domownika, wyeksportuj now\u0105 kopi\u0119 zapasow\u0105, a on j\u0105 zaimportuje. Najnowsza kopia zapasowa jest \u017ar\xf3d\u0142em prawdy.',
     syncNote: 'Uwaga: Wszystkie zmiany s\u0105 r\u0119czne. Nie ma automatycznej synchronizacji \u2014 ka\u017cdy domownik musi r\u0119cznie eksportowa\u0107 i importowa\u0107 przy udost\u0119pnianiu aktualizacji.',
     exportDescription: 'Pobierz ca\u0142y inwentarz jako plik JSON. Mo\u017cesz przywr\xf3ci\u0107 t\u0119 kopi\u0119 zapasow\u0105 na innym urz\u0105dzeniu lub udost\u0119pni\u0107 j\u0105 domownikom.',
     importCSVDescription: 'Importuj leki hurtowo z pliku CSV. Przypisz kolumny arkusza do p\xf3l leku, podrzyj dane i zatwierd\u017a.',

  Note: The executor should write Polish characters directly (UTF-8). Plain text equivalents for the main values:
     syncStep1 Polish: '1. Użyj przycisku „Eksportuj kopię zapasową" powyżej, aby zapisać plik w folderze udostępnionym (np. OneDrive, Google Drive, Dropbox).'
     syncStep2 Polish: '2. Udostępnij link do folderu swojemu domownikowi.'
     syncStep3 Polish: '3. Twój domownik otwiera tę aplikację na swoim urządzeniu, pobiera plik kopii zapasowej i używa przycisku „Importuj kopię zapasową", aby go przywrócić.'
     syncStep4 Polish: '4. Aby przesłać swoje zmiany do domownika, wyeksportuj nową kopię zapasową, a on ją zaimportuje. Najnowsza kopia zapasowa jest źródłem prawdy.'
     syncNote Polish: 'Uwaga: Wszystkie zmiany są ręczne. Nie ma automatycznej synchronizacji — każdy domownik musi ręcznie eksportować i importować przy udostępnianiu aktualizacji.'
     exportDescription Polish: 'Pobierz cały inwentarz jako plik JSON. Możesz przywrócić tę kopię zapasową na innym urządzeniu lub udostępnić ją domownikom.'
     importCSVDescription Polish: 'Importuj leki hurtowo z pliku CSV. Przypisz kolumny arkusza do pól leku, podejrzyj dane i zatwierdź.'

  C. In the toasts object, add after catalogDeleted:
     csvImportNeedsCatalog: 'Import CSV wymaga co najmniej jednego leku. Dodaj lek i spróbuj ponownie.',

  D. Add a new catalog object after the detail object:
     catalog: {
       selectOrCreate: 'Wybierz lub utwórz lek',
       placeholderText: 'Zacznij pisać nazwę leku…',
       createEntry: 'Utwórz',
       noMedicinesYet: 'Brak leków. Wpisz nazwę, aby utworzyć nowy.',
     },

  E. Add a new csv object after the catalog object:
     csv: {
       previewHeader: 'Podgląd (pierwsze 5 wierszy)',
       rowCount: 'wierszy',
       importing: 'Importowanie…',
       importComplete: 'Importuj',
       back: 'Wstecz',
       cancel: 'Anuluj',
     },
  </action>

  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -5 && grep -c "syncStep1" src/i18n/en.ts && grep -c "syncStep1" src/i18n/pl.ts && grep -c "csvImportNeedsCatalog" src/i18n/types.ts</automated>
  </verify>

  <acceptance_criteria>
    - npx tsc --noEmit exits 0 (all three files satisfy the updated TranslationDict type)
    - src/i18n/types.ts contains both a catalog and a csv section in TranslationDict (grep -c "selectOrCreate" src/i18n/types.ts outputs 1; grep -c "previewHeader" src/i18n/types.ts outputs 1)
    - src/i18n/en.ts contains catalog.selectOrCreate value 'Select or Create Medicine' and csv.previewHeader value 'Preview (first 5 rows)'
    - src/i18n/pl.ts contains catalog.selectOrCreate value and csv.previewHeader value in Polish
    - src/i18n/en.ts contains syncStep1 through syncStep4 and syncNote (grep -c "syncStep" src/i18n/en.ts outputs 5)
    - src/i18n/pl.ts contains syncStep1 through syncStep4 and syncNote (grep -c "syncStep" src/i18n/pl.ts outputs 5)
    - src/i18n/en.ts form object contains saving: 'Moving…' (grep -c "saving:" src/i18n/en.ts outputs 1)
    - src/i18n/pl.ts toasts contains csvImportNeedsCatalog (grep -c "csvImportNeedsCatalog" src/i18n/pl.ts outputs 1)
  </acceptance_criteria>

  <done>All 30+ new translation keys exist in TranslationDict (typed), en.ts (English), and pl.ts (Polish). TypeScript build passes. Plan 07-06 can now wire all 7 gap components with full type safety.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Translation dict → component render | Translation strings are hardcoded in codebase; no user input crosses this boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-05-01 | Tampering | src/i18n/pl.ts | low | accept | Polish strings are hardcoded in source; no runtime input; reviewed on commit |
| T-07-05-02 | Information Disclosure | Translation values | low | accept | Strings are display text only; no internal logic or secrets exposed |
</threat_model>

<verification>
Run after both tasks complete:

1. `npx tsc --noEmit` — must exit 0
2. `grep -c 'Living Room Cabinet' src/i18n/types.ts` — must output 1 (in LOCATION_KEYS)
3. `grep -c 'livingRoomCabinet' src/i18n/en.ts` — must output 1
4. `grep -c 'livingRoomCabinet' src/i18n/pl.ts` — must output 1
5. `grep -c 'syncStep1' src/i18n/en.ts` — must output 1
6. `grep -c 'selectOrCreate' src/i18n/types.ts` — must output 1
7. `grep -c 'previewHeader' src/i18n/types.ts` — must output 1
8. `grep -c 'csvImportNeedsCatalog' src/i18n/en.ts` — must output 1
</verification>

<success_criteria>
- LOCATION_KEYS has exactly 7 entries covering all predefined DB locations (I18N-04, Gap 1 closed)
- TranslationDict type has all keys needed by Plan 07-06 component wiring tasks
- en.ts and pl.ts are in full sync with the updated TranslationDict type
- `npx tsc --noEmit` passes with zero errors
</success_criteria>

<output>
Create `.planning/phases/07-i18n-polish-language/07-05-SUMMARY.md` when done
</output>
