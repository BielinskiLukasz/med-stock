---
phase: 07-i18n-polish-language
plan: 06
type: execute
wave: 2
depends_on:
  - 07-05
files_modified:
  - src/components/FilterChips.tsx
  - src/components/MedicineCard.tsx
  - src/components/MoveStockSheet.tsx
  - src/components/CatalogAutocomplete.tsx
  - src/components/SyncInstructions.tsx
  - src/components/CSVPreview.tsx
  - src/components/ExportSection.tsx
  - src/components/ImportCSVSection.tsx
autonomous: true
gap_closure: true
requirements:
  - I18N-02
  - I18N-04

estimate:
  tokens: 55000
  raw_tokens: 32000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "FilterChips renders active category filter chip labels via t(CATEGORY_KEYS[v]) — switching to Polish shows 'Ból i gorączka' instead of 'Pain & Fever' (I18N-02, I18N-04, Gap 2)"
    - "FilterChips renders active location filter chip labels via t(LOCATION_KEYS[v]) — switching to Polish shows 'Szafka łazienkowa' instead of 'Bathroom Cabinet' (I18N-02, I18N-04, Gap 3)"
    - "MedicineCard renders the 'at' preposition via t('common.at') — shows 'w' in Polish (I18N-02, Gap 5)"
    - "MoveStockSheet renders all 7 visible strings via t() — box/quantity labels, validation messages, and submit button in-progress state all update on language switch (I18N-02, Gap 4)"
    - "CatalogAutocomplete displays heading and placeholder in the active language (I18N-02, Gap 6)"
    - "SyncInstructions displays all 5 paragraphs in the active language (I18N-02, Gap 7)"
    - "CSVPreview displays preview header, row count label, and all action button labels in the active language (I18N-02, Gap 8)"
    - "ExportSection description and ImportCSVSection idle description display in the active language (I18N-02, low-priority)"
    - "ImportCSVSection guard toast uses t('toasts.csvImportNeedsCatalog') (I18N-02, low-priority)"
  artifacts:
    - "src/components/FilterChips.tsx — imports CATEGORY_KEYS and LOCATION_KEYS; chip label template strings use t() lookups"
    - "src/components/MedicineCard.tsx — 'at' replaced with t('common.at') on line 37"
    - "src/components/MoveStockSheet.tsx — 8 hardcoded strings replaced with t() calls"
    - "src/components/CatalogAutocomplete.tsx — useLang() added; 4 hardcoded strings replaced"
    - "src/components/SyncInstructions.tsx — useLang() added; 5 hardcoded paragraphs replaced"
    - "src/components/CSVPreview.tsx — useLang() added; 6 hardcoded strings replaced"
    - "src/components/ExportSection.tsx — description paragraph replaced with t('data.exportDescription')"
    - "src/components/ImportCSVSection.tsx — idle description and guard toast replaced"
  key_links:
    - "FilterChips must add CATEGORY_KEYS and LOCATION_KEYS to the existing '@/i18n' import line — currently only useLang is imported"
    - "CatalogAutocomplete, SyncInstructions, CSVPreview have no useLang() call at all — must add both the import and the hook call"
    - "MoveStockSheet t() calls use form.* and units.units keys from Plan 07-05 Task 2"
    - "LOCATION_KEYS[v] lookup in FilterChips will now resolve for all 7 predefined locations (Gap 1 fixed in Plan 07-05)"
---

<objective>
Wire all 8 components that the Phase 07 verification identified as having zero or partial i18n coverage. Every hardcoded English string in these components is replaced with a t() call using the translation keys added in Plan 07-05. No new keys are added here — only component wiring.

Purpose: After this plan, the phase goal is achieved: all user-visible text updates when the language is switched between English and Polish.
Output: 8 components fully translated; `npx tsc --noEmit` passes; all 7 BLOCKER gaps from VERIFICATION.md closed.
</objective>

<execution_context>
@C:/my-code/vibe-coding/med-stock/.claude/gsd-core/workflows/execute-plan.md
@C:/my-code/vibe-coding/med-stock/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-i18n-polish-language/07-CONTEXT.md
@.planning/phases/07-i18n-polish-language/07-VERIFICATION.md
@.planning/phases/07-i18n-polish-language/07-05-SUMMARY.md
</context>

<tasks>

<task type="tracer">
  <name>Wire FilterChips CATEGORY_KEYS/LOCATION_KEYS + MedicineCard 'at' + MoveStockSheet 8 strings (Gaps 2–5)</name>

  <files>src/components/FilterChips.tsx, src/components/MedicineCard.tsx, src/components/MoveStockSheet.tsx</files>

  <read_first>
    - src/components/FilterChips.tsx (full file) — confirm current import line (line 3: only useLang); confirm lines 26–38 for the category/location chip label template strings that need replacement
    - src/components/MedicineCard.tsx (full file) — confirm line 37 hardcoded 'at' in template string
    - src/components/MoveStockSheet.tsx (full file) — read lines 85–175 to locate all 8 hardcoded strings; confirm existing import already has LOCATION_KEYS (line 6) and useLang (line 6)
    - src/i18n/index.ts — confirm CATEGORY_KEYS, LOCATION_KEYS are exported from '@/i18n'
    - src/i18n/types.ts (after Plan 07-05) — confirm form.boxesToMove, form.unitsPerBox, form.quantityToMove, form.boxValidationMin, form.boxValidationMax, form.quantityValidationMin, form.quantityValidationMax, form.saving all exist in TranslationDict
  </read_first>

  <action>
  === src/components/FilterChips.tsx (Gaps 2 and 3) ===

  Change the import on line 3 from:
     import { useLang } from '@/i18n'
  to:
     import { useLang, CATEGORY_KEYS, LOCATION_KEYS } from '@/i18n'

  In the selectedCategories.map() call (around line 26–28), the category chip label currently reads:
     label: `${t('filter.category')}: ${v}`,
  Replace with:
     label: `${t('filter.category')}: ${t(CATEGORY_KEYS[v] ?? 'categories.other')}`,

  In the selectedLocations.map() call (around line 30–32), the location chip label currently reads:
     label: `${t('filter.location')}: ${v}`,
  Replace with:
     label: `${t('filter.location')}: ${t(LOCATION_KEYS[v] ?? v)}`,

  The LOCATION_KEYS fallback uses v (the raw location string) directly when LOCATION_KEYS has no entry — this preserves user-created location names exactly as stored, per the D-06/D-07 decision from CONTEXT.md.
  The CATEGORY_KEYS fallback uses 'categories.other' — unknown category values display as the translated "Other".

  === src/components/MedicineCard.tsx (Gap 5) ===

  On line 37, the paragraph text reads:
     {medicine.quantity} {unitDisplay} at {locationDisplay}
  Replace the hardcoded string 'at' with a t() expression. The JSX becomes:
     {medicine.quantity} {unitDisplay} {t('common.at')} {locationDisplay}
  Note: common.at already exists in en.ts ('at') and pl.ts ('w') — no dictionary change needed.

  === src/components/MoveStockSheet.tsx (Gap 4) ===

  The component already imports useLang and has `const { t } = useLang()`. Make 8 targeted replacements:

  1. Box label (line 90): The JSX label element currently contains the text string
     Boxes to move (max {maxBoxes})
  Replace with the template expression:
     {`${t('form.boxesToMove')} (max ${maxBoxes})`}

  2. Per-box info (line 101): The paragraph currently contains:
     = {unitsPerBox} {stock.quantityUnit || 'units'} per box
  Replace with:
     {`= ${unitsPerBox} ${stock.quantityUnit || t('units.units')} ${t('form.unitsPerBox')}`}

  3. Box min validation (line 104): The paragraph text is:
     Must be at least 1 box
  Replace with:
     {t('form.boxValidationMin')}

  4. Box max validation (line 107): The paragraph text is:
     Cannot exceed {maxBoxes} boxes
  Replace with:
     {t('form.boxValidationMax')}
  (The static translation conveys the constraint without needing the dynamic count; the input max attribute already enforces the limit visually.)

  5. Quantity label (line 113): The JSX label element currently contains:
     Quantity to move (max {maxQty})
  Replace with:
     {`${t('form.quantityToMove')} (max ${maxQty})`}

  6. Quantity min validation (line 124): The paragraph text is:
     Quantity must be at least 1
  Replace with:
     {t('form.quantityValidationMin')}

  7. Quantity max validation (line 127): The paragraph text is:
     Cannot exceed {maxQty}
  Replace with:
     {t('form.quantityValidationMax')}

  8. Submit button in-progress state (line 171): The ternary currently reads:
     {isSubmitting ? 'Moving\u2026' : t('form.save')}
  Replace the hardcoded string with:
     {isSubmitting ? t('form.saving') : t('form.save')}
  </action>

  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -5 && grep -c "CATEGORY_KEYS" src/components/FilterChips.tsx && grep -c "LOCATION_KEYS" src/components/FilterChips.tsx && grep -c "form.boxValidationMin" src/components/MoveStockSheet.tsx</automated>
  </verify>

  <acceptance_criteria>
    - src/components/FilterChips.tsx imports CATEGORY_KEYS and LOCATION_KEYS from '@/i18n' (grep -c "CATEGORY_KEYS" src/components/FilterChips.tsx outputs 2 or more — import and usage)
    - src/components/FilterChips.tsx contains LOCATION_KEYS[v] in the location chip label construction (grep -c "LOCATION_KEYS\[v\]" src/components/FilterChips.tsx outputs 1)
    - src/components/MedicineCard.tsx contains t('common.at') (grep -c "common.at" src/components/MedicineCard.tsx outputs 1)
    - src/components/MoveStockSheet.tsx contains t('form.boxValidationMin') (grep -c "form.boxValidationMin" src/components/MoveStockSheet.tsx outputs 1)
    - src/components/MoveStockSheet.tsx contains t('form.saving') (grep -c "form.saving" src/components/MoveStockSheet.tsx outputs 1)
    - src/components/MoveStockSheet.tsx contains t('form.quantityToMove') (grep -c "form.quantityToMove" src/components/MoveStockSheet.tsx outputs 1)
    - npx tsc --noEmit exits 0
  </acceptance_criteria>

  <done>FilterChips renders category and location chip labels via t() lookups. MedicineCard renders 'at' preposition from active language. MoveStockSheet has no hardcoded English labels or validation messages. TypeScript build passes — the 3 highest-visibility gaps are closed.</done>
</task>

<task type="auto">
  <name>Wire CatalogAutocomplete, SyncInstructions, CSVPreview, ExportSection, ImportCSVSection (Gaps 6–9)</name>

  <files>src/components/CatalogAutocomplete.tsx, src/components/SyncInstructions.tsx, src/components/CSVPreview.tsx, src/components/ExportSection.tsx, src/components/ImportCSVSection.tsx</files>

  <read_first>
    - src/components/CatalogAutocomplete.tsx (full file) — confirm no useLang import/call exists; identify lines 32–33 (heading), 37–38 (placeholder), 75–77 (empty state), 88–91 (create button)
    - src/components/SyncInstructions.tsx (full file) — confirm no useLang import/call exists; identify all 5 paragraph content blocks
    - src/components/CSVPreview.tsx (full file) — confirm no useLang import/call; identify lines 32–33 (header/row count), 68–76 (action buttons)
    - src/components/ExportSection.tsx (full file) — confirm useLang already imported; identify the hardcoded description paragraph (~lines 25–28)
    - src/components/ImportCSVSection.tsx (full file) — confirm useLang already imported; identify the idle description paragraph (~lines 111–114) and the guard toast call (~lines 62–66)
    - src/i18n/types.ts (after Plan 07-05) — confirm catalog, csv, data.syncStep*, data.exportDescription, data.importCSVDescription, toasts.csvImportNeedsCatalog all exist
  </read_first>

  <action>
  === src/components/CatalogAutocomplete.tsx (Gap 6) ===

  The component currently has no i18n imports. Add to the import section (after the last existing import):
     import { useLang } from '@/i18n'

  Inside the CatalogAutocomplete function body, as the first line before any state declarations, add:
     const { t } = useLang()

  Make 4 string replacements:

  1. Heading (line 33): The h2 element text is:
     Select or Create Medicine
  Replace with:
     {t('catalog.selectOrCreate')}

  2. Input placeholder (line 38): The Input element placeholder prop is:
     placeholder="Start typing a medicine name\u2026"
  Replace with:
     placeholder={t('catalog.placeholderText')}

  3. Empty state paragraph (around line 76–77): The paragraph text is:
     No medicines yet. Type a name to create one.
  Replace with:
     {t('catalog.noMedicinesYet')}

  4. Create button (around line 89–91): The button text is:
     Create "{searchText}"
  Replace with the template expression:
     {`${t('catalog.createEntry')} "${searchText}"`}

  === src/components/SyncInstructions.tsx (Gap 7) ===

  The component currently exports a function with no imports. Add at the top of the file:
     import { useLang } from '@/i18n'

  Inside SyncInstructions function body, add as first line:
     const { t } = useLang()

  Replace the content of each of the 5 paragraph elements:

  Paragraph 1 (currently contains the &ldquo;Export backup&rdquo; step text):
     Replace full paragraph JSX content with: {t('data.syncStep1')}

  Paragraph 2 (share the folder link):
     Replace full paragraph JSX content with: {t('data.syncStep2')}

  Paragraph 3 (household member imports):
     Replace full paragraph JSX content with: {t('data.syncStep3')}

  Paragraph 4 (push changes):
     Replace full paragraph JSX content with: {t('data.syncStep4')}

  Note paragraph (the one with className="mt-2 text-xs"):
     Replace full paragraph JSX content with: {t('data.syncNote')}

  Keep all className attributes on the <p> elements unchanged.

  === src/components/CSVPreview.tsx (Gap 8) ===

  The component currently has no i18n imports. Add after the existing imports:
     import { useLang } from '@/i18n'

  Inside the CSVPreview function body, add as first line:
     const { t } = useLang()

  Make 6 string replacements:

  1. Preview header paragraph (line 32):
     Current text: Preview (first 5 rows)
     Replace with: {t('csv.previewHeader')}

  2. Row count span (line 33):
     Current text: {totalCount} rows
     Replace with: {`${totalCount} ${t('csv.rowCount')}`}

  3. Commit button loading state:
     Current: 'Importing...'
     Replace with: t('csv.importing')

  4. Commit button non-loading state:
     Current: `Import ${totalCount} medicines`
     Replace with: `${t('csv.importComplete')} (${totalCount})`

  5. Back button text:
     Current: Back
     Replace with: {t('csv.back')}

  6. Cancel button text:
     Current: Cancel
     Replace with: {t('csv.cancel')}

  === src/components/ExportSection.tsx (low-priority) ===

  ExportSection already imports useLang and has `const { t } = useLang()`. Find the hardcoded description paragraph (the <p> element with className="text-sm text-muted-foreground" that starts with "Download your entire inventory"). Replace its text content with:
     {t('data.exportDescription')}

  === src/components/ImportCSVSection.tsx (low-priority) ===

  ImportCSVSection already imports useLang and has `const { t } = useLang()`. Make 2 replacements:

  1. Idle state description paragraph (around line 111–114): The <p> element text starts with "Bulk import medicines from a CSV spreadsheet". Replace its text content with:
     {t('data.importCSVDescription')}

  2. Guard toast (around line 62–66): The toast.error call currently passes a hardcoded multi-sentence English string starting with 'CSV import requires at least one medicine catalog'. Replace the hardcoded string argument with:
     t('toasts.csvImportNeedsCatalog')
  </action>

  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -5 && grep -c "catalog.selectOrCreate" src/components/CatalogAutocomplete.tsx && grep -c "data.syncStep1" src/components/SyncInstructions.tsx && grep -c "csv.previewHeader" src/components/CSVPreview.tsx</automated>
  </verify>

  <acceptance_criteria>
    - src/components/CatalogAutocomplete.tsx imports useLang from '@/i18n' (grep -c "useLang" src/components/CatalogAutocomplete.tsx outputs 2 — import and call)
    - src/components/CatalogAutocomplete.tsx contains t('catalog.selectOrCreate') (grep -c "catalog.selectOrCreate" src/components/CatalogAutocomplete.tsx outputs 1)
    - src/components/CatalogAutocomplete.tsx contains t('catalog.placeholderText') (grep -c "catalog.placeholderText" src/components/CatalogAutocomplete.tsx outputs 1)
    - src/components/SyncInstructions.tsx imports useLang from '@/i18n' (grep -c "useLang" src/components/SyncInstructions.tsx outputs 2)
    - src/components/SyncInstructions.tsx contains t('data.syncStep1') and t('data.syncNote') (grep -c "data.syncStep" src/components/SyncInstructions.tsx outputs 4; grep -c "data.syncNote" src/components/SyncInstructions.tsx outputs 1)
    - src/components/CSVPreview.tsx imports useLang from '@/i18n' (grep -c "useLang" src/components/CSVPreview.tsx outputs 2)
    - src/components/CSVPreview.tsx contains t('csv.previewHeader') (grep -c "csv.previewHeader" src/components/CSVPreview.tsx outputs 1)
    - src/components/CSVPreview.tsx contains t('csv.back') and t('csv.cancel') (grep -c "csv.back" src/components/CSVPreview.tsx outputs 1)
    - src/components/ExportSection.tsx contains t('data.exportDescription') (grep -c "data.exportDescription" src/components/ExportSection.tsx outputs 1)
    - src/components/ImportCSVSection.tsx contains t('toasts.csvImportNeedsCatalog') (grep -c "csvImportNeedsCatalog" src/components/ImportCSVSection.tsx outputs 1)
    - npx tsc --noEmit exits 0
  </acceptance_criteria>

  <done>CatalogAutocomplete, SyncInstructions, CSVPreview, ExportSection, and ImportCSVSection all use t() for every user-visible string. All 7 BLOCKER gaps and 2 low-priority gaps from VERIFICATION.md are resolved. TypeScript build passes. Phase 07 goal achieved: all UI text updates on language switch.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Translation keys → JSX render | Keys are string literals from Plan 07-05 dictionary; no user input enters the t() call |
| CSVPreview totalCount → render | totalCount is derived from Papa Parse result (a number), not a translated string — no XSS vector |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-06-01 | Tampering | i18n translation lookup | low | accept | t() keys are string literals hardcoded in source; cannot be injected at runtime |
| T-07-06-02 | Information Disclosure | ImportCSVSection guard toast | low | accept | Toast message exposes DB state (catalog exists/not) — acceptable UX context for the user operating their own local-only data |
</threat_model>

<verification>
Run after both tasks complete:

1. `npx tsc --noEmit` — must exit 0
2. `grep -c "LOCATION_KEYS\[v\]" src/components/FilterChips.tsx` — must output 1
3. `grep -c "CATEGORY_KEYS\[v\]" src/components/FilterChips.tsx` — must output 1
4. `grep -c "common.at" src/components/MedicineCard.tsx` — must output 1
5. `grep -c "form.saving" src/components/MoveStockSheet.tsx` — must output 1
6. `grep -c "useLang" src/components/CatalogAutocomplete.tsx` — must output 2
7. `grep -c "data.syncStep" src/components/SyncInstructions.tsx` — must output 4
8. `grep -c "csv.previewHeader" src/components/CSVPreview.tsx` — must output 1
9. `grep -c "data.exportDescription" src/components/ExportSection.tsx` — must output 1
10. `grep -c "csvImportNeedsCatalog" src/components/ImportCSVSection.tsx` — must output 1
</verification>

<success_criteria>
- All 7 BLOCKER gaps from VERIFICATION.md are closed
- All 2 low-priority gaps (ExportSection description, ImportCSVSection guard) are closed
- `npx tsc --noEmit` passes with zero errors across the entire project
- No component in src/components/ that the VERIFICATION.md identified as having zero i18n still has hardcoded English strings for user-visible text
- Phase 07 goal achieved: switching language updates every visible string in the app to the selected language
</success_criteria>

<output>
Create `.planning/phases/07-i18n-polish-language/07-06-SUMMARY.md` when done
</output>
