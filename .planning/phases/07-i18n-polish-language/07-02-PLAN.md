---
phase: 07-i18n-polish-language
plan: 02
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - src/lib/expiry.ts
  - src/components/StatusBadge.tsx
  - src/components/FilterChips.tsx
  - src/components/FilterBottomSheet.tsx
  - src/components/MedicineCard.tsx
  - src/components/MedicineCardAggregate.tsx
autonomous: true
requirements:
  - I18N-02
  - I18N-04
  - I18N-05

estimate:
  tokens: 70000
  raw_tokens: 35000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "StatusBadge renders the status label from t() in the active language — switching language changes badge text immediately (I18N-02, D-04)"
    - "STATUS_LABELS is not exported from expiry.ts; no file imports it after this plan — TypeScript build enforces this (D-04)"
    - "FilterBottomSheet status chip labels, category chip labels, and sort option labels all update on language switch (I18N-02, I18N-04)"
    - "FilterChips status chip label uses t() — active filter chips show the translated status name (I18N-02)"
    - "MedicineCard shows location null as t('locationNames.other') in active language, quantityUnit as t(UNIT_KEY), expiryDate as formatDate(date, lang) (I18N-02, I18N-04, I18N-05, D-06)"
    - "MedicineCardAggregate shows catalog.category through CATEGORY_KEYS translation and quantityUnit through UNIT_KEYS (I18N-02, I18N-04)"
    - "User-typed custom location names are shown as stored — no translation attempted (D-06, D-07)"
  artifacts:
    - "src/lib/expiry.ts — STATUS_LABELS removed; calculateStatus() and type exports unchanged"
    - "src/components/StatusBadge.tsx — uses useLang + t() with statusKey Record for rendering"
    - "src/components/FilterChips.tsx — status label chip uses t() via statusKey lookup"
    - "src/components/FilterBottomSheet.tsx — status options, category options, sort options all use t()"
    - "src/components/MedicineCard.tsx — location, quantityUnit, expiryDate display translated"
    - "src/components/MedicineCardAggregate.tsx — category display and quantityUnit translated"
  key_links:
    - "StatusBadge imports useLang from '@/i18n'; no longer imports STATUS_LABELS from '@/lib/expiry'"
    - "FilterBottomSheet must translate both status labels (7 values) and category labels (10 values from CATEGORY_KEYS)"
    - "UNIT_KEYS Record in i18n/index.ts maps canonical unit strings to units.* keys — MedicineCard uses it"
    - "Predefined location translation in MedicineCard: check LOCATION_KEYS[medicine.location] first; fall back to medicine.location for user-created names (D-06)"
---

<objective>
Remove STATUS_LABELS from expiry.ts and update all its consumers to use the t() translation hook. Also translate all display strings in MedicineCard and MedicineCardAggregate (status, location, quantity unit, date). FilterBottomSheet and FilterChips are updated to render all filter option labels in the active language.

Purpose: Status badges and filter chips are the most visible translated elements. This plan covers every component that currently imports STATUS_LABELS.
Output: STATUS_LABELS removed; StatusBadge, FilterChips, FilterBottomSheet, MedicineCard, MedicineCardAggregate fully translated.
</objective>

<execution_context>
@C:/my-code/vibe-coding/med-stock/.claude/gsd-core/workflows/execute-plan.md
@C:/my-code/vibe-coding/med-stock/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-i18n-polish-language/07-CONTEXT.md
@.planning/phases/07-i18n-polish-language/07-PATTERNS.md
@.planning/phases/07-i18n-polish-language/07-UI-SPEC.md
@.planning/phases/07-i18n-polish-language/07-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Remove STATUS_LABELS and translate status/category/filter display (D-04)</name>

  <read_first>
    - src/lib/expiry.ts (full file) — locate the STATUS_LABELS const block to delete (lines 13-21); verify calculateStatus() and all type exports remain untouched
    - src/components/StatusBadge.tsx (full file) — current import of STATUS_LABELS; replace with useLang pattern from PATTERNS.md §StatusBadge
    - src/components/FilterChips.tsx (full file) — current STATUS_LABELS usage in label construction
    - src/components/FilterBottomSheet.tsx (lines 1-100) — current STATUS_LABELS + ALL_CATEGORIES usage, sort option labels
    - src/i18n/index.ts — verify CATEGORY_KEYS and the full useLang export are available from Plan 01
    - .planning/phases/07-i18n-polish-language/07-PATTERNS.md §StatusBadge — exact statusKey Record pattern to use
    - .planning/phases/07-i18n-polish-language/07-CONTEXT.md §D-04, D-05 — what changes and what stays
  </read_first>

  <action>
In src/lib/expiry.ts: delete the STATUS_LABELS const (the exported Record mapping each MedicineStatus to its English display string). Keep all type declarations (AutoStatus, ManualStatus, MedicineStatus) and the calculateStatus() function completely unchanged. The calculateStatus() return values ('Active', 'Opened', 'Expired', 'ExceededOpenPeriod', 'UsedUp', 'Disposed', 'Archived') remain as-is — they are canonical string keys, not display strings (per D-04).

In src/components/StatusBadge.tsx:
  - Remove the import of STATUS_LABELS from '@/lib/expiry'.
  - Add import of useLang from '@/i18n'.
  - Inside the StatusBadge component, call const { t } = useLang().
  - Define a local Record statusKey typed as Record typed MedicineStatus string that maps each status canonical value to its dot-notation translation key: Active maps to 'status.active', Opened to 'status.opened', Expired to 'status.expired', ExceededOpenPeriod to 'status.exceededOpenPeriod', UsedUp to 'status.usedUp', Disposed to 'status.disposed', Archived to 'status.archived'.
  - Render t(statusKey[status]) instead of STATUS_LABELS[status]. All styling (STATUS_STYLES Record and the className) stays unchanged.

In src/components/FilterChips.tsx:
  - Remove the import of STATUS_LABELS from '@/lib/expiry'.
  - Add import of useLang from '@/i18n'.
  - Call const { t } = useLang() inside the component.
  - Wherever STATUS_LABELS is used to build the status chip label (e.g., in the label construction for selectedStatuses), replace it with a local statusKey Record (same shape as in StatusBadge) and call t(statusKey[v as MedicineStatus]).

In src/components/FilterBottomSheet.tsx:
  - Remove the import of STATUS_LABELS from '@/lib/expiry'. Add import of useLang and CATEGORY_KEYS from '@/i18n'.
  - Call const { t } = useLang() inside FilterBottomSheet.
  - Define a local statusKey Record (same pattern as StatusBadge) to map each status value to its translation key; render t(statusKey[status]) instead of STATUS_LABELS[status] in the status option list.
  - Remove the local ALL_CATEGORIES constant. Import CATEGORIES from '@/types/medicine'. In the category filter section, iterate over CATEGORIES and display t(CATEGORY_KEYS[cat] ?? 'categories.other') for each option label.
  - Translate all sort-option labels: render t('filter.byName'), t('filter.byExpiry'), t('filter.byStatus'), t('filter.byCreated') for the four sort fields; t('filter.asc') and t('filter.desc') for direction options. Translate section headings using t('filter.byStatusLabel'), t('filter.byCategoryLabel'), t('filter.byLocationLabel'), t('filter.sortBy'), and the sheet title t('filter.title').
  - Do not change location filter section — location names are user-created strings displayed as-is (D-07); the "Locations" section heading uses t('filter.byLocationLabel').
  </action>

  <verify>
    <automated>npm run build 2>&1 | tail -5</automated>
    <automated>npx vitest run 2>&1 | tail -10</automated>
  </verify>

  <acceptance_criteria>
    - `npm run build` exits 0 with zero TypeScript errors
    - `npx vitest run` passes all existing tests (no regressions)
    - All existing tests in the test suite still pass after STATUS_LABELS removal
    - TypeScript verifies no file imports from '@/lib/expiry' a non-existent export (compile-time enforcement of D-04)
    - FilterBottomSheet no longer contains a local ALL_CATEGORIES array literal — it uses the imported CATEGORIES constant
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Translate MedicineCard and MedicineCardAggregate display strings (D-06, I18N-02, I18N-04, I18N-05)</name>

  <read_first>
    - src/components/MedicineCard.tsx (full file) — current hardcoded strings: 'Other' for null location, quantityUnit fallback 'units', 'Expires:' label, expiryDate direct display
    - src/components/MedicineCardAggregate.tsx (full file) — current hardcoded strings: catalog.category direct display, quantityUnit fallback 'units', 'across N locations' phrase
    - src/i18n/index.ts — CATEGORY_KEYS, LOCATION_KEYS, UNIT_KEYS (to be added if missing: a Record mapping canonical unit strings like 'tablets' to 'units.tablets'), useLang export
    - src/lib/utils.ts — formatDate signature from Plan 01 TDD task
    - .planning/phases/07-i18n-polish-language/07-CONTEXT.md §D-06, D-07, D-08, D-11, D-13 — which values translate, which stay as-is
    - .planning/phases/07-i18n-polish-language/07-UI-SPEC.md §Component Updates §Location Display — null location, predefined vs. user-created
  </read_first>

  <action>
If UNIT_KEYS (a Record from canonical unit string to 'units.*' key, e.g., 'tablets' maps to 'units.tablets') is not present in src/i18n/index.ts, add it alongside CATEGORY_KEYS and LOCATION_KEYS. UNIT_KEYS covers all QUANTITY_UNITS values from '@/types/medicine' plus 'doses' and the fallback 'units'.

In src/components/MedicineCard.tsx:
  - Add imports of useLang and LOCATION_KEYS and UNIT_KEYS from '@/i18n', and formatDate from '@/lib/utils'.
  - Call const { lang, t } = useLang() inside MedicineCard.
  - Replace the null-location fallback 'Other' with: medicine.location !== null ? (LOCATION_KEYS[medicine.location] ? t(LOCATION_KEYS[medicine.location]) : medicine.location) : t('locationNames.other'). This preserves user-created custom location names as-is (D-07) while translating predefined ones (D-06).
  - Replace the quantityUnit fallback 'units' with: t(UNIT_KEYS[medicine.quantityUnit ?? ''] ?? 'units.units').
  - Replace the direct expiryDate display (medicine.expiryDate in JSX) with formatDate(medicine.expiryDate, lang). The 'Expires:' label before it becomes t('dates.expires').

In src/components/MedicineCardAggregate.tsx:
  - Add imports of useLang, CATEGORY_KEYS, UNIT_KEYS from '@/i18n'.
  - Call const { t } = useLang() inside MedicineCardAggregate.
  - Replace catalog.category direct display with: catalog.category ? t(CATEGORY_KEYS[catalog.category] ?? 'categories.other') : null.
  - Replace the quantityUnit fallback 'units' with t(UNIT_KEYS[quantityUnit] ?? 'units.units') where quantityUnit is the existing derived value.
  - Translate the 'across N locations' suffix: replace the hardcoded string template with t('common.across') + ' ' + stockCount + ' ' + t('common.locations'). The condition stockCount greater than 1 stays unchanged.
  </action>

  <verify>
    <automated>npm run build 2>&1 | tail -5</automated>
    <automated>npx vitest run 2>&1 | tail -5</automated>
  </verify>

  <acceptance_criteria>
    - `npm run build` exits 0
    - MedicineCard renders location null as the translated 'Other' string in active language
    - MedicineCard renders predefined location names (Bathroom Cabinet, etc.) translated when Polish is active
    - MedicineCard renders user-created location names unchanged regardless of language (D-07)
    - MedicineCard renders expiryDate through formatDate (DD.MM.YYYY in Polish, YYYY-MM-DD in English)
    - MedicineCardAggregate renders catalog.category through the CATEGORY_KEYS translation lookup
    - `npx vitest run` exits 0 with all prior tests passing
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DB → display | Location names and category values from Dexie pass through translation lookup |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-04 | Tampering | CATEGORY_KEYS lookup | low | accept | Only display is affected; DB category values unchanged. Unknown keys fall back to the raw value (D-05). |
| T-07-05 | Tampering | LOCATION_KEYS lookup | low | mitigate | Unknown location name (user-created) bypasses translation and renders as stored — this is the correct D-07 behavior, not a risk |
</threat_model>

<verification>
1. `npm run build` passes zero errors
2. `npx vitest run` all tests pass
3. Manual: switch to Polish — StatusBadge shows Polish status labels; switch back to English — English labels restored
4. Manual: switch to Polish — category names in FilterBottomSheet show Polish; filter chips for active status show Polish label
5. Manual: MedicineCard with a null-location medicine shows 'Inne' in Polish mode and 'Other' in English mode
6. Manual: MedicineCard with expiry date shows DD.MM.YYYY in Polish mode
</verification>

<success_criteria>
- STATUS_LABELS removed from expiry.ts; TypeScript build enforces no stale imports
- StatusBadge, FilterChips, FilterBottomSheet render in active language (I18N-02)
- Category display translated in FilterBottomSheet and MedicineCardAggregate (I18N-04)
- Location null rendered as translated 'Other'; predefined location names translated (I18N-04, D-06)
- Dates formatted per locale in MedicineCard (I18N-05)
- All existing vitest tests pass
</success_criteria>

<output>
Create `.planning/phases/07-i18n-polish-language/07-02-SUMMARY.md` when done
</output>

## Artifacts This Phase Produces

| Symbol | Type | File |
|--------|------|------|
| UNIT_KEYS | Record const (if added) | src/i18n/index.ts |
| statusKey (internal) | Record | src/components/StatusBadge.tsx |
</output>
