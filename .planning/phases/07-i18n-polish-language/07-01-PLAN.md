---
phase: 07-i18n-polish-language
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/i18n/index.ts
  - src/i18n/en.ts
  - src/i18n/pl.ts
  - src/lib/utils.ts
  - src/lib/utils.test.ts
  - src/App.tsx
  - src/components/BottomTabBar.tsx
autonomous: true
requirements:
  - I18N-01
  - I18N-03

estimate:
  tokens: 80000
  raw_tokens: 40000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Tapping the flag button in BottomTabBar immediately switches all tab labels between English and Polish without a page reload (I18N-01, I18N-03, D-09, D-10)"
    - "Language choice persists in localStorage under key 'medstock-lang' and is restored on the next app load (D-10, I18N-03)"
    - "TypeScript compilation succeeds: both en.ts and pl.ts satisfy TranslationDict — any missing key is a compile error (D-01)"
    - "formatDate('2026-12-31', 'pl') returns '31.12.2026'; formatDate('2026-12-31', 'en') returns '2026-12-31' (D-11, I18N-05)"
    - "formatDate with null or undefined input returns the hardcoded no-expiry label for the given lang (D-11)"
    - "useLang() t() function returns the key name itself when a key is not found — visible regression, never a blank string (UI-SPEC backstop)"
  artifacts:
    - "src/i18n/index.ts — exports Lang, TranslationDict, LanguageProvider, useLang, CATEGORY_KEYS, LOCATION_KEYS, FORM_TYPE_KEYS"
    - "src/i18n/en.ts — exports const en: TranslationDict with all English strings"
    - "src/i18n/pl.ts — exports const pl: TranslationDict with all Polish strings"
    - "src/lib/utils.ts — exports formatDate(dateString, lang)"
    - "src/lib/utils.test.ts — vitest tests for formatDate"
    - "src/App.tsx — LanguageProvider wraps RouterProvider"
    - "src/components/BottomTabBar.tsx — flag emoji toggle + translated tab labels"
  key_links:
    - "LanguageProvider must be the outermost wrapper in App.tsx so every screen gets language context"
    - "useLang() must call useContext(LangContext) — throws if called outside LanguageProvider"
    - "localStorage read on LanguageProvider mount; localStorage write on every setLang call"
    - "TranslationDict type used as annotation for both en and pl objects — TypeScript guarantees structural match"

  planner_assumptions:
    - "ASSUMPTION (I18N-01 boundary): Toggle has exactly two states (en/pl). No boundary condition beyond end-of-state-space applies."
    - "ASSUMPTION (I18N-01 precision): String key lookups involve no numeric precision. Not applicable."
    - "ASSUMPTION (I18N-01 idempotency): Calling setLang('en') when lang is already 'en' is idempotent — React skips re-render on same state value."
    - "ASSUMPTION (I18N-01 concurrency): Language switch is a synchronous React Context setState. No parallel write scenario exists in a single-tab PWA."
    - "ASSUMPTION (I18N-02 adjacency): UI string keys are discrete dictionary lookups with no adjacency/collision condition."
    - "ASSUMPTION (I18N-02 encoding): Polish diacritics (ą,ę,ó,ś,ź,ż,ć,ł,ń) are BMP Unicode code points, stored correctly by all browsers in localStorage (UTF-16). No encoding edge case."
    - "ASSUMPTION (I18N-03 localStorage unavailability): If localStorage.getItem returns null (fresh install or cleared storage), LanguageProvider defaults to 'en'. If localStorage is fully unavailable (privacy browsers), the app still works in English; no crash."
    - "ASSUMPTION (I18N-05 null): formatDate with null/undefined input returns a hardcoded fallback ('No expiry' for EN, 'Bez daty ważności' for PL) defined directly in the function body. The translation dict key dates.noExpiry is also defined for callers that prefer t() — both must match."
    - "ASSUMPTION (pluralization): Polish unit labels in pl.ts use the invariant genitive-plural form (e.g., 'tabletek', 'kapsułek') for all quantities. Full Polish plural grammar (1, 2-4, 5+ forms) is deferred beyond v1.2."
    - "ASSUMPTION (RTL): Polish is LTR. No RTL layout changes needed for Phase 7."

  prohibitions:
    - statement: "Never translate or alter stored values in IndexedDB or any database field — DB values are immutable to this phase; only display-layer rendering changes"
      status: flagged-unverified
      verification: "npm run build passes; no db.medicines.put() or db.medicine_catalog.put() calls added in this phase"
    - statement: "Never translate user-typed content (medicine names, custom location names, custom category text, notes, or any free-text input field value)"
      status: flagged-unverified
      verification: "Code review: useLang/t() is only called with static dict keys, never with dynamic user-input values"
    - statement: "Never trigger a full page reload on language switch — language change must propagate via React Context re-render only, no window.location.reload() or navigate(0)"
      status: flagged-unverified
      verification: "Toggle language in browser DevTools Network tab — no document reload event fires"
---

<objective>
Create the complete i18n foundation: typed translation dictionaries, LanguageProvider React Context, useLang hook with t() lookup, localStorage persistence, and the language toggle in BottomTabBar. Wire LanguageProvider into App.tsx and translate all five navigation tab labels. Also add the formatDate() pure utility function with TDD coverage.

Purpose: Plans 02–04 depend on this infrastructure. The tracer proves end-to-end language switching works before any call sites are migrated.
Output: src/i18n/ module (3 files), updated App.tsx, updated BottomTabBar.tsx, updated utils.ts + utils.test.ts
</objective>

<execution_context>
@C:/my-code/vibe-coding/med-stock/.claude/gsd-core/workflows/execute-plan.md
@C:/my-code/vibe-coding/med-stock/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-i18n-polish-language/07-CONTEXT.md
@.planning/phases/07-i18n-polish-language/07-PATTERNS.md
@.planning/phases/07-i18n-polish-language/07-UI-SPEC.md
</context>

<tasks>

<task type="tracer">
  <name>End-to-end "switch language" — i18n module + App.tsx wire-up + BottomTabBar toggle</name>

  <read_first>
    - src/App.tsx (lines 1-47) — current structure; LanguageProvider wraps RouterProvider here (per D-02)
    - src/components/BottomTabBar.tsx (lines 1-75) — existing NavLink pattern to replicate for the toggle button
    - .planning/phases/07-i18n-polish-language/07-PATTERNS.md §i18n/index.ts and §BottomTabBar — named-export patterns, hook shape, button structure
    - .planning/phases/07-i18n-polish-language/07-CONTEXT.md §Decisions D-01 through D-10 — all locked decisions
    - .planning/phases/07-i18n-polish-language/07-UI-SPEC.md §Language Toggle UI — aria-label, min-h-[44px], emoji rendering
    - src/stores/uiStore.ts (lines 1-30) — Zustand v5 curried pattern for reference; language does NOT go here (D-02)
  </read_first>

  <action>
Create src/i18n/index.ts with:
  - Named exports only (no default export per CLAUDE.md convention).
  - Export type Lang = 'en' | 'pl'.
  - Export type TranslationDict with the following nested structure (all values typed string):
    nav (medicines, dashboard, trash, locations, data),
    status (active, opened, expired, exceededOpenPeriod, usedUp, disposed, archived),
    categories (painFever, antibiotics, allergy, digestive, vitaminsSupplements, skinTopical, eyeEar, coldFlu, heartCirculation, other),
    locationNames (bathroomCabinet, bedroomCabinet, kitchenDrawer, other),
    formTypes (tablet, capsule, syrup, cream, drops, spray, powder, gel, ointment, patch, inhaler, suppository, other),
    units (tablets, capsules, ml, g, pcs, patches, drops, doses, days, weeks, months, box, boxes, units, locations),
    dates (noExpiry, expires, opened),
    medicines (title, searchPlaceholder, addButton, emptyHeading, emptyBody, noResultsHeading, noResultsBody, noStockHeading, noStockBody, loading),
    dashboard (title, expired, expiringSoon, exceededOpenPeriod, total, loading),
    trash (title, emptyBody, view, restore, deletePermanently, deleteConfirmTitle, deleteConfirmBody, cancel, deleted, unknown),
    locations (title),
    data (title, exportSection, importSection, syncSection, exportButton, importJSONButton, importCSVButton),
    history (title, noHistory, added, deleted, restored, updated, fieldsUpdated, fieldChanged),
    form (name, namePlaceholder, nameRequired, category, noCategory, formType, noType, quantity, quantityUnit, expiryDate, openedDate, pao, paoUnit, location, noLocation, addLocation, newLocationPlaceholder, addNewLocation, notes, notesPlaceholder, packCount, save, saveChanges, cancel, addMedicine, editMedicine, editStock, moveStock, targetLocation, openBox, moveToTrash, delete, deleteConfirmTitle, deleteConfirmBody),
    filter (title, sortBy, byName, byExpiry, byStatus, byCreated, asc, desc, byStatusLabel, byCategoryLabel, byLocationLabel, clearAll, status, category, location),
    toasts (saved, updated, deleted, restored, moved, exported, imported, importPartial, saveFailed, deleteFailed, moveFailed, locationFailed, exportFailed, importFailed, boxOpened, catalogDeleted),
    common (loading, unknown, acrossLocations, across, locations).
  - Export three lookup Records that map canonical code values to TranslationDict dot-notation keys:
    CATEGORY_KEYS: Record string string — maps each value from CATEGORIES array (e.g., 'Pain and Fever') to the categories namespace key (e.g., 'categories.painFever').
    LOCATION_KEYS: Record string string — maps each predefined DB location name ('Bathroom Cabinet', 'Bedroom Cabinet', 'Kitchen Drawer') to the locationNames key.
    FORM_TYPE_KEYS: Record string string — maps each MedicineForm const value ('Tablet', 'Capsule', ...) to the formTypes key.
  - Export a React Context LangContext (do not export it as default).
  - Export LanguageProvider: a React component that reads localStorage.getItem('medstock-lang') on mount (per D-10), defaults to 'en' if null or invalid, stores lang in useState, and writes localStorage.setItem('medstock-lang', newLang) in the setLang setter before calling the state setter. Provide LangContext value as { lang, setLang, t }.
  - The t(key: string) function inside LanguageProvider: split key on '.', walk two levels into the active dict (en or pl), return the string value, or return the key itself if the path is missing (never return undefined or throw).
  - Export useLang(): calls useContext(LangContext); throws Error('useLang must be used inside LanguageProvider') if context is null.
  - TypeScript strict mode is enforced: no any anywhere; the t() function uses explicit indexed access with proper unknown casting and typeof guards.

Create src/i18n/en.ts with:
  - Named export: const en: TranslationDict = { ... }
  - Fill all keys with English strings based on existing string literals in the codebase (read the source files to collect them).
  - Key reference: nav.medicines = 'Medicines', nav.dashboard = 'Dashboard', nav.trash = 'Trash', nav.locations = 'Locations', nav.data = 'Data'; status.active = 'Active', status.exceededOpenPeriod = 'Exceeded Open Period', status.usedUp = 'Used Up'; categories.painFever = 'Pain & Fever', categories.vitaminsSupplements = 'Vitamins & Supplements', categories.skinTopical = 'Skin & Topical', categories.eyeEar = 'Eye & Ear', categories.coldFlu = 'Cold & Flu', categories.heartCirculation = 'Heart & Circulation'; locationNames.bathroomCabinet = 'Bathroom Cabinet', locationNames.bedroomCabinet = 'Bedroom Cabinet', locationNames.kitchenDrawer = 'Kitchen Drawer', locationNames.other = 'Other'; formTypes.tablet = 'Tablet' ... (each value from MedicineForm const); units.tablets = 'tablets', units.box = 'box', units.boxes = 'boxes', units.units = 'units', units.locations = 'locations', units.days = 'days', units.weeks = 'weeks', units.months = 'months'; dates.noExpiry = 'No expiry', dates.expires = 'Expires', dates.opened = 'Opened'; toasts.saved = 'Saved', toasts.saveFailed = 'Failed to save. Please try again.', etc. Fill all other namespaces by reading the actual string literals in each source file.

Create src/i18n/pl.ts with:
  - Named export: const pl: TranslationDict = { ... }
  - Fill all keys with accurate Polish translations:
    nav: Leki, Panel, Kosz, Lokalizacje, Dane.
    status: Aktywny, Otwarty, Przeterminowany, Przekroczono okres otwarcia, Zużyty, Zutylizowany, Zarchiwizowany.
    categories: Ból i gorączka, Antybiotyki, Alergia, Trawienie, Witaminy i suplementy, Skóra i miejscowe, Oczy i uszy, Przeziębienie i grypa, Serce i krążenie, Inne.
    locationNames: Szafka łazienkowa, Szafka przy łóżku, Szuflada kuchenna, Inne.
    formTypes: Tabletka, Kapsułka, Syrop, Krem, Krople, Spray, Proszek, Żel, Maść, Plaster, Inhalator, Czopek, Inne.
    units: tabletek, kapsułek, ml, g, szt., plastrów, kropli, dawek, dni, tygodni, miesięcy, opakowanie, opakowań, szt., lokalizacjach.
    dates.noExpiry = 'Bez daty ważności', dates.expires = 'Ważny do', dates.opened = 'Otwarto'.
    medicines.emptyHeading = 'Brak leków', medicines.emptyBody = 'Dodaj pierwszy lek', medicines.noResultsHeading = 'Brak wyników', medicines.noResultsBody = 'Spróbuj innej nazwy'.
    toasts.saveFailed = 'Błąd zapisu. Spróbuj ponownie.', toasts.saved = 'Zapisano', etc. Translate all other keys accurately.

Update src/App.tsx:
  - Add import of LanguageProvider from '@/i18n'.
  - Wrap the RouterProvider element with LanguageProvider as the single outermost wrapper (per D-02): return LanguageProvider wrapping RouterProvider.
  - Do not move the router or the storage persist call — both stay at module scope.

Update src/components/BottomTabBar.tsx (per D-09, UI-SPEC §Language Toggle UI):
  - Add import of useLang from '@/i18n'.
  - Call const { lang, setLang, t } = useLang() inside BottomTabBar.
  - Replace the five hardcoded tab label strings ('Medicines', 'Dashboard', 'Trash', 'Locations', 'Data') with t('nav.medicines'), t('nav.dashboard'), t('nav.trash'), t('nav.locations'), t('nav.data').
  - After the last NavLink (Data), add a button element with: onClick toggling lang between 'en' and 'pl' via setLang, aria-label set to 'Switch to Polish' when lang is 'en' and 'Switch to English' when lang is 'pl', className replicating the NavLink flex layout (flex flex-1 flex-col items-center justify-center gap-1 text-xs font-medium transition-colors text-gray-500 hover:text-gray-700) plus min-h-[44px] min-w-[44px] for touch target (per UI-SPEC).
  - Inside the button, render a span with the flag emoji: the UK flag emoji when lang is 'en', the Polish flag emoji when lang is 'pl'.
  - Do not add a text label below the emoji.
  </action>

  <verify>
    <automated>npm run build 2>&1 | tail -5</automated>
    <automated>node -e "
const { en } = require('./src/i18n/en.ts');
console.log(en?.nav?.medicines === 'Medicines' ? 'OK' : 'FAIL');
" 2>&1 || npx vitest run src/lib/utils.test.ts 2>&1 | tail -10</automated>
    <human-check>Start dev server (npm run dev), open app, tap the flag emoji in BottomTabBar — all five tab labels switch between English and Polish immediately. Reload the page — the chosen language is restored. No full reload occurs on toggle (Network tab shows no document request).</human-check>
  </verify>

  <acceptance_criteria>
    - TypeScript build passes with zero errors: `npm run build` exits 0
    - src/i18n/index.ts exports Lang, TranslationDict, LanguageProvider, useLang, CATEGORY_KEYS, LOCATION_KEYS, FORM_TYPE_KEYS as named exports
    - src/i18n/en.ts and src/i18n/pl.ts are both annotated as TranslationDict and TypeScript accepts them without error
    - Both language dicts have identical key shapes — any structural mismatch is a type error (D-01 compile-time coverage)
    - App.tsx wraps RouterProvider in LanguageProvider (D-02)
    - BottomTabBar renders five translated tab labels and one flag-emoji toggle button (D-09)
    - The toggle button has min-h-[44px] min-w-[44px] and aria-label reflecting the other language (UI-SPEC)
  </acceptance_criteria>

  <reversibility rating="costly">
    Switching away from custom hook/context to react-i18next later requires replacing every t('key') call site across the entire app. Documented in D-01.
  </reversibility>
</task>

<task type="tdd">
  <name>formatDate() — pure locale-aware date formatter with vitest coverage</name>

  <read_first>
    - src/lib/utils.ts (lines 1-6) — existing cn() export; formatDate is added here as a named export (D-11)
    - .planning/phases/07-i18n-polish-language/07-CONTEXT.md §D-11, D-12, D-13 — formatting rules, null handling, scope
    - .planning/phases/07-i18n-polish-language/07-PATTERNS.md §utils.ts — exact function signature and null-handling pattern
    - .planning/phases/07-i18n-polish-language/07-UI-SPEC.md §Date Formatting — EN returns stored string, PL returns DD.MM.YYYY
  </read_first>

  <test_first>
    Write src/lib/utils.test.ts first. The file must have these failing tests before touching utils.ts:
    - formatDate('2026-12-31', 'en') === '2026-12-31'
    - formatDate('2026-01-05', 'pl') === '05.01.2026'
    - formatDate('2026-12-31', 'pl') === '31.12.2026'
    - formatDate(null, 'en') returns a non-empty string (the English no-expiry label)
    - formatDate(null, 'pl') returns a non-empty string (the Polish no-expiry label)
    - formatDate(undefined, 'en') returns a non-empty string
    - formatDate('2026-01-01', 'pl') === '01.01.2026' (leading-zero preservation test)
    Run `npx vitest run src/lib/utils.test.ts` and confirm all tests fail before implementing.
  </test_first>

  <action>
After all tests are written and confirmed failing, add to src/lib/utils.ts a named export function formatDate that accepts dateString typed as string or null or undefined, and lang typed as Lang imported from '@/i18n'. For null or undefined input: return the hardcoded English label 'No expiry' when lang is 'en', and 'Bez daty ważności' when lang is 'pl'. For non-null input when lang is 'en': return dateString unchanged. For non-null input when lang is 'pl': split dateString on '-', destructure into year, month, day, and return the template literal day dot month dot year. Do not use Intl.DateTimeFormat (D-11). Do not import the Lang context — import only the Lang type from '@/i18n'. The function is a pure utility with no side effects.
  </action>

  <verify>
    <automated>npx vitest run src/lib/utils.test.ts</automated>
  </verify>

  <acceptance_criteria>
    Tests pass: `npx vitest run src/lib/utils.test.ts` exits 0 with all test cases passing.
    formatDate is a named export in src/lib/utils.ts — no default export added.
    No Intl.DateTimeFormat or Date constructor used in the implementation (D-11).
    TypeScript strict mode satisfied: no any in the function signature or body.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| localStorage → LanguageProvider | Untrusted string read from browser storage on mount |
| User locale/language | Language preference is client-side only; no server trust boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-01 | Tampering | localStorage 'medstock-lang' read | low | mitigate | Validate the read value against the Lang union ('en' or 'pl') before applying; fall back to 'en' if value is anything else — prevents injection of arbitrary locale strings |
| T-07-02 | Information Disclosure | Translation dict | low | accept | Dicts are static JS bundles — no user data included; no secrets |
| T-07-03 | Spoofing | Language toggle | low | accept | Toggle is UI-only; no authentication gate needed for a household app |
</threat_model>

<verification>
1. `npm run build` passes with zero TypeScript errors
2. `npx vitest run src/lib/utils.test.ts` passes all formatDate tests
3. Manual: toggle language in browser — tab labels switch instantly, no reload
4. Manual: reload — language is restored from localStorage
5. Manual: DevTools Application > Local Storage > medstock-lang key exists with 'en' or 'pl'
</verification>

<success_criteria>
- i18n module created and TypeScript-valid with complete en + pl dicts (D-01, D-03)
- LanguageProvider wired in App.tsx (D-02)
- Language toggle visible in BottomTabBar with correct flag emoji and aria-label (D-09)
- Tab labels render in active language (I18N-01)
- Language persists in localStorage and is restored on reload (I18N-03, D-10)
- formatDate() passes all TDD tests (I18N-05, D-11)
- Zero TypeScript errors
</success_criteria>

<output>
Create `.planning/phases/07-i18n-polish-language/07-01-SUMMARY.md` when done
</output>

## Artifacts This Phase Produces

| Symbol | Type | File |
|--------|------|------|
| `Lang` | type | src/i18n/index.ts |
| `TranslationDict` | type | src/i18n/index.ts |
| `LanguageProvider` | React component | src/i18n/index.ts |
| `useLang()` | hook | src/i18n/index.ts |
| `CATEGORY_KEYS` | Record const | src/i18n/index.ts |
| `LOCATION_KEYS` | Record const | src/i18n/index.ts |
| `FORM_TYPE_KEYS` | Record const | src/i18n/index.ts |
| `en` | TranslationDict const | src/i18n/en.ts |
| `pl` | TranslationDict const | src/i18n/pl.ts |
| `formatDate` | function | src/lib/utils.ts |
</output>
