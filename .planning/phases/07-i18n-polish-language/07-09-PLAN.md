---
phase: 07-i18n-polish-language
plan: 09
type: execute
wave: 1
depends_on: []
files_modified:
  - src/i18n/types.ts
  - src/i18n/en.ts
  - src/i18n/pl.ts
  - src/routes/medicines/new.tsx
  - src/components/CatalogEditSheet.tsx
  - src/components/StockEditSheet.tsx
  - src/components/MedicineForm.tsx
  - src/components/StockFields.tsx
  - src/components/BottomTabBar.tsx
  - src/components/SearchBar.tsx
  - src/components/FilterChips.tsx
  - "src/routes/medicines/[id].tsx"
autonomous: true
gap_closure: true
requirements:
  - I18N-02

estimate:
  tokens: 42000
  raw_tokens: 30000
  tasks: 4
  confidence: low

must_haves:
  truths:
    - "Add Medicine wizard Step 2 ('create catalog') submit button shows translated idle text (t('form.nextAddStock')) and translated submitting text (t('form.creating')) in both EN and PL (CR-01)"
    - "Add Medicine wizard Step 3 ('add stock') submit button shows translated idle text (t('form.addStock')) and translated submitting text (t('form.savingGeneric')) in both EN and PL, distinct from MoveStockSheet's 'Moving…' meaning of form.saving (CR-01)"
    - "CatalogEditSheet, StockEditSheet, and MedicineForm submit buttons show t('form.savingGeneric') during submission instead of a hardcoded English literal, with a consistent ellipsis character across all three (WR-01)"
    - "MedicineForm and StockFields custom-quantity-unit SelectItem option, its follow-up Input placeholder, and the PAO-value/quantity/packCount numeric example placeholders all render via t() in the active language (WR-02)"
    - "All 8 aria-label attributes across BottomTabBar, SearchBar, FilterChips, medicines/new.tsx, and medicines/[id].tsx render via t() in the active language for screen readers (WR-03)"
    - { statement: "A t() call with an unrecognized/missing dictionary key falls back to displaying the raw dot-path key string rather than throwing or rendering blank — pre-existing LanguageProvider behavior (WR-07 in 07-REVIEW.md), unchanged by this plan's additive key changes", verification: backstop }
  artifacts:
    - src/i18n/types.ts
    - src/i18n/en.ts
    - src/i18n/pl.ts
    - src/routes/medicines/new.tsx
    - src/components/CatalogEditSheet.tsx
    - src/components/StockEditSheet.tsx
    - src/components/MedicineForm.tsx
    - src/components/StockFields.tsx
    - src/components/BottomTabBar.tsx
    - src/components/SearchBar.tsx
    - src/components/FilterChips.tsx
    - "src/routes/medicines/[id].tsx"
  key_links:
    - "medicines/new.tsx Step 2/Step 3 submit buttons -> TranslationDict.form.creating / form.nextAddStock / form.savingGeneric / form.addStock"
    - "CatalogEditSheet.tsx, StockEditSheet.tsx, MedicineForm.tsx submit buttons -> TranslationDict.form.savingGeneric (NOT form.saving, which remains MoveStockSheet-only and means 'Moving…')"
    - "MedicineForm.tsx + StockFields.tsx custom-unit SelectItem/Input/placeholders -> TranslationDict.form.customUnitOption / form.customUnitPlaceholder / form.paoValuePlaceholder / form.quantityPlaceholder / form.packCountPlaceholder"
    - "BottomTabBar.tsx, SearchBar.tsx, FilterChips.tsx, medicines/new.tsx, medicines/[id].tsx aria-label attributes -> new TranslationDict.aria namespace (backToSearch, switchToPolish, switchToEnglish, clearSearch, removeFilter, editStockEntry, deleteCatalog, editCatalog)"
  prohibitions:
    - requirement_id: I18N-02
      category: values
      status: resolved
      verification: judgment
      resolution: null
      reason: null
      statement: "MUST NOT route user-typed content (a custom quantity-unit string the user types into the '__CUSTOM__' Input, a user-created location name, a medicine name) through t() while wiring the new static UI labels — only the static labels/placeholders this plan introduces (e.g. the 'Other...' SelectItem, the 'Custom unit' placeholder) are translated; the user's own typed value must keep rendering verbatim in both languages, per 07-CONTEXT.md D-07."
    - requirement_id: I18N-02
      category: safety
      status: resolved
      verification: judgment
      resolution: null
      reason: null
      statement: "MUST NOT change any IndexedDB schema, stored medicine/catalog/location/history value, or canonical English constant (CATEGORIES, QUANTITY_UNITS, MedicineForm values, LOCATION_KEYS source names) while closing these gaps — this is a display-string-only wiring change; db.medicine_catalog, db.medicines, db.locations, and db.history remain byte-for-byte unaffected."

---

<objective>
Close the 4 documented translation-coverage gaps from 07-VERIFICATION.md's re-verification (triggered by 07-REVIEW.md's code review) so Phase 7's "full string coverage" goal (I18N-02) is actually achieved:

1. **CR-01 (Critical, phase-blocking):** the Add Medicine wizard's primary submit buttons (`src/routes/medicines/new.tsx` lines 163 & 207) hardcode English text instead of calling `t()`.
2. **WR-01 (Warning):** 3 edit sheets (`CatalogEditSheet.tsx`, `StockEditSheet.tsx`, `MedicineForm.tsx`) hardcode `'Saving…'`/`'Saving...'` instead of a translated key.
3. **WR-02 (Warning):** custom quantity-unit option/placeholder and numeric example placeholders in `MedicineForm.tsx` and `StockFields.tsx` are hardcoded English.
4. **WR-03 (Warning):** 8 `aria-label` attributes across 6 files are hardcoded English, never localized.

Purpose: These are the last 4 unclosed items blocking Phase 7 from passing verification. All 4 extend the SAME `t()`/`useLang()`/`TranslationDict` pattern established in Plans 07-01 through 07-08 (per 07-CONTEXT.md D-01/D-03) — no new architecture, no new dependency, purely additive dictionary keys plus literal-to-`t()`-call swaps.

Output: `src/i18n/types.ts`, `src/i18n/en.ts`, `src/i18n/pl.ts` extended with 9 new `form.*` keys and a new `aria.*` namespace (8 keys); all 4 gap call sites wired through `t()`.

**Out of scope (explicitly, per 07-REVIEW.md but not promoted to a 07-VERIFICATION.md gap):** WR-04 (localStorage try/catch), WR-05 (fragile `t(LOCATION_KEYS[name] ?? name)` fallback pattern), WR-06 (duplicated `statusKey` lookup), WR-07 (untyped `t()` key parameter), WR-08 (orphaned `form.namePlaceholder` key / hardcoded `CatalogFields.tsx`/`MedicineForm.tsx` name placeholder), IN-01/IN-02/IN-03 (dead keys, missing sort-by-status UI, stale comment). Do not touch these in this plan.
</objective>

<execution_context>
@C:/my-code/vibe-coding/med-stock/.claude/gsd-core/workflows/execute-plan.md
@C:/my-code/vibe-coding/med-stock/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/07-i18n-polish-language/07-CONTEXT.md
@.planning/phases/07-i18n-polish-language/07-VERIFICATION.md
@.planning/phases/07-i18n-polish-language/07-REVIEW.md
@src/i18n/types.ts
@src/i18n/en.ts
@src/i18n/pl.ts
</context>

<flagged_assumptions>
Per the spec-less probe fallback protocol, the deterministic edge-probe ran against I18N-01..05
and returned 12 applicable items, all `unresolved` except one. These are carried forward as
explicit flagged assumptions (never silently dropped) because a defensible resolution does not
exist for THIS plan's actual scope (swapping hardcoded string literals for `t()` calls against a
fixed, enumerated list of 4 known gaps — not the general I18N-01..05 feature set, which Plans
07-01 through 07-08 already built):

- I18N-01 boundary/precision/idempotency/concurrency (4 items) — not applicable; this plan adds no
  numeric/threshold/concurrent logic, only string-literal replacement.
- I18N-02 adjacency/empty/encoding/ordering (4 items) — not applicable except the "empty" item,
  which resolved as the backstop truth above (missing-key fallback behavior, pre-existing per WR-07).
- I18N-03 unclassified (1 item) — not applicable; this plan does not touch localStorage persistence.
- I18N-04 empty/encoding (2 items) — not applicable; this plan adds no new location/category data.
- I18N-05 unclassified (1 item) — not applicable; this plan does not touch date formatting.

From 07-UI-SPEC.md's `## UI Considerations`, two items are `unresolved` there too (RTL layout,
Polish pluralization rules). Both are pre-existing, phase-wide assumptions inherited unchanged
from Plans 07-01–07-08 — this plan introduces no new pluralized or RTL-sensitive strings (all 17
new keys are static singular labels/placeholders), so neither assumption is newly triggered here,
but both remain open for the phase as a whole and are surfaced rather than silently dropped.
</flagged_assumptions>

<threat_model>
## Trust Boundaries

None newly introduced. This plan touches only client-side display-string rendering (translation
dictionary lookups and JSX text/`aria-label` content) inside an already-trusted, already-rendered
component tree. No new external input, no new network call, no new persistence path, no
authentication surface. ASVS Level 1 applies; nothing in this plan approaches "high severity."

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-09-01 | Information Disclosure | `src/i18n/LanguageProvider.tsx` `t()` key-fallback (WR-07 in 07-REVIEW.md, out of scope) | low | accept | Unchanged pre-existing behavior: an unrecognized dictionary key renders its raw dot-path string instead of throwing or blanking. This plan only ADDS correctly-typed keys (TypeScript's `TranslationDict` structural type forces both `en.ts` and `pl.ts` to define every new key, so `npm run build` fails closed on a missing key). Fixing the underlying untyped-`t()`-parameter gap is WR-07, explicitly deferred. |
| T-07-09-02 | Tampering | New `TranslationDict` keys (`types.ts`/`en.ts`/`pl.ts`) | low | accept | Additive, compile-time-checked string constants only — no user input, no DB write, no new package. `tsc -b` (via `npm run build`) enforces both language dicts satisfy the extended type before the build can pass. |

**Package legitimacy gate:** Not applicable — this plan adds zero npm/pip/cargo packages.
</threat_model>

<tasks>

<task type="tracer">
  <name>Task 1: Add all 17 new translation keys and close CR-01 (Add Medicine wizard buttons)</name>
  <reversibility rating="reversible">Additive TranslationDict keys plus a two-line ternary swap behind the existing t()/useLang() interface — a single git revert undoes it with no data or contract impact.</reversibility>
  <read_first>src/i18n/types.ts, src/i18n/en.ts, src/i18n/pl.ts, src/routes/medicines/new.tsx, .planning/phases/07-i18n-polish-language/07-CONTEXT.md (D-01 custom t()/useLang() pattern, D-03 flat namespace-by-screen key structure), .planning/phases/07-i18n-polish-language/07-VERIFICATION.md (gap #1, CR-01)</read_first>
  <files>src/i18n/types.ts, src/i18n/en.ts, src/i18n/pl.ts, src/routes/medicines/new.tsx</files>
  <action>
    This task is the tracer: it establishes the dictionary-extension pattern end-to-end (type ->
    both language values -> one real wired call site -> build-verified) before the remaining
    3 tasks reuse the already-added keys.

    Step 1 — extend `TranslationDict` in `src/i18n/types.ts`:
    - Inside the existing `form: { ... }` object type, add 9 new fields (after the existing
      `saving: string` field, same indentation level): `creating: string`, `nextAddStock: string`,
      `addStock: string`, `savingGeneric: string`, `customUnitOption: string`,
      `customUnitPlaceholder: string`, `paoValuePlaceholder: string`, `quantityPlaceholder: string`,
      `packCountPlaceholder: string`.
    - Add a new top-level sibling object type (alongside `form`, `filter`, `toasts`, etc.) named
      `aria`, with 8 string fields: `backToSearch`, `switchToPolish`, `switchToEnglish`,
      `clearSearch`, `removeFilter`, `editStockEntry`, `deleteCatalog`, `editCatalog`.

    Step 2 — add matching English values to `src/i18n/en.ts`, inside `form: { ... }` (9 new keys)
    and as a new top-level `aria: { ... }` block (8 keys):
    `creating` = the current submitting-state text for the catalog-create step; `nextAddStock` =
    the current idle-state text for that same button; `addStock` = the current idle-state text for
    the stock-form submit button; `savingGeneric` = a generic saving indicator distinct from the
    existing `saving` field (which means "Moving…" and is MoveStockSheet-only) — use the ellipsis
    character `…` (not three plain dots) for consistency with the rest of the dictionary;
    `customUnitOption` = the current custom-unit select option label (three literal trailing dots,
    matching the existing UI text style, not the ellipsis character); `customUnitPlaceholder` = the
    current custom-unit input placeholder; `paoValuePlaceholder` / `quantityPlaceholder` /
    `packCountPlaceholder` = the three existing numeric example placeholders used in
    MedicineForm.tsx / StockFields.tsx (paoValue, quantity, packCount respectively — reuse the
    "e.g. N" style already used by `namePlaceholder` in this same file). For `aria`, use the
    current English text already present as literal `aria-label` values in `new.tsx`,
    `BottomTabBar.tsx`, `SearchBar.tsx`, `FilterChips.tsx`, and `[id].tsx` (see Task 4 for exact
    source locations) — `removeFilter` should be just the leading verb (no dynamic label baked in,
    since FilterChips concatenates it with the chip's own text at render time, matching the
    existing `${t('filter.category')}: ${...}` concatenation pattern already used in that file).

    Step 3 — add the Polish equivalents to `src/i18n/pl.ts` in the same two locations, following
    the register and style already used throughout that file (e.g. `np.` prefix for "e.g." per the
    existing `namePlaceholder` entry; `…` ellipsis character for progressive/in-flight states,
    matching `saving: 'Przenoszę…'`).

    Step 4 — wire CR-01 in `src/routes/medicines/new.tsx`: on the Step-2 ("create-catalog") submit
    button, change the ternary that currently renders the hardcoded submitting/idle button text to
    call `t('form.creating')` for the submitting branch and `t('form.nextAddStock')` for the idle
    branch. On the Step-3 ("stock-form") submit button, change the ternary that currently renders
    the hardcoded submitting/idle button text to call `t('form.savingGeneric')` for the submitting
    branch (NOT `t('form.saving')`, which is reserved for MoveStockSheet's "Moving…" meaning) and
    `t('form.addStock')` for the idle branch. `useLang()` and `t` are already imported and
    destructured in this file (line 20) — no new import needed.
  </action>
  <verify>
    <automated>npm run build && [ "$(grep -c "t('form.creating')" "src/routes/medicines/new.tsx")" -ge 1 ] && [ "$(grep -c "t('form.nextAddStock')" "src/routes/medicines/new.tsx")" -ge 1 ] && [ "$(grep -c "t('form.savingGeneric')" "src/routes/medicines/new.tsx")" -ge 1 ] && [ "$(grep -c "t('form.addStock')" "src/routes/medicines/new.tsx")" -ge 1 ]</automated>
    <fails_when>npm run build exits non-zero (a missing key in en.ts or pl.ts breaks TranslationDict structural typing, or a JSX/TS syntax error was introduced), OR any grep -c count is 0 (an expected t() call site in medicines/new.tsx is missing)</fails_when>
  </verify>
  <acceptance_criteria>
    - src/i18n/types.ts's `form` type has creating, nextAddStock, addStock, savingGeneric, customUnitOption, customUnitPlaceholder, paoValuePlaceholder, quantityPlaceholder, packCountPlaceholder fields
    - src/i18n/types.ts has a new top-level `aria` type with backToSearch, switchToPolish, switchToEnglish, clearSearch, removeFilter, editStockEntry, deleteCatalog, editCatalog fields
    - src/i18n/en.ts and src/i18n/pl.ts both populate all 17 new keys (9 form + 8 aria) with non-empty string values
    - src/routes/medicines/new.tsx's Step-2 submit button calls t('form.creating') / t('form.nextAddStock') instead of a hardcoded string literal
    - src/routes/medicines/new.tsx's Step-3 submit button calls t('form.savingGeneric') / t('form.addStock') instead of a hardcoded string literal
    - npm run build exits 0
  </acceptance_criteria>
  <done>CR-01 is closed: both Add Medicine wizard submit buttons render translated text in both languages, and all 17 new dictionary keys needed by Tasks 2-4 exist and type-check.</done>
</task>

<task type="auto">
  <name>Task 2: Close WR-01 (edit sheet "Saving…" hardcoded in 3 files)</name>
  <reversibility rating="reversible">Swaps a hardcoded literal for an existing translated key (added in Task 1) at 3 call sites — no interface change, trivially revertable.</reversibility>
  <read_first>src/components/CatalogEditSheet.tsx, src/components/StockEditSheet.tsx, src/components/MedicineForm.tsx, src/i18n/en.ts (confirm form.savingGeneric exists after Task 1), .planning/phases/07-i18n-polish-language/07-VERIFICATION.md (gap #3, WR-01)</read_first>
  <files>src/components/CatalogEditSheet.tsx, src/components/StockEditSheet.tsx, src/components/MedicineForm.tsx</files>
  <action>
    In `src/components/CatalogEditSheet.tsx`, on the submit `Button`'s conditional text
    (`form.formState.isSubmitting ? ... : t('form.saveChanges')`), change the submitting-state
    branch to call `t('form.savingGeneric')` instead of the current hardcoded literal.

    In `src/components/StockEditSheet.tsx`, on the submit `Button`'s conditional text
    (`form.formState.isSubmitting ? ... : t('form.save')`), change the submitting-state branch to
    call `t('form.savingGeneric')` instead of the current hardcoded literal.

    In `src/components/MedicineForm.tsx`, on the final submit `Button`'s conditional text
    (`form.formState.isSubmitting ? ... : resolvedLabel`), change the submitting-state branch to
    call `t('form.savingGeneric')` instead of the current hardcoded literal (this call site also
    has an inconsistent plain-dots ellipsis today — the new call resolves that inconsistency too,
    since form.savingGeneric uses the standard ellipsis character established in Task 1).

    `useLang()` and `t` are already imported and destructured in all three files — no new import
    needed. Do not touch `form.saveChanges`, `form.save`, or `resolvedLabel` (the idle-state
    labels) — only the submitting-state branch changes in each file.
  </action>
  <verify>
    <automated>npm run build && [ "$(grep -c "t('form.savingGeneric')" "src/components/CatalogEditSheet.tsx")" -ge 1 ] && [ "$(grep -c "t('form.savingGeneric')" "src/components/StockEditSheet.tsx")" -ge 1 ] && [ "$(grep -c "t('form.savingGeneric')" "src/components/MedicineForm.tsx")" -ge 1 ]</automated>
    <fails_when>npm run build exits non-zero, or any of the three grep -c counts is 0 (form.savingGeneric not wired at that submit button)</fails_when>
  </verify>
  <acceptance_criteria>
    - src/components/CatalogEditSheet.tsx's submit button submitting-state branch calls t('form.savingGeneric')
    - src/components/StockEditSheet.tsx's submit button submitting-state branch calls t('form.savingGeneric')
    - src/components/MedicineForm.tsx's submit button submitting-state branch calls t('form.savingGeneric'), resolving both the missing translation and the inconsistent ellipsis style in one change
    - npm run build exits 0
  </acceptance_criteria>
  <done>WR-01 is closed: all 3 edit-flow submit buttons show a translated, consistently-styled generic saving indicator, distinct from MoveStockSheet's "Moving…".</done>
</task>

<task type="auto">
  <name>Task 3: Close WR-02 (custom-unit option/placeholder + numeric example placeholders)</name>
  <reversibility rating="reversible">Swaps 8 hardcoded literal/placeholder values for existing translated keys (added in Task 1) across 2 files — no interface change, trivially revertable.</reversibility>
  <read_first>src/components/MedicineForm.tsx, src/components/StockFields.tsx, src/i18n/en.ts (confirm the 5 form.* placeholder/option keys exist after Task 1), .planning/phases/07-i18n-polish-language/07-VERIFICATION.md (gap #4, WR-02)</read_first>
  <files>src/components/MedicineForm.tsx, src/components/StockFields.tsx</files>
  <action>
    In both `src/components/MedicineForm.tsx` and `src/components/StockFields.tsx`:
    - Change the `quantityUnit` field's `SelectItem value="__CUSTOM__"` option text to call
      `t('form.customUnitOption')` instead of the current hardcoded literal.
    - Change the follow-up custom-unit `Input`'s `placeholder` prop to call
      `t('form.customUnitPlaceholder')` instead of the current hardcoded literal.
    - Change the `paoValue` number `Input`'s `placeholder` prop to call
      `t('form.paoValuePlaceholder')` instead of the current hardcoded literal.
    - Change the `quantity` number `Input`'s `placeholder` prop to call
      `t('form.quantityPlaceholder')` instead of the current hardcoded literal.

    In `src/components/StockFields.tsx` only (MedicineForm.tsx has no `packCount` field): change
    the `packCount` number `Input`'s `placeholder` prop to call `t('form.packCountPlaceholder')`
    instead of the current hardcoded literal.

    Do not wrap the user's own typed custom-unit value (the `field.value`/`onChange` bound to the
    custom-unit `Input`) in `t()` — only the static option label and the static placeholder text
    are translated keys; the value the user types remains free text in both languages, per
    07-CONTEXT.md D-07 (see this plan's `must_haves.prohibitions`). `useLang()` and `t` are already
    imported and destructured in both files — no new import needed.
  </action>
  <verify>
    <automated>npm run build && for f in "src/components/MedicineForm.tsx" "src/components/StockFields.tsx"; do [ "$(grep -c "t('form.customUnitOption')" "$f")" -ge 1 ] && [ "$(grep -c "t('form.customUnitPlaceholder')" "$f")" -ge 1 ] && [ "$(grep -c "t('form.paoValuePlaceholder')" "$f")" -ge 1 ] && [ "$(grep -c "t('form.quantityPlaceholder')" "$f")" -ge 1 ] || exit 1; done && [ "$(grep -c "t('form.packCountPlaceholder')" "src/components/StockFields.tsx")" -ge 1 ]</automated>
    <fails_when>npm run build exits non-zero, or the for-loop exits 1 (customUnitOption/customUnitPlaceholder/paoValuePlaceholder/quantityPlaceholder missing in either file), or the final packCountPlaceholder grep -c count in StockFields.tsx is 0</fails_when>
  </verify>
  <acceptance_criteria>
    - src/components/MedicineForm.tsx's custom-unit SelectItem calls t('form.customUnitOption') and its follow-up Input placeholder calls t('form.customUnitPlaceholder')
    - src/components/StockFields.tsx's custom-unit SelectItem calls t('form.customUnitOption') and its follow-up Input placeholder calls t('form.customUnitPlaceholder')
    - Both files' paoValue Input placeholder calls t('form.paoValuePlaceholder') and quantity Input placeholder calls t('form.quantityPlaceholder')
    - src/components/StockFields.tsx's packCount Input placeholder calls t('form.packCountPlaceholder')
    - Neither file passes the user-typed custom-unit field value through t()
    - npm run build exits 0
  </acceptance_criteria>
  <done>WR-02 is closed: the custom quantity-unit option/placeholder and all three numeric example placeholders render translated text in MedicineForm.tsx and StockFields.tsx, without translating any user-typed value.</done>
</task>

<task type="auto">
  <name>Task 4: Close WR-03 (8 hardcoded aria-label attributes across 6 files)</name>
  <reversibility rating="reversible">Swaps 8 hardcoded aria-label literals for existing translated keys (added in Task 1) across 5 files — no interface change, trivially revertable.</reversibility>
  <read_first>src/components/BottomTabBar.tsx, src/components/SearchBar.tsx, src/components/FilterChips.tsx, src/routes/medicines/new.tsx, src/routes/medicines/[id].tsx, src/i18n/en.ts (confirm the aria.* keys exist after Task 1), .planning/phases/07-i18n-polish-language/07-VERIFICATION.md (gap #2, WR-03)</read_first>
  <files>src/components/BottomTabBar.tsx, src/components/SearchBar.tsx, src/components/FilterChips.tsx, src/routes/medicines/new.tsx, src/routes/medicines/[id].tsx</files>
  <action>
    In `src/components/BottomTabBar.tsx`, change the language-toggle button's `aria-label`
    ternary (currently two hardcoded literals keyed on `lang === 'en'`) to call
    `t('aria.switchToPolish')` for the `lang === 'en'` branch and `t('aria.switchToEnglish')` for
    the other branch.

    In `src/components/SearchBar.tsx`, change the clear-search `Button`'s `aria-label` prop to
    call `t('aria.clearSearch')` instead of the current hardcoded literal.

    In `src/components/FilterChips.tsx`, change the chip-removal `Button`'s `aria-label` template
    literal (currently a hardcoded leading word concatenated with the chip's own label) to
    concatenate `t('aria.removeFilter')` with the chip's label instead of the hardcoded leading
    word, keeping the same template-literal structure and the trailing `${chip.label}` unchanged.

    In `src/routes/medicines/new.tsx`, change BOTH "back to search" icon buttons' `aria-label`
    props (Step 2's back button and Step 3's back button) to call `t('aria.backToSearch')` instead
    of the current hardcoded literal — same key at both call sites.

    In `src/routes/medicines/[id].tsx`, change the "edit stock entry" pencil-icon button's
    `aria-label` to call `t('aria.editStockEntry')`, the "delete catalog" trash-icon button's
    `aria-label` to call `t('aria.deleteCatalog')`, and the "edit catalog" pencil-icon button's
    `aria-label` to call `t('aria.editCatalog')` — three separate call sites, three separate keys.

    `useLang()` and `t`/`lang` are already imported and destructured in all 5 files — no new
    import needed.
  </action>
  <verify>
    <automated>npm run build && [ "$(grep -c "t('aria.switchToPolish')" "src/components/BottomTabBar.tsx")" -ge 1 ] && [ "$(grep -c "t('aria.switchToEnglish')" "src/components/BottomTabBar.tsx")" -ge 1 ] && [ "$(grep -c "t('aria.clearSearch')" "src/components/SearchBar.tsx")" -ge 1 ] && [ "$(grep -c "t('aria.removeFilter')" "src/components/FilterChips.tsx")" -ge 1 ] && [ "$(grep -c "t('aria.backToSearch')" "src/routes/medicines/new.tsx")" -ge 2 ] && [ "$(grep -c "t('aria.editStockEntry')" "src/routes/medicines/[id].tsx")" -ge 1 ] && [ "$(grep -c "t('aria.deleteCatalog')" "src/routes/medicines/[id].tsx")" -ge 1 ] && [ "$(grep -c "t('aria.editCatalog')" "src/routes/medicines/[id].tsx")" -ge 1 ]</automated>
    <fails_when>npm run build exits non-zero, or any grep -c count is below its required minimum (a required aria.* call site is missing — note medicines/new.tsx requires 2 occurrences of aria.backToSearch, one per wizard step)</fails_when>
  </verify>
  <acceptance_criteria>
    - src/components/BottomTabBar.tsx's language-toggle aria-label ternary calls t('aria.switchToPolish') / t('aria.switchToEnglish')
    - src/components/SearchBar.tsx's clear-search button aria-label calls t('aria.clearSearch')
    - src/components/FilterChips.tsx's chip-removal button aria-label concatenates t('aria.removeFilter') with the chip's own label
    - src/routes/medicines/new.tsx has 2 call sites of t('aria.backToSearch') (Step 2 and Step 3 back buttons)
    - src/routes/medicines/[id].tsx's edit-stock, delete-catalog, and edit-catalog buttons call t('aria.editStockEntry') / t('aria.deleteCatalog') / t('aria.editCatalog') respectively
    - npm run build exits 0
  </acceptance_criteria>
  <done>WR-03 is closed: all 8 aria-label attributes across the 5 named files render translated text, so screen readers deliver the active language to Polish users.</done>
</task>

</tasks>

<verification>
Run in order after all 4 tasks complete:

1. `npm run build` — exits 0 (TypeScript strict-mode compile across all 12 modified files, TranslationDict structural typing enforced for both en.ts and pl.ts).
2. `npx vitest run` — exits 0, no regression in existing suites (no test in this repo targets the i18n dictionary directly; this is a regression guard for historyOps/expiry/utils tests that live alongside the touched components).
3. `npm run lint` — exits 0 (oxlint across all modified files).
4. Manual spot check (optional, not required for phase completion since 07-UAT.md's prior UAT already exercises these screens): toggle to Polish, visit the Add Medicine wizard through both steps, an edit sheet, and the medicine detail screen; confirm no English text remains at any of the 4 gap locations.

All 4 must pass before this plan is considered complete. `npm run build` failing on a missing dictionary key is the primary safety net — TranslationDict's structural type makes an incomplete translation a compile error, not a silent runtime fallback.
</verification>

<success_criteria>
- CR-01, WR-01, WR-02, and WR-03 (all 4 gaps in 07-VERIFICATION.md's `gaps:` list) are closed.
- `src/i18n/types.ts`, `en.ts`, and `pl.ts` all compile together under `npm run build` with the 17 new keys populated in both languages.
- No hardcoded English string literal remains at any of the 12 call sites named in 07-VERIFICATION.md's Key Artifacts Audit table for these 4 gaps.
- `npx vitest run` and `npm run lint` both exit 0 with no new failures.
- Requirement I18N-02 ("All UI strings display in the active language") is unblocked.
</success_criteria>

## Artifacts This Phase Produces

New `TranslationDict` keys added by this plan (all added in Task 1; Tasks 2-4 only wire existing call sites to them). Any later drift check or SUMMARY diff should treat these as new-this-phase symbols, not pre-existing ones:

**`form` namespace (9 new keys):**
- `form.creating` — Add Medicine wizard Step 2 submitting-state text (CR-01)
- `form.nextAddStock` — Add Medicine wizard Step 2 idle-state text (CR-01)
- `form.addStock` — Add Medicine wizard Step 3 idle-state text (CR-01)
- `form.savingGeneric` — generic "Saving…" indicator, used by Step 3 of the wizard (CR-01) AND CatalogEditSheet/StockEditSheet/MedicineForm (WR-01) — distinct from the pre-existing `form.saving` ("Moving…", MoveStockSheet-only)
- `form.customUnitOption` — custom quantity-unit SelectItem label (WR-02)
- `form.customUnitPlaceholder` — custom quantity-unit Input placeholder (WR-02)
- `form.paoValuePlaceholder` — PAO-value numeric example placeholder (WR-02)
- `form.quantityPlaceholder` — quantity numeric example placeholder (WR-02)
- `form.packCountPlaceholder` — pack-count numeric example placeholder, StockFields.tsx only (WR-02)

**New top-level `aria` namespace (8 new keys, WR-03):**
- `aria.backToSearch` — medicines/new.tsx back-button (2 call sites)
- `aria.switchToPolish` — BottomTabBar language toggle, EN-active state
- `aria.switchToEnglish` — BottomTabBar language toggle, PL-active state
- `aria.clearSearch` — SearchBar clear button
- `aria.removeFilter` — FilterChips chip-removal button (concatenated with the chip's own label)
- `aria.editStockEntry` — medicines/[id].tsx stock-entry edit button
- `aria.deleteCatalog` — medicines/[id].tsx catalog delete button
- `aria.editCatalog` — medicines/[id].tsx catalog edit button

<output>
Create `.planning/phases/07-i18n-polish-language/07-09-SUMMARY.md` when done
</output>
