---
phase: 07-i18n-polish-language
plan: 04
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - src/routes/medicines/index.tsx
  - src/routes/medicines/new.tsx
  - src/routes/medicines/[id].tsx
  - src/routes/medicines/[id].edit.tsx
  - src/routes/dashboard/index.tsx
  - src/routes/trash/index.tsx
  - src/routes/locations/index.tsx
  - src/routes/data/index.tsx
  - src/components/SearchBar.tsx
  - src/components/ExportSection.tsx
  - src/components/ImportCSVSection.tsx
  - src/components/ImportJSONSection.tsx
  - src/components/SyncInstructions.tsx
autonomous: true
requirements:
  - I18N-02
  - I18N-05

estimate:
  tokens: 90000
  raw_tokens: 45000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Medicines screen title, empty state messages, search placeholder, and Add button label render in active language (I18N-02)"
    - "Medicine detail view labels (Expires, Opened, box/boxes, at location) render in active language; dates use formatDate (I18N-02, I18N-05, D-11)"
    - "Toast messages from all medicine routes (saved, updated, deleted, moved, catalog deleted, box opened) render in active language (I18N-02)"
    - "Dashboard screen title renders in active language (I18N-02)"
    - "Trash screen title, button labels (View, Restore, Delete Permanently), confirmation dialog, and deleted date render in active language (I18N-02, I18N-05)"
    - "Locations screen title renders in active language (I18N-02)"
    - "Data screen section headings (Export backup, Import backup, Sync with household) and SyncInstructions prose render in active language (I18N-02)"
    - "SearchBar default placeholder renders in active language when no override placeholder is passed (I18N-02)"
    - "Predefined location names in LocationsScreen display list are translated; user-created names are shown as-is (I18N-04, D-06, D-07)"
  artifacts:
    - "src/routes/medicines/index.tsx — screen title, empty states, search placeholder, add button translated"
    - "src/routes/medicines/new.tsx — screen title and toast messages translated"
    - "src/routes/medicines/[id].tsx — section labels, date display, location/unit display, toast messages translated"
    - "src/routes/medicines/[id].edit.tsx — screen title and toast messages translated"
    - "src/routes/dashboard/index.tsx — screen title and DashboardCard labels translated"
    - "src/routes/trash/index.tsx — title, buttons, confirmation dialog, date display translated"
    - "src/routes/locations/index.tsx — title, predefined location display, button labels translated"
    - "src/routes/data/index.tsx — section headings translated"
    - "src/components/SearchBar.tsx — default placeholder translatable via prop; caller passes t('medicines.searchPlaceholder')"
    - "src/components/ExportSection.tsx — button label and toast messages translated"
    - "src/components/ImportCSVSection.tsx — labels and toast messages translated"
    - "src/components/ImportJSONSection.tsx — labels and toast messages translated"
    - "src/components/SyncInstructions.tsx — all prose sentences translated"
  key_links:
    - "medicines/index.tsx passes t('medicines.searchPlaceholder') to SearchBar placeholder prop — SearchBar itself needs no i18n import"
    - "medicines/[id].tsx uses formatDate for stock.expiryDate and stock.openedDate display (D-11, D-13)"
    - "trash/index.tsx: deletedAt is an ISO timestamp — use toLocaleDateString with locale from lang, not formatDate (deletedAt is not a YYYY-MM-DD stored field)"
    - "dashboard/index.tsx: DashboardCard label prop receives translated string — DashboardCard itself needs no i18n import"
    - "locations/index.tsx: predefined location display uses LOCATION_KEYS lookup from '@/i18n'; isDefault flag identifies predefined locations"
---

<objective>
Translate all route-level screens and the remaining utility components: screen titles, empty states, loading text, button labels, confirmation dialog strings, toast messages, date display, location display in detail view, and the full SyncInstructions prose. This is the final wave-2 plan — after this plan, all user-visible strings in the app are fully translated.

Purpose: Routes are the top-level composition layer. After Plans 02 and 03 translate the shared components, this plan finishes the job at the screen level.
Output: All 5 route screens + 5 utility components fully translated; no hardcoded English strings remain in the UI layer.
</objective>

<execution_context>
@C:/my-code/vibe-coding/med-stock/.claude/gsd-core/workflows/execute-plan.md
@C:/my-code/vibe-coding/med-stock/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-i18n-polish-language/07-CONTEXT.md
@.planning/phases/07-i18n-polish-language/07-PATTERNS.md
@.planning/phases/07-i18n-polish-language/07-UI-SPEC.md
@.planning/phases/07-i18n-polish-language/07-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Translate medicine routes — index, new, [id], [id].edit (I18N-02, I18N-05)</name>

  <read_first>
    - src/routes/medicines/index.tsx (full file) — screen title 'Medicines', empty state messages, SearchBar placeholder, Add button, loading text; all hardcoded strings to translate
    - src/routes/medicines/new.tsx (full file) — page heading/title, toast.error messages
    - src/routes/medicines/[id].tsx (lines 270-400) — stock card display: 'Expires:', 'Opened:', 'box'/'boxes', location null fallback 'Other', quantityUnit 'units' fallback, 'Open box' button, toast messages, screen section headings
    - src/routes/medicines/[id].tsx (lines 1-150) — component logic and imports section; understand existing structure before modifying
    - src/routes/medicines/[id].edit.tsx (full file) — page heading, toast.error/toast.success messages
    - src/lib/utils.ts — formatDate signature (Plan 01)
    - src/i18n/index.ts — useLang, LOCATION_KEYS, UNIT_KEYS, t()
    - .planning/phases/07-i18n-polish-language/07-CONTEXT.md §D-06, D-07, D-11, D-13 — location translation rules, date formatting scope
    - .planning/phases/07-i18n-polish-language/07-UI-SPEC.md §Date Formatting, §Component Updates
  </read_first>

  <action>
In src/routes/medicines/index.tsx:
  - Add import of useLang from '@/i18n'.
  - Call const { t } = useLang() inside MedicineList.
  - SearchBar placeholder prop: pass t('medicines.searchPlaceholder') — SearchBar already accepts placeholder as a prop.
  - Screen title 'Medicines' (the h1 element) → t('medicines.title').
  - Add button label 'Add' → t('medicines.addButton').
  - Loading state text 'Loading...' → t('common.loading').
  - Empty state: 'No medicines yet. Tap + to add your first medicine.' → t('medicines.emptyBody') (the heading can be a separate key or combined).
  - 'Add Medicine' button in empty state → t('medicines.addButton').
  - No-search-results state: 'No medicines match your search.' → t('medicines.noResultsBody').
  - No-filter-results state: 'No medicines match your filters.' → use a filter-specific key or t('medicines.noResultsBody').

In src/routes/medicines/new.tsx:
  - Add import of useLang from '@/i18n'.
  - Call const { t } = useLang() inside MedicineNew.
  - Page heading if present → t('medicines.addButton') or equivalent 'Add Medicine' key.
  - All toast.error messages → t('toasts.saveFailed') or specific keys from the toasts namespace.
  - toast.success if present → t('toasts.saved').

In src/routes/medicines/[id].tsx:
  - Add import of useLang and LOCATION_KEYS and UNIT_KEYS from '@/i18n'; add import of formatDate from '@/lib/utils'.
  - Call const { lang, t } = useLang() inside MedicineDetail (the main route component).
  - Stock card display (lines ~286-302 area): replace the stock info template string with translated parts:
    'box' → t('units.box'); 'boxes' → t('units.boxes').
    The quantityUnit fallback 'units' → t('units.units').
    The ternary: stock.packCount > 0 ? template with t('units.box')/t('units.boxes') : plain quantity + t(UNIT_KEYS[stock.quantityUnit ?? ''] ?? 'units.units').
    Location display 'Other' → LOCATION_KEYS[stock.location] ? t(LOCATION_KEYS[stock.location]) : (stock.location ?? t('locationNames.other')).
    'Expires:' label → t('dates.expires').
    stock.expiryDate direct display → formatDate(stock.expiryDate, lang).
    'Opened:' label → t('dates.opened').
    stock.openedDate direct display → formatDate(stock.openedDate, lang).
  - 'Open box' button label → t('form.openBox').
  - Toast messages: toast.success('Catalog updated') → t('toasts.updated'); toast.success('Stock updated') → t('toasts.updated'); toast.success('Stock moved') → t('toasts.moved'); toast.error('Failed to delete...') → t('toasts.deleteFailed'); toast.success('Catalog deleted') → t('toasts.catalogDeleted'); toast.success('Box opened') → t('toasts.boxOpened').
  - Section headings if any (Stock Entries title, etc.) → translate via t().
  - 'Move Stock' button, 'Edit' button aria-labels, 'Delete' confirmation dialog strings → translate via t('form.*') and t('trash.*') keys as appropriate.
  - AlertDialog confirmation text (delete confirmation) → t('form.deleteConfirmTitle'), t('form.deleteConfirmBody').

In src/routes/medicines/[id].edit.tsx:
  - Add import of useLang from '@/i18n'.
  - Call const { t } = useLang() inside MedicineEdit.
  - Page heading/title → translate via t().
  - toast.success('Medicine updated') → t('toasts.updated').
  - toast.error('Failed to save. Please try again.') → t('toasts.saveFailed').
  </action>

  <verify>
    <automated>npm run build 2>&1 | tail -5</automated>
    <automated>npx vitest run src/routes/medicines/ 2>&1 | tail -10</automated>
  </verify>

  <acceptance_criteria>
    - `npm run build` exits 0 with zero TypeScript errors
    - `npx vitest run` exits 0 — all medicine-route tests pass (including [id].test.tsx)
    - Stock cards in [id].tsx render stock.expiryDate through formatDate in active language (DD.MM.YYYY for PL)
    - Stock location null renders as t('locationNames.other') in active language
    - All toast messages in medicine routes use t() keys — no hardcoded English toast strings remain
    - Empty state messages on index.tsx render in active language
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Translate remaining routes and utility components — dashboard, trash, locations, data, SearchBar, Export/Import sections, SyncInstructions (I18N-02)</name>

  <read_first>
    - src/routes/dashboard/index.tsx (full file) — screen title, DashboardCard label prop strings ('Expired', 'Expiring soon', 'Exceeded open period', 'Total medicines'), loading text; DashboardCard receives label as prop so translation happens here not in DashboardCard
    - src/routes/trash/index.tsx (full file) — screen title 'Trash', 'Trash is empty.', 'Deleted', button labels 'View', 'Restore', 'Delete Permanently', AlertDialog confirmation dialog text, date display (deletedAt is ISO timestamp — use toLocaleDateString with locale based on lang, not formatDate)
    - src/routes/locations/index.tsx (lines 1-100) — screen title 'Locations', add-location input placeholder, 'Add', 'Cancel', 'Save', 'Delete', error messages, AlertDialog text; predefined location names in list display — use LOCATION_KEYS for isDefault locations
    - src/routes/data/index.tsx (full file) — section headings 'Export backup', 'Import backup', 'Sync with household', screen title 'Data'
    - src/components/SearchBar.tsx (full file) — default placeholder prop value to translate; caller already passes placeholder from index.tsx
    - src/components/ExportSection.tsx (full file) — button label, toast messages
    - src/components/ImportCSVSection.tsx (full file) — all toast messages, labels
    - src/components/ImportJSONSection.tsx (full file) — all toast messages, labels
    - src/components/SyncInstructions.tsx (full file) — four numbered step paragraphs and the note paragraph to translate
    - src/i18n/index.ts — useLang, LOCATION_KEYS from Plan 01
    - src/lib/utils.ts — formatDate (note: NOT used for deletedAt which is an ISO timestamp)
    - .planning/phases/07-i18n-polish-language/07-CONTEXT.md §D-06, D-07 — predefined vs. user-created location rules
  </read_first>

  <action>
In src/routes/dashboard/index.tsx:
  - Add import of useLang from '@/i18n'.
  - Call const { t } = useLang() inside DashboardScreen.
  - 'Loading...' text → t('common.loading').
  - DashboardCard label props: 'Expired' → t('dashboard.expired'), 'Expiring soon' → t('dashboard.expiringSoon'), 'Exceeded open period' → t('dashboard.exceededOpenPeriod'), 'Total medicines' → t('dashboard.total').

In src/routes/trash/index.tsx:
  - Add import of useLang from '@/i18n'.
  - Call const { lang, t } = useLang() inside TrashScreen.
  - Screen title 'Trash' → t('trash.title').
  - 'Trash is empty.' → t('trash.emptyBody').
  - 'Deleted' label before the date → t('trash.deleted').
  - The deletedAt display: use medicine.deletedAt ? new Date(medicine.deletedAt).toLocaleDateString(lang === 'pl' ? 'pl-PL' : 'en-GB') : '' — this is a full ISO timestamp, not a YYYY-MM-DD field, so Intl.toLocaleDateString is appropriate here (distinct from D-11 which applies to stored YYYY-MM-DD strings).
  - Button labels: 'View' → t('trash.view'), 'Restore' → t('trash.restore'), 'Delete Permanently' → t('trash.deletePermanently').
  - AlertDialogTitle → t('trash.deleteConfirmTitle'); AlertDialogDescription → t('trash.deleteConfirmBody').
  - AlertDialogCancel → t('trash.cancel'); AlertDialogAction → t('trash.deletePermanently').
  - Quantity unit fallback 'units' → t('units.units'); location null fallback 'Other' → t('locationNames.other'); 'boxes' → t('units.boxes').

In src/routes/locations/index.tsx:
  - Add import of useLang and LOCATION_KEYS from '@/i18n'.
  - Call const { t } = useLang() inside LocationsScreen.
  - Screen title 'Locations' → t('locations.title').
  - For each location displayed: if the location is predefined (isDefault === true) and LOCATION_KEYS[location.name] exists, display t(LOCATION_KEYS[location.name]); otherwise display location.name as-is (D-06, D-07).
  - Add-location input placeholder, 'Add' and 'Cancel' and 'Save' and 'Delete' button labels → translate via t('form.*') keys.
  - Error messages in handleAdd and handleRename → translate via t('toasts.*') keys.
  - AlertDialog confirmation dialog for delete → translate title and description via t() keys.

In src/routes/data/index.tsx:
  - Add import of useLang from '@/i18n'.
  - Call const { t } = useLang() inside DataScreen.
  - Section headings: 'Export backup' → t('data.exportSection'), 'Import backup' → t('data.importSection'), 'Sync with household' → t('data.syncSection').

In src/components/SearchBar.tsx:
  - The default prop value for placeholder ('Search medicines by name…') should be updated to accept a translated value. The caller (medicines/index.tsx, already updated in Task 1) now passes t('medicines.searchPlaceholder') as the placeholder prop. The SearchBar component itself does not need a useLang import — keep it as a pure display component. Update only the JSDoc or comment on the default value if needed; no functional change required if the caller already provides the translated prop.

In src/components/ExportSection.tsx:
  - Add import of useLang from '@/i18n'.
  - Call const { t } = useLang() inside ExportSection.
  - Export button label → t('data.exportButton').
  - toast.success for export → t('toasts.exported') (the success message currently includes the date; the Polish version can similarly include the date).
  - toast.error for export failure → t('toasts.exportFailed').

In src/components/ImportCSVSection.tsx:
  - Add import of useLang from '@/i18n'.
  - Call const { t } = useLang() inside ImportCSVSection.
  - All toast.error messages → translate via t('toasts.importFailed') or specific import keys.
  - toast.warning → translate via a warnings key or t('toasts.importPartial').
  - toast.success → t('toasts.imported').
  - Any visible button labels or section labels → translate via t() keys.

In src/components/ImportJSONSection.tsx:
  - Add import of useLang from '@/i18n'.
  - Call const { t } = useLang() inside ImportJSONSection.
  - All toast.error messages → t('toasts.importFailed') or specific import error keys.
  - toast.success with medicine count → translate the message template (keep the count interpolated inline).
  - Any visible button labels or section labels → translate via t() keys.

In src/components/SyncInstructions.tsx:
  - Add import of useLang from '@/i18n'.
  - Call const { t } = useLang() inside SyncInstructions.
  - Replace each paragraph's content with a t() key. Define one key per paragraph in the data namespace or a sync sub-namespace: e.g., t('data.syncStep1'), t('data.syncStep2'), t('data.syncStep3'), t('data.syncStep4'), t('data.syncNote').
  - The Polish translations for each step must be accurate paraphrases of the English sync instructions.
  - Ensure the keys exist in both en.ts and pl.ts (update en.ts and pl.ts if keys are missing — the TranslationDict type will enforce this at compile time).
  </action>

  <verify>
    <automated>npm run build 2>&1 | tail -5</automated>
    <automated>npx vitest run 2>&1 | tail -5</automated>
  </verify>

  <acceptance_criteria>
    - `npm run build` exits 0 with zero TypeScript errors
    - `npx vitest run` exits 0 — all existing tests pass
    - Dashboard DashboardCard labels render in active language (Polish: Przeterminowane, Wygasające wkrótce, etc.)
    - Trash screen title, empty state, button labels, and dialog text render in active language
    - Trash deletedAt date renders in locale-appropriate format (toLocaleDateString with 'pl-PL' when Polish)
    - Locations screen displays predefined location names in active language; user-created locations unchanged (D-06, D-07)
    - Data screen section headings render in active language
    - SyncInstructions prose renders in active language
    - Import/export toast messages render in active language
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| File system / JSON import → ImportJSONSection | Untrusted file content; translation does not affect validation logic |
| CSV file → ImportCSVSection | Untrusted file content; translation does not affect Papa Parse or Zod validation |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-08 | Tampering | ImportJSONSection translation | low | accept | Translation affects only UI strings in toast messages and labels, not the Zod validation logic or the import payload |
| T-07-09 | Tampering | ImportCSVSection translation | low | accept | Same as above — translation is display-layer only; Papa Parse and column mapping logic are unchanged |
| T-07-10 | Information Disclosure | SyncInstructions locale | low | accept | Sync instructions are public how-to text with no secrets; Polish translation is a display change only |
</threat_model>

<verification>
1. `npm run build` exits 0
2. `npx vitest run` all tests pass
3. Manual — Medicines screen in Polish: title 'Leki', search placeholder in Polish, 'Dodaj' button
4. Manual — Medicine detail in Polish: 'Ważny do' label, DD.MM.YYYY date, 'Inne' for null location
5. Manual — Trash screen in Polish: 'Kosz' title, 'Przywróć'/'Usuń na stałe' buttons
6. Manual — Dashboard in Polish: DashboardCard labels show Polish text
7. Manual — Data screen in Polish: section headings and SyncInstructions in Polish
8. Manual — Locations screen in Polish: 'Szafka łazienkowa' etc. for predefined locations; user-created names unchanged
</verification>

<success_criteria>
- All route screens and utility components display strings in the active language (I18N-02)
- Dates in trash (deletedAt) and medicine detail use locale-appropriate formatting (I18N-05)
- Predefined location names translate; user-created location names stay as-is (I18N-04, D-06, D-07)
- Zero TypeScript errors; all vitest tests pass
- After Plans 01–04 are all complete: no hardcoded English UI strings remain outside of t() calls
</success_criteria>

<output>
Create `.planning/phases/07-i18n-polish-language/07-04-SUMMARY.md` when done
</output>

## Artifacts This Phase Produces

No new exported symbols — all changes are internal call-site translations in existing route and utility components.
