---
phase: 02-search-dashboard-audit
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - src/stores/uiStore.ts
  - src/components/ui/sheet.tsx
  - src/components/SearchBar.tsx
  - src/components/FilterBottomSheet.tsx
  - src/components/FilterChips.tsx
  - src/routes/medicines/index.tsx
  - src/components/BottomTabBar.tsx
  - src/App.tsx
autonomous: true
requirements:
  - SRCH-01
  - SRCH-02
  - SRCH-03
  - SRCH-04
  - SRCH-05
  - SRCH-06

must_haves:
  truths:
    - "SearchBar is permanently visible at the top of the Medicines screen (D-20) — not behind a toggle or tab"
    - "Typing in SearchBar instantly filters medicines by substring match on name, case-insensitive (D-21)"
    - "Search results use existing MedicineCard component — name, location, expiry, status badge in one glance (D-22, SRCH-02)"
    - "Search and filters apply simultaneously with AND logic (D-23): search 'ibu' AND location 'Bathroom' returns intersection"
    - "Empty search bar shows full list with any active filters applied; no 'start typing' placeholder state (D-24)"
    - "Filter icon in Medicines screen header opens a bottom sheet (shadcn Sheet side='bottom') with Category/Location/Status checkboxes and Sort options (D-29)"
    - "Filter badge on icon shows count of active filters (D-29): e.g., '2' when 2 filters are active"
    - "Dismissible FilterChips appear above medicine list for each active filter (D-30)"
    - "Filter/sort state lives in Zustand uiStore — persists across navigation (D-31)"
    - "Status filter supports all 6 statuses; auto-calculated statuses (Active/Opened/Expired) are post-filtered via useMemo+calculateStatus (D-32)"
    - "OR logic within filter dimension, AND across dimensions (D-33)"
    - "Bottom tab bar has 4 tabs: Medicines | Dashboard | Trash | Locations (D-39)"
    - "App.tsx registers /dashboard and /trash routes as children of RootLayout (D-40)"
  artifacts:
    - src/stores/uiStore.ts
    - src/components/ui/sheet.tsx
    - src/components/SearchBar.tsx
    - src/components/FilterBottomSheet.tsx
    - src/components/FilterChips.tsx
    - src/routes/medicines/index.tsx
    - src/components/BottomTabBar.tsx
    - src/App.tsx
  key_links:
    - "uiStore.ts selectedCategories/selectedLocations/selectedStatuses consumed by FilterBottomSheet, FilterChips, medicines/index.tsx"
    - "uiStore.ts filterSheetOpen / setFilterSheetOpen wires FilterBottomSheet open state"
    - "medicines/index.tsx useLiveQuery filters deletedAt===null (depends on 02-01 db.version(2))"
    - "App.tsx /dashboard and /trash routes wired — Dashboard and Trash components created in 02-03 and 02-04"
---

## Phase Goal

**As a** user at a pharmacy, **I want to** search for a medicine by name from the Medicines screen and filter by category/location/status, **so that** I can see its stock and validity status in under 5 seconds without any drill-down.

<objective>
Deliver the complete Search and Filter/Sort vertical slice: Zustand uiStore extended with filter state, shadcn Sheet installed, SearchBar and FilterBottomSheet and FilterChips components created, Medicines screen updated to use two-step query+memo pattern, tab bar extended to 4 tabs, and App.tsx wired with Dashboard/Trash routes.

Purpose: This is the pharmacy use-case — the primary Phase 2 success scenario. The search bar must be the first visible element on the Medicines tab. Filter/sort state persists so the user returns to their filtered view after drilling into a detail.

Output: Fully interactive search + filter/sort on Medicines screen; 4-tab navigation; App.tsx ready for Dashboard and Trash screens.
</objective>

<execution_context>
@C:/Users/lukasz.bielinski/OneDrive - Accenture/my-folders/my-projects/vibe-coding/med-stock/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/lukasz.bielinski/OneDrive - Accenture/my-folders/my-projects/vibe-coding/med-stock/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-search-dashboard-audit/02-CONTEXT.md
@.planning/phases/02-search-dashboard-audit/02-RESEARCH.md
@.planning/phases/02-search-dashboard-audit/02-PATTERNS.md
@.planning/phases/02-search-dashboard-audit/02-UI-SPEC.md
@.planning/phases/02-search-dashboard-audit/02-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend uiStore, install shadcn Sheet, create SearchBar + FilterBottomSheet + FilterChips</name>
  <files>
    src/stores/uiStore.ts,
    src/components/ui/sheet.tsx,
    src/components/SearchBar.tsx,
    src/components/FilterBottomSheet.tsx,
    src/components/FilterChips.tsx
  </files>
  <read_first>
    - src/stores/uiStore.ts — read current state (locationDialogOpen only) before extending
    - src/components/BottomTabBar.tsx — read Tailwind class patterns for consistency
    - src/components/ui/input.tsx — read shadcn Input pattern for use inside SearchBar
    - src/components/ui/button.tsx — read shadcn Button pattern for clear button and filter chips
    - src/components/ui/alert-dialog.tsx — read Radix-backed shadcn component pattern (Sheet follows same structure)
    - src/lib/expiry.ts — read MedicineStatus type for status filter list
    - .planning/phases/02-search-dashboard-audit/02-PATTERNS.md — sections for uiStore.ts, SearchBar.tsx, FilterBottomSheet.tsx, FilterChips.tsx, shared Zustand array selector pattern
    - .planning/phases/02-search-dashboard-audit/02-RESEARCH.md — Pattern 4 (Zustand filter state), Pattern 5 (Filter Bottom Sheet), Pitfall 4 (shallow import path)
    - .planning/phases/02-search-dashboard-audit/02-UI-SPEC.md — Copywriting Contract (placeholder text, chip format, filter badge label), Component Inventory (new components)
  </read_first>
  <behavior>
    - uiStore useActiveFilterCount() returns 0 when no filters active
    - uiStore useActiveFilterCount() returns 3 when one category + one location + one status selected
    - uiStore toggleCategory('Pain') adds to selectedCategories; toggleCategory('Pain') again removes it
    - uiStore clearAllFilters() sets all three arrays to []
    - SearchBar renders Input with placeholder "Search medicines by name…" and no clear button when value is empty
    - SearchBar renders clear button (aria-label="Clear search") when value is non-empty
    - FilterChips returns null when no filters active
    - FilterChips renders a chip labeled "Category: Pain & Fever ×" when Pain & Fever is in selectedCategories
  </behavior>
  <action>
STEP 1 — Rewrite src/stores/uiStore.ts:
  Replace entire file. Import `create` from 'zustand'. Import `shallow` from 'zustand/shallow' (Zustand v5 import path — not 'zustand/react/shallow', Pitfall 4). Export types: `SortField = 'name' | 'expiryDate' | 'category'` and `SortDirection = 'asc' | 'desc'`.
  
  UIState interface adds to existing fields: `selectedCategories: string[]`, `selectedLocations: string[]`, `selectedStatuses: string[]`, `sortField: SortField`, `sortDirection: SortDirection`, `filterSheetOpen: boolean`, and actions: `toggleCategory(value: string)`, `toggleLocation(value: string)`, `toggleStatus(value: string)`, `setSort(field, direction)`, `clearAllFilters()`, `setFilterSheetOpen(open: boolean)`.
  
  Keep existing: `locationDialogOpen: boolean`, `setLocationDialogOpen(open: boolean)`.
  
  Use Zustand v5 curried form: `export const useUIStore = create<UIState>()((set) => ({...}))`.
  Toggle actions use set with updater: `set(s => ({ selectedCategories: s.selectedCategories.includes(value) ? s.selectedCategories.filter(v => v !== value) : [...s.selectedCategories, value] }))`.
  
  Export derived selector: `export const useActiveFilterCount = () => useUIStore(s => s.selectedCategories.length + s.selectedLocations.length + s.selectedStatuses.length)`.
  Re-export: `export { shallow }` (so FilterBottomSheet and FilterChips import shallow from this module).

STEP 2 — Install shadcn Sheet component:
  Run `npx shadcn@latest add sheet` in the project root. This generates `src/components/ui/sheet.tsx`. If the CLI is unavailable in the execution environment, manually create `src/components/ui/sheet.tsx` by copying the shadcn sheet component source from the shadcn GitHub repository (it wraps @radix-ui/react-dialog which is already installed as a transitive dep). The Sheet component exports: Sheet, SheetClose, SheetContent, SheetDescription, SheetFooter, SheetHeader, SheetOverlay, SheetPortal, SheetTitle, SheetTrigger. Verify the file exists and the build passes.

STEP 3 — Create src/components/SearchBar.tsx:
  Props: `{ value: string; onChange: (value: string) => void; placeholder?: string }`. Default placeholder: "Search medicines by name…" (per UI-SPEC copywriting contract). Use shadcn Input inside a `div className="relative flex items-center"`. Add `pr-8` on Input for clear button space. Render clear button (shadcn Button variant="ghost" size="sm", aria-label="Clear search") absolutely positioned on the right — only render when value.length > 0. Clear button onClick calls onChange(''). Set `autoComplete="off"` on Input.

STEP 4 — Create src/components/FilterBottomSheet.tsx:
  Import Sheet, SheetContent, SheetHeader, SheetTitle from '@/components/ui/sheet'. Import `useUIStore, shallow` from '@/stores/uiStore'. Import `calculateStatus` type awareness — import `MedicineStatus` type from '@/lib/expiry' for the 6-status list.
  
  Use controlled Sheet: `const { filterSheetOpen, setFilterSheetOpen } = useUIStore()`. Subscribe to arrays with shallow: `const selectedCategories = useUIStore(s => s.selectedCategories, shallow)` (same for selectedLocations, selectedStatuses).
  
  SheetContent: `side="bottom"` and `className="max-h-[80vh] overflow-y-auto rounded-t-xl px-4 pb-6"`.
  
  Filter dimensions (three sections): Status (6 statuses: 'Active', 'Opened', 'Expired', 'Used Up', 'Disposed', 'Archived' — D-32), Category (10 predefined categories from D-10: 'Pain & Fever', 'Antibiotics', 'Allergy', 'Digestive', 'Vitamins & Supplements', 'Skin & Topical', 'Eye & Ear', 'Cold & Flu', 'Heart & Circulation', 'Other'), Location (load from Dexie via useLiveQuery — query `db.locations.orderBy('name').toArray()` to show current locations).
  
  Each filter option: checkbox-style button that calls toggle action. Show checkmark or highlighted state when selected.
  
  Sort section: three buttons for sortField (Name, Expiry Date, Category) + toggle for sortDirection (A-Z / Z-A for name/category; Soonest / Latest for expiry). Call `setSort(field, direction)`.
  
  Bottom of sheet: shadcn Button variant="outline" with `clearAllFilters()` — label "Clear all filters".

STEP 5 — Create src/components/FilterChips.tsx:
  Subscribe to all three filter arrays with shallow. Derive `all` array: spread selectedCategories as `{ label: 'Category: ${v}', remove: () => toggleCategory(v) }`, same for selectedLocations ('Location: ${v}') and selectedStatuses ('Status: ${v}'). If `all.length === 0` return null. Render `div className="flex flex-wrap gap-2 px-4 py-2"`. Each chip: `span` with rounded-full bg-gray-100 text, chip.label text, and a small ghost Button with aria-label="Remove ${chip.label}" that calls chip.remove. The × symbol as button content (plain text character, not imported icon).
  </action>
  <verify>
    <automated>cd "C:/Users/lukasz.bielinski/OneDrive - Accenture/my-folders/my-projects/vibe-coding/med-stock" && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0
    - `src/stores/uiStore.ts` exports `useUIStore`, `useActiveFilterCount`, `shallow`, `SortField`, `SortDirection`
    - `src/stores/uiStore.ts` contains `import { shallow } from 'zustand/shallow'`
    - `src/stores/uiStore.ts` contains `selectedCategories: []`, `selectedLocations: []`, `selectedStatuses: []`
    - `src/stores/uiStore.ts` contains `clearAllFilters` action
    - `src/components/ui/sheet.tsx` exists and exports `SheetContent`
    - `src/components/SearchBar.tsx` exports `SearchBar` and accepts `value`, `onChange`, `placeholder` props
    - `src/components/FilterBottomSheet.tsx` exports `FilterBottomSheet` and uses `side="bottom"` on SheetContent
    - `src/components/FilterChips.tsx` exports `FilterChips` and returns null when no filters are active
  </acceptance_criteria>
  <done>uiStore has full filter/sort state; shadcn Sheet is installed; SearchBar, FilterBottomSheet, FilterChips are created and build-clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update Medicines screen (two-step query), BottomTabBar (4 tabs), App.tsx (new routes)</name>
  <files>
    src/routes/medicines/index.tsx,
    src/components/BottomTabBar.tsx,
    src/App.tsx
  </files>
  <read_first>
    - src/routes/medicines/index.tsx — read current state (useLiveQuery with manualStatus notEqual Disposed, no search)
    - src/components/BottomTabBar.tsx — read current 2-tab NavLink pattern
    - src/App.tsx — read current router definition and imports
    - src/lib/expiry.ts — read calculateStatus signature for use in useMemo
    - src/lib/db.ts — read Medicine interface to confirm deletedAt is available (from 02-01)
    - .planning/phases/02-search-dashboard-audit/02-PATTERNS.md — "src/routes/medicines/index.tsx" and "src/App.tsx" and "src/components/BottomTabBar.tsx" sections
    - .planning/phases/02-search-dashboard-audit/02-RESEARCH.md — Pattern 2 (active records query), Pattern 3 (live search + two-step), Pitfall 1 (never use where('deletedAt').equals(null)), Pitfall 3 (calculateStatus must be in useMemo not useLiveQuery querier)
    - .planning/phases/02-search-dashboard-audit/02-UI-SPEC.md — Interaction section (Search, Filter/Sort Controls, Filter Chips), Copywriting (empty states)
  </read_first>
  <behavior>
    - When searchQuery is empty and no filters: all active medicines (deletedAt === null) are shown
    - When searchQuery is 'ibu': only medicines whose name.toLowerCase().includes('ibu') and deletedAt===null are shown
    - When selectedStatuses includes 'Expired': only medicines where calculateStatus(m) === 'Expired' are shown (post-filter in useMemo)
    - When selectedCategories has ['Pain & Fever'] and selectedLocations has ['Bathroom Cabinet']: AND logic applied — only Pain & Fever medicines in Bathroom Cabinet are shown
    - OR logic within dimension: selectedLocations=['Bathroom Cabinet','Kitchen Drawer'] shows medicines in either location
    - useLiveQuery re-runs when searchQuery changes (searchQuery in deps array)
    - useMemo re-runs when medicines array, selectedStatuses, selectedCategories, selectedLocations, sortField, or sortDirection changes
    - Filter badge on filter icon shows count from useActiveFilterCount
  </behavior>
  <action>
STEP 1 — Rewrite src/routes/medicines/index.tsx:
  The component is renamed from MedicineList to MedicineList (keep export name). Import additions: `useState, useMemo` from 'react', `useUIStore, useActiveFilterCount, shallow` from '@/stores/uiStore', `SearchBar` from '@/components/SearchBar', `FilterBottomSheet` from '@/components/FilterBottomSheet', `FilterChips` from '@/components/FilterChips', `calculateStatus` from '@/lib/expiry', and Lucide's `SlidersHorizontal` icon from 'lucide-react'.
  
  Add local state: `const [searchQuery, setSearchQuery] = useState('')`.
  Subscribe to Zustand with shallow for arrays: `const selectedCategories = useUIStore(s => s.selectedCategories, shallow)`, same for selectedLocations and selectedStatuses. Get `sortField, sortDirection, setFilterSheetOpen` from useUIStore. Get `filterCount = useActiveFilterCount()`.
  
  STEP 1a — Replace the useLiveQuery call entirely:
    New querier (deps: [searchQuery]): `db.medicines.toCollection().filter(m => { if (m.deletedAt !== null) return false; const q = searchQuery.toLowerCase().trim(); if (q && !m.name.toLowerCase().includes(q)) return false; return true; }).toArray()`.
    Do NOT use `where('deletedAt').equals(null)` — this silently fails (Pitfall 1, D-41 amendment).
    Do NOT put calculateStatus inside the querier (Pitfall 3).
  
  STEP 1b — Add useMemo for client-side post-filter and sort (deps include medicines + all Zustand filter state):
    const filtered = useMemo(() => { if (!medicines) return []; const now = new Date(); return medicines.filter(m => { const status = calculateStatus(m, now); if (selectedStatuses.length > 0 && !selectedStatuses.includes(status)) return false; if (selectedCategories.length > 0 && !selectedCategories.includes(m.category ?? 'Other')) return false; if (selectedLocations.length > 0 && !selectedLocations.includes(m.location ?? 'Other')) return false; return true; }).sort((a, b) => { const dir = sortDirection === 'asc' ? 1 : -1; const va = String(a[sortField as keyof typeof a] ?? ''); const vb = String(b[sortField as keyof typeof b] ?? ''); return va.localeCompare(vb) * dir; }); }, [medicines, selectedStatuses, selectedCategories, selectedLocations, sortField, sortDirection]).
  
  STEP 1c — Update JSX layout:
    Top section: full-width SearchBar (value=searchQuery, onChange=setSearchQuery, placeholder="Search medicines by name…").
    Below SearchBar: FilterChips component (auto-renders only when filters active).
    Page header row: `<h1>Medicines</h1>` + filter icon button (SlidersHorizontal icon, onClick=() => setFilterSheetOpen(true)) with badge showing filterCount when filterCount > 0.
    Below header: Add Medicine button (Link to /medicines/new).
    Medicine list: map over `filtered` (not `medicines`) → MedicineCard.
    Empty state when medicines===undefined: loading message.
    Empty state when filtered.length===0 and searchQuery is non-empty: "No medicines match your search" (UI-SPEC copywriting).
    Empty state when filtered.length===0 and searchQuery is empty: "No medicines yet. Tap + to add your first medicine." (UI-SPEC copywriting) + Add Medicine button.
    Add FilterBottomSheet component at end of JSX tree (it manages its own open state via Zustand).

STEP 2 — Update src/components/BottomTabBar.tsx:
  Keep existing Medicines and Locations NavLinks with their exact className pattern. Insert two new NavLinks between Medicines and Locations (D-39 tab order: Medicines | Dashboard | Trash | Locations):
  - Dashboard: `to="/dashboard"`, label "Dashboard", same className pattern
  - Trash: `to="/trash"`, label "Trash", same className pattern
  Use the same cn() + isActive pattern as existing tabs. Import Lucide icons if desired (Home, LayoutDashboard, Trash2, MapPin) but text labels are sufficient per UI-SPEC.

STEP 3 — Update src/App.tsx:
  Add two new route imports (components will exist after Plan 02-03 and 02-04, but import them now to wire routing — the files will be stubs until those plans run, or add placeholder components directly in App.tsx to avoid build errors):
    `import { DashboardScreen } from '@/routes/dashboard/index'`
    `import { TrashScreen } from '@/routes/trash/index'`
  Add two new route entries inside the children array of RootLayout (D-40):
    `{ path: 'dashboard', element: <DashboardScreen /> }`
    `{ path: 'trash', element: <TrashScreen /> }`
  
  IMPORTANT: src/routes/dashboard/index.tsx and src/routes/trash/index.tsx do not exist yet (they are created in Plans 02-03 and 02-04). To avoid a build failure in this plan, create minimal stub files:
    src/routes/dashboard/index.tsx: `export function DashboardScreen() { return <div className="p-4">Dashboard coming soon</div> }`
    src/routes/trash/index.tsx: `export function TrashScreen() { return <div className="p-4">Trash coming soon</div> }`
  These stubs will be replaced by Plans 02-03 and 02-04.
  Preserve the existing router constant outside the React tree pattern (line 11 comment must remain).
  </action>
  <verify>
    <automated>cd "C:/Users/lukasz.bielinski/OneDrive - Accenture/my-folders/my-projects/vibe-coding/med-stock" && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0
    - `src/routes/medicines/index.tsx` contains `toCollection().filter(m =>` for the active-records query (not `where('deletedAt')`)
    - `src/routes/medicines/index.tsx` contains `useMemo` with `calculateStatus` for post-filter (Pitfall 3 guard)
    - `src/routes/medicines/index.tsx` contains `SearchBar` in JSX
    - `src/routes/medicines/index.tsx` contains `FilterChips` in JSX
    - `src/routes/medicines/index.tsx` contains `FilterBottomSheet` in JSX
    - `src/routes/medicines/index.tsx` does NOT contain `where('manualStatus').notEqual` (Phase 1 pattern replaced)
    - `src/components/BottomTabBar.tsx` contains NavLink `to="/dashboard"` and NavLink `to="/trash"`
    - `src/App.tsx` contains `path: 'dashboard'` and `path: 'trash'` route entries
    - `src/routes/dashboard/index.tsx` exists (stub acceptable)
    - `src/routes/trash/index.tsx` exists (stub acceptable)
  </acceptance_criteria>
  <done>Medicines screen has search bar + filter chips + two-step query+memo pattern; tab bar has 4 tabs; App.tsx has dashboard and trash routes. Build passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User input → SearchBar → client-side filter | searchQuery is a controlled string; used only in .toLowerCase().includes() — no DOM injection surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-03 | Spoofing/Tampering | SearchBar — searchQuery rendered to DOM | low | accept | React escapes all rendered strings by default; searchQuery is never passed to dangerouslySetInnerHTML; no XSS surface |
| T-02-04 | Tampering | FilterBottomSheet — status filter options hardcoded | low | accept | Status list is hardcoded in component; user cannot inject custom status values |
| T-02-SC | Tampering | npx shadcn@latest add sheet | medium | mitigate | shadcn is an official Vercel project; package is @radix-ui/react-dialog which is already installed. Executor confirms sheet.tsx generates from official shadcn registry before accepting. |
</threat_model>

<verification>
After both tasks:

1. `npm run build` exits 0 — full TypeScript check passes
2. `npm run test -- --run` exits 0 — Phase 1 + 02-01 tests still pass
3. Structural assertions:
   - `grep -c 'toCollection().filter' src/routes/medicines/index.tsx` > 0 (not where().equals(null))
   - `grep -c 'useMemo' src/routes/medicines/index.tsx` > 0
   - `grep -c 'calculateStatus' src/routes/medicines/index.tsx` > 0
   - `grep -c 'SearchBar' src/routes/medicines/index.tsx` > 0
   - `grep -c 'dashboard' src/components/BottomTabBar.tsx` > 0
   - `grep -c 'trash' src/components/BottomTabBar.tsx` > 0
4. Manual verification (checkpoint:human-verify is in end-of-phase UAT per workflow mode):
   - Open app at /#/medicines; search bar visible immediately at top
   - Type partial name; results filter instantly
   - Tap filter icon; bottom sheet slides up from bottom
   - Select a filter; chip appears above list; badge on icon shows count
   - Navigate to detail view; press Back; filter is still active
</verification>

<success_criteria>
- Search bar is the first element on the Medicines tab (D-20 — permanently visible)
- Instant filtering on keystroke with substring match (D-21)
- Results use existing MedicineCard (D-22)
- Search + filters combine with AND logic (D-23)
- Empty search shows full list (D-24)
- Filter bottom sheet opens from filter icon (D-29)
- FilterChips appear for active filters (D-30)
- Filter state persists via Zustand (D-31)
- All 6 statuses supported in filter (D-32)
- OR within / AND across filter dimensions (D-33)
- 4-tab navigation works (D-39)
- /dashboard and /trash routes registered (D-40)
- Build passes, all prior tests pass
</success_criteria>

<output>
Create `.planning/phases/02-search-dashboard-audit/02-02-SUMMARY.md` when done.
</output>

## Artifacts This Phase Produces

| Symbol | Type | File | Consumed By |
|--------|------|------|-------------|
| `useUIStore` (extended) | Zustand store | `src/stores/uiStore.ts` | Plans 02-03, 02-04 (tap-to-filter navigation) |
| `useActiveFilterCount` | Derived selector | `src/stores/uiStore.ts` | medicines/index.tsx |
| `shallow` (re-export) | Import alias | `src/stores/uiStore.ts` | FilterBottomSheet, FilterChips |
| `SearchBar` | React component | `src/components/SearchBar.tsx` | medicines/index.tsx |
| `FilterBottomSheet` | React component | `src/components/FilterBottomSheet.tsx` | medicines/index.tsx |
| `FilterChips` | React component | `src/components/FilterChips.tsx` | medicines/index.tsx |
| `SheetContent` (etc.) | shadcn component | `src/components/ui/sheet.tsx` | FilterBottomSheet |
| `DashboardScreen` (stub) | Route component | `src/routes/dashboard/index.tsx` | App.tsx (replaced by 02-03) |
| `TrashScreen` (stub) | Route component | `src/routes/trash/index.tsx` | App.tsx (replaced by 02-04) |
