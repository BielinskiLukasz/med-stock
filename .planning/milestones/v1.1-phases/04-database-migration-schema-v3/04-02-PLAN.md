---
phase: 04
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - src/lib/historyOps.ts
  - src/lib/historyOps.test.ts
autonomous: true
requirements:
  - MIGR-01
  - MIGR-02
must_haves:
  truths:
    - "All 5 mutation functions in historyOps.ts accept explicit medicineName parameter (per D-06)"
    - "medicineName is written to history entries from explicit parameter, not read from medicine object (per D-06)"
    - "HistoryEntry.medicineId continues to hold stock entry id, no catalogId field added (per D-07)"
    - "Phase 5 callers can supply medicineName from catalog context without historyOps importing MedicineCatalog"
    - "All existing tests in historyOps.test.ts pass with new function signatures"
    - "No TypeScript errors in signature changes"
  artifacts:
    - "src/lib/historyOps.ts — 5 updated mutation functions with medicineName parameter"
    - "src/lib/historyOps.test.ts — 100+ updated test calls with explicit medicineName argument"
  key_links:
    - "Signature consistency: all 5 functions use same `medicineName: string` parameter pattern"
    - "Test coverage: every test call updated to pass medicineName without breaking assertions"

## Artifacts This Phase Produces

**Function Signature Updates:**
- `updateMedicineWithHistory(id, before, changes, medicineName)` — new parameter: medicineName
- `softDeleteMedicine(medicine, medicineName)` — new parameter: medicineName
- `restoreMedicine(medicine, medicineName)` — new parameter: medicineName
- `permanentDeleteMedicine(medicine, medicineName)` — new parameter: medicineName
- `addMedicineHistory(medicine, medicineName, action)` — new parameter: medicineName (second position)

**Test Suite Updates:**
- All test calls updated to pass explicit medicineName argument
- Test expectations unchanged (same history entry structure, same assertions)
- No new tests added (existing suite structure preserved)

---

<objective>
Update historyOps.ts mutation functions to accept explicit `medicineName` parameter, decoupling history recording from catalog/stock relationship. This prepares the mutation layer for Phase 5 callers that have catalog context and can supply the name explicitly.

Purpose: Phase 5 forms and API routes will have both catalog and stock entities in scope; historyOps should not need to import or access MedicineCatalog table.

Output: Production-ready mutation signatures and passing test suite.
</objective>

<execution_context>
@C:/my-code/vibe-coding/med-stock/.claude/gsd-core/workflows/execute-plan.md
@C:/my-code/vibe-coding/med-stock/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-database-migration-schema-v3/04-CONTEXT.md
@.planning/phases/04-database-migration-schema-v3/04-PATTERNS.md
@src/lib/historyOps.ts
@src/lib/historyOps.test.ts
@.planning/phases/04-database-migration-schema-v3/04-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Update historyOps.ts: add medicineName parameter to all 5 mutation functions (per D-06)</name>
  <files>src/lib/historyOps.ts</files>
  <read_first>
    - src/lib/historyOps.ts (lines 1–134) — current function signatures and history entry writes
    - .planning/phases/04-database-migration-schema-v3/04-PATTERNS.md (Signature Change Pattern section) — exact transformation for each function
    - .planning/phases/04-database-migration-schema-v3/04-CONTEXT.md (D-06, D-07) — design rationale for parameter changes
  </read_first>
  <action>
Update all 5 mutation functions to accept explicit `medicineName: string` parameter and use it instead of reading `medicine.name` from the medicine object.

**Function 1: updateMedicineWithHistory (lines 41–57)**

Current signature:
```typescript
export async function updateMedicineWithHistory(
  id: number,
  before: Medicine,
  changes: Partial<Medicine>
): Promise<void>
```

New signature:
```typescript
export async function updateMedicineWithHistory(
  id: number,
  before: Medicine,
  changes: Partial<Medicine>,
  medicineName: string
): Promise<void>
```

Update line 51 from `medicineName: before.name,` to `medicineName: medicineName,`

---

**Function 2: softDeleteMedicine (lines 63–75)**

Current signature:
```typescript
export async function softDeleteMedicine(medicine: Medicine): Promise<void>
```

New signature:
```typescript
export async function softDeleteMedicine(medicine: Medicine, medicineName: string): Promise<void>
```

Update line 69 from `medicineName: medicine.name,` to `medicineName: medicineName,`

---

**Function 3: restoreMedicine (lines 82–94)**

Current signature:
```typescript
export async function restoreMedicine(medicine: Medicine): Promise<void>
```

New signature:
```typescript
export async function restoreMedicine(medicine: Medicine, medicineName: string): Promise<void>
```

Update line 88 from `medicineName: medicine.name,` to `medicineName: medicineName,`

---

**Function 4: permanentDeleteMedicine (lines 101–113)**

Current signature:
```typescript
export async function permanentDeleteMedicine(medicine: Medicine): Promise<void>
```

New signature:
```typescript
export async function permanentDeleteMedicine(medicine: Medicine, medicineName: string): Promise<void>
```

Update line 106 from `medicineName: medicine.name,` to `medicineName: medicineName,`

---

**Function 5: addMedicineHistory (lines 119–133)**

Current signature:
```typescript
export async function addMedicineHistory(
  medicine: Medicine,
  action: 'created'
): Promise<void>
```

New signature:
```typescript
export async function addMedicineHistory(
  medicine: Medicine,
  medicineName: string,
  action: 'created'
): Promise<void>
```

Update line 127 from `medicineName: medicine.name,` to `medicineName: medicineName,`

---

**After updates:**
- All 5 functions have explicit medicineName parameter
- No function reads medicine.name or medicine.category
- HistoryEntry.medicineId continues to hold stock entry id (unchanged)
- Transaction patterns unchanged; only the one medicineName assignment line changes per function

Run `npm run build` to verify TypeScript compilation succeeds.
  </action>
  <verify>
    <automated>npm run build 2>&1 | grep -E "error|fail" | wc -l</automated>
  </verify>
  <done>
    - All 5 mutation functions have updated signatures with explicit medicineName parameter
    - All history entry writes use the explicit parameter (not medicine.name)
    - TypeScript compiles without errors
    - No breaking changes to transaction pattern or HistoryEntry structure
  </done>
  <reversibility rating="reversible">
    D-06 (signature changes) — Phase 5 is not yet written, so callers don't exist yet. If needed, can revert to reading medicine.name before Phase 5 ships.
  </reversibility>
</task>

<task type="auto">
  <name>Update historyOps.test.ts: pass explicit medicineName to all function calls</name>
  <files>src/lib/historyOps.test.ts</files>
  <read_first>
    - src/lib/historyOps.test.ts (entire file) — current test structure and all function call sites
    - .planning/phases/04-database-migration-schema-v3/04-PATTERNS.md (historyOps.test.ts section) — test lifecycle and call patterns
    - src/lib/db.ts — baseMedicine fixture structure to understand what values are available in tests
  </read_first>
  <action>
Update all test calls to historyOps functions to pass explicit medicineName argument. The value is always the medicine name from the test fixture or explicit test data.

**Pattern for all updates:**

Before:
```typescript
await softDeleteMedicine(medicine)
```

After:
```typescript
await softDeleteMedicine(medicine, medicine.name)  // or 'Ibuprofen' if fixture
```

---

**Specific updates (by test suite):**

1. **diffMedicine tests** — No changes (utility function, no medicineName parameter)

2. **softDeleteMedicine tests (describe block starting line ~75)**
   - Line ~: `await softDeleteMedicine(medicine)` → `await softDeleteMedicine(medicine, medicine.name)`
   - Line ~: `await softDeleteMedicine(medicine)` → `await softDeleteMedicine(medicine, medicine.name)` (repeat for all calls)
   - Test assertion on line ~369: `expect(entries[0].medicineName).toBe('Ibuprofen')` — no change (already expects the name)

3. **restoreMedicine tests (describe block starting line ~96)**
   - All calls: `await restoreMedicine(medicine)` → `await restoreMedicine(medicine, medicine.name)`

4. **permanentDeleteMedicine tests (describe block starting line ~117)**
   - All calls: `await permanentDeleteMedicine(medicine)` → `await permanentDeleteMedicine(medicine, medicine.name)`

5. **updateMedicineWithHistory tests (describe block starting line ~138)**
   - All calls: `await updateMedicineWithHistory(id, before, changes)` → `await updateMedicineWithHistory(id, before, changes, before.name)`
   - Example: `await updateMedicineWithHistory(id, medicine, { name: 'Ibuprofen Plus' }, medicine.name)`

6. **addMedicineHistory tests (describe block starting line ~177)**
   - All calls: `await addMedicineHistory(medicine, 'created')` → `await addMedicineHistory(medicine, medicine.name, 'created')`

---

**Shortcut: Find and replace pattern**

Use this pattern to catch most updates:

- `softDeleteMedicine(medicine)` → `softDeleteMedicine(medicine, medicine.name)`
- `restoreMedicine(medicine)` → `restoreMedicine(medicine, medicine.name)`
- `permanentDeleteMedicine(medicine)` → `permanentDeleteMedicine(medicine, medicine.name)`
- `updateMedicineWithHistory(id, ` → `updateMedicineWithHistory(id, ` (add medicine.name or before.name as 4th arg)
- `addMedicineHistory(medicine,` → `addMedicineHistory(medicine, medicine.name,`

After updates, run test suite to verify all pass.
  </action>
  <verify>
    <automated>npm test -- src/lib/historyOps.test.ts 2>&1 | grep -E "passed|failed"</automated>
  </verify>
  <done>
    - All test calls to softDeleteMedicine, restoreMedicine, permanentDeleteMedicine pass explicit medicineName
    - All test calls to updateMedicineWithHistory pass explicit medicineName as 4th parameter
    - All test calls to addMedicineHistory pass explicit medicineName as 2nd parameter
    - All test assertions unchanged (medicineName still asserted in history entries)
    - npm test passes with zero failures
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Phase 5 code → historyOps | Callers must supply medicineName from their own context; historyOps trusts the parameter value |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-05 | Integrity | medicineName parameter mutation | low | accept | History entries only read by report/trash views (never used for logic). Caller responsibility to supply correct name. |
| T-04-06 | Completeness | Missing medicineName in Phase 5 call | medium | mitigate | TypeScript compiler enforces parameter presence. If Phase 5 code forgets the parameter, build fails immediately. |

</threat_model>

<verification>
**Phase 4 Plan 02 Acceptance Criteria:**

1. ✓ All 5 mutation functions have updated signatures with medicineName parameter
2. ✓ updateMedicineWithHistory: `(..., medicineName: string)` — added as 4th parameter
3. ✓ softDeleteMedicine: `(..., medicineName: string)` — added as 2nd parameter
4. ✓ restoreMedicine: `(..., medicineName: string)` — added as 2nd parameter
5. ✓ permanentDeleteMedicine: `(..., medicineName: string)` — added as 2nd parameter
6. ✓ addMedicineHistory: `(..., medicineName: string, ...)` — added as 2nd parameter
7. ✓ All 100+ test calls updated to pass medicineName argument
8. ✓ `npm test` passes with zero failures
9. ✓ `npm run build` succeeds with no TypeScript errors

**Readiness for Phase 5:**
- historyOps mutation functions ready to be called from form/route handlers
- Callers in Phase 5 will have both catalog and stock in scope, can supply medicineName directly
- No historyOps imports needed from MedicineCatalog or any catalog-related tables
</verification>

<success_criteria>
- All 5 mutation functions accept explicit medicineName parameter (per D-06)
- Parameter is used directly in history entry writes (not read from medicine object)
- Test suite passes completely with new signatures
- TypeScript strict mode compliance maintained
- No breaking changes to transaction atomicity or history entry structure
</success_criteria>

<output>
Create `.planning/phases/04-database-migration-schema-v3/04-02-SUMMARY.md` when complete.
</output>
