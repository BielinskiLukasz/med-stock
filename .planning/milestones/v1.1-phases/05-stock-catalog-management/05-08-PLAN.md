---
phase: 05-stock-catalog-management
plan: 08
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/lib/aggregation.ts
  - src/lib/aggregation.test.ts
autonomous: true
gap_closure: true
gap_ids: [G-05-4]
requirements: [CAT-01]

must_haves:
  truths:
    - "computeCatalogAggregate returns the worst-case AutoStatus using priority order: Expired(4) > ExceededOpenPeriod(3) > Opened(2) > Active(1)"
    - "Entries with a ManualStatus result from calculateStatus are excluded from worst-case evaluation"
    - "PAO-only entries with no expiryDate are included in evaluation — calculateStatus handles the logic"
    - "totalQty remains the sum of (stock.quantity ?? 0) across all entries — unchanged"
  artifacts:
    - src/lib/aggregation.ts (priority-reduce implementation)
    - src/lib/aggregation.test.ts (priority ordering, manual status exclusion, PAO-only coverage)
  key_links:
    - "calculateStatus() called on every entry in the activeStocks array, not just nearest-expiry"
    - "PRIORITY map drives worst-case reduce; MANUAL_STATUSES set gates exclusion before priority check"
---

<objective>
Rewrite computeCatalogAggregate to use a priority-based worst-case reduction across all stock entries instead of the nearest-expiry proxy. The nearest-expiry approach misses ExceededOpenPeriod (which has no expiry date), lets manual statuses leak into the aggregate, and returns incorrect results when the nearest-expiry entry is not the most critical one.

Purpose: Ensures the aggregate status shown in the list view accurately reflects the worst health state across all stock entries per catalog.
Output: Rewritten aggregation.ts and updated aggregation.test.ts with full priority-ordering coverage.
</objective>

<execution_context>
@C:/my-code/vibe-coding/med-stock/.claude/gsd-core/workflows/execute-plan.md
@C:/my-code/vibe-coding/med-stock/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-stock-catalog-management/05-UAT.md
</context>

<feature>
  <name>computeCatalogAggregate priority-reduce (G-05-4)</name>
  <files>src/lib/aggregation.ts, src/lib/aggregation.test.ts</files>
  <behavior>
    RED phase — write these failing tests in aggregation.test.ts before touching aggregation.ts:

    1. Priority: ExceededOpenPeriod wins over Opened and Active.
       Input: two entries — one Opened (has openedDate, no PAO, future expiry) and one ExceededOpenPeriod (has openedDate + PAO where PAO window has elapsed).
       Expected: status === 'ExceededOpenPeriod'.

    2. Priority: Expired wins over ExceededOpenPeriod.
       Input: one entry with past expiryDate (Expired), one entry with elapsed PAO (ExceededOpenPeriod).
       Expected: status === 'Expired'.

    3. Manual status excluded from aggregate.
       Input: one entry with manualStatus='UsedUp', one entry with future expiryDate and no openedDate (Active).
       Expected: status === 'Active' (ManualStatus entry is skipped; Active entry remains).

    4. PAO-only entry (no expiryDate) with elapsed PAO yields ExceededOpenPeriod in aggregate.
       Input: one entry with expiryDate=null, openedDate=past, pao={value:1,unit:'days'} (PAO elapsed long ago).
       Expected: status === 'ExceededOpenPeriod'.

    5. All-ManualStatus entries with no AutoStatus entries fall back to Active.
       Input: two entries both with manualStatus='Disposed'.
       Expected: status === 'Active'.

    Existing tests must continue to pass (do not delete them — keep nearest-expiry-is-still-worst-case tests, they remain valid because Expired still wins).

    GREEN phase — rewrite computeCatalogAggregate in aggregation.ts:
    - Define a PRIORITY map: { Expired: 4, ExceededOpenPeriod: 3, Opened: 2, Active: 1 }.
    - Define a MANUAL_STATUSES set: { 'UsedUp', 'Disposed', 'Archived' }.
    - Start with worstStatus = 'Active'.
    - For each stock entry in activeStocks: call calculateStatus(stock). If the result is in MANUAL_STATUSES, skip. Otherwise, if PRIORITY[result] > PRIORITY[worstStatus], update worstStatus.
    - totalQty computation is unchanged: sum of (stock.quantity ?? 0).
    - Return { status: worstStatus, totalQty }.

    REFACTOR phase — verify the PRIORITY and MANUAL_STATUSES structures are module-level constants (not recreated per call).
  </behavior>
  <implementation>
    calculateStatus is already imported in aggregation.ts. Add the PRIORITY constant and MANUAL_STATUSES Set as module-level constants after the imports. Replace the nearestExpiryStock reduce with a for-of loop that calls calculateStatus on every entry and updates worstStatus via the PRIORITY map. The return shape { status, totalQty } is unchanged — no callers need updating.

    The MedicineStatus type from expiry.ts covers both AutoStatus and ManualStatus. AutoStatus members are the only keys needed in PRIORITY (Active, Opened, Expired, ExceededOpenPeriod). Cast the result of PRIORITY[s as AutoStatus] defensively — if s is not an AutoStatus key, the value is undefined which is falsy, so the > check safely skips it.
  </implementation>
</feature>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Pure function | computeCatalogAggregate has no I/O — all inputs are typed DB records |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-08-01 | Information Disclosure | aggregate status leak | low | accept | Manual statuses are intentionally excluded — correct by design, not a security concern |
</threat_model>

<verification>
RED: `npx vitest run src/lib/aggregation.test.ts` — new tests fail, existing tests pass.
GREEN: `npx vitest run src/lib/aggregation.test.ts` — all tests pass.
Final: `npx vitest run` — full suite green. `npx tsc --noEmit` — clean.
</verification>

<success_criteria>
Gap G-05-4 closed. All aggregation tests pass including the five new priority-ordering cases. No TypeScript errors. Full test suite green.
</success_criteria>

<output>
Create `.planning/phases/05-stock-catalog-management/05-08-SUMMARY.md` when done
</output>
