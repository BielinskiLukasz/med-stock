---
phase: 05-stock-catalog-management
plan: 10
type: execute
wave: 2
depends_on: [05-07, 05-08, 05-09]
files_modified:
  - src/routes/medicines/[id].tsx
  - src/lib/aggregation.ts
autonomous: true
gap_closure: true
gap_ids: [G-05-2, G-05-3]
requirements: [STOCK-01, STOCK-03]

must_haves:
  truths:
    - "Open box on a stock entry with packCount > 1 decrements packCount by 1 on the original and creates a new entry with quantity=stock.quantity and packCount=1"
    - "Open box button is visible when packCount > 1, even if quantity <= 1"
    - "Stock entry cards display packCount when set: e.g. '2 boxes × 30 tablets'"
    - "computeCatalogAggregate totalQty counts (packCount ?? 1) * quantity per entry"
  artifacts:
    - src/routes/medicines/[id].tsx (handleOpenBoxClick + Open box guard + stock card display)
    - src/lib/aggregation.ts (totalQty updated to multiply by packCount)
  key_links:
    - "packCount field now exists in Medicine (added by 05-09) — [id].tsx and aggregation.ts can read it"
    - "handleOpenBoxClick branches: packCount > 1 path uses pack-level split; fallback path is unchanged unit-level split"
---

<objective>
Complete the G-05-2 and G-05-3 fixes: wire packCount into the Open box behavior (G-05-3) and into the aggregate quantity display (G-05-2 display half). The schema and form work was done in 05-09; this plan delivers the behavior that depends on packCount being present in the DB.

Purpose: Users expect "Open box" to operate at the box level: opening one box leaves the remaining boxes intact and creates a new entry with the full per-box quantity marked as opened. Without packCount this was impossible; now that packCount exists, the behavior can be implemented correctly.
Output: Updated [id].tsx and aggregation.ts.
</objective>

<execution_context>
@C:/my-code/vibe-coding/med-stock/.claude/gsd-core/workflows/execute-plan.md
@C:/my-code/vibe-coding/med-stock/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-stock-catalog-management/05-UAT.md
@.planning/phases/05-stock-catalog-management/05-09-SUMMARY.md
</context>

<tasks>

<task type="tracer">
  <name>Tracer: handleOpenBoxClick pack-level split + Open box guard update (G-05-3)</name>
  <files>src/routes/medicines/[id].tsx</files>
  <action>
    Read the current file before editing. This file was already edited in 05-07 (Open box guard and ChangeHistory). Build on that state.

    Open box guard update:
    The current guard condition (after 05-07) is `(stock.quantity ?? 0) > 1 && !stock.openedDate`. Update it to also show the button when packCount > 1 and the entry is not yet opened. New condition: `((stock.quantity ?? 0) > 1 || (stock.packCount ?? 0) > 1) && !stock.openedDate`.

    handleOpenBoxClick rewrite:
    The function currently hardcodes `quantity: 1` for the new entry and decrements quantity by 1 on the original. Replace with a branch:

    When `stock.packCount && stock.packCount > 1` (entry has multiple boxes): the pack-level split path. The new entry gets `quantity: stock.quantity` (same per-box quantity) and `packCount: 1` (one box). The original entry gets `packCount: stock.packCount - 1` (one fewer box) and quantity remains unchanged. Both changes are in the same db.transaction. History is recorded for both (existing addMedicineHistory and updateMedicineWithHistory calls — update the updateMedicineWithHistory call to pass `{ packCount: stock.packCount - 1, updatedAt: now }` instead of `{ quantity: (stock.quantity ?? 0) - 1, updatedAt: now }`).

    When packCount is null or 1 (no box tracking or single box): keep the existing unit-level behaviour — new entry gets `quantity: 1, packCount: null`, original decrements quantity by 1. This path is unchanged from the current implementation except adding `packCount: null` to the new entry creation to satisfy the updated Medicine type.

    In the db.medicines.add call for the new entry in the pack-level path, include all required Medicine fields: catalogId, quantity (from stock.quantity), quantityUnit, expiryDate, openedDate (today), pao, location, manualStatus: null, notes, packCount: 1, createdAt: now, updatedAt: now, deletedAt: null.

    Stock card display:
    In the stock entry card JSX, after the quantity/unit line (`{stock.quantity} {stock.quantityUnit || 'units'}`), add a conditional: when `stock.packCount` is a positive number, show it as "{stock.packCount} boxes × {stock.quantity} {stock.quantityUnit || 'units'}"; when packCount is null or absent, show the existing quantity display unchanged.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>TypeScript compiles. For a stock entry with packCount=2 and quantity=30: clicking Open box decrements packCount to 1 on the original and creates a new entry with quantity=30, packCount=1, openedDate=today. For a stock entry with packCount=null and quantity=5: clicking Open box decrements quantity to 4 and creates a new entry with quantity=1. Open box button is hidden when entry has openedDate set (from 05-07).</done>
</task>

<task type="auto">
  <name>Update aggregate totalQty to account for packCount (G-05-2 display)</name>
  <files>src/lib/aggregation.ts</files>
  <action>
    Read the current file before editing. This file was rewritten in 05-08 to use priority-reduce for status. Build on that state.

    Update the totalQty computation inside computeCatalogAggregate. Currently it sums `(stock.quantity ?? 0)` across all entries. Update it to multiply each entry's quantity by its pack count: `(stock.packCount ?? 1) * (stock.quantity ?? 0)`. When packCount is null (not tracked), treat it as 1 — so the total is the same as before for entries without pack tracking. When packCount is 2 and quantity is 30, the contribution is 60.

    The return shape `{ status, totalQty }` is unchanged. No callers need updating.
  </action>
  <verify>
    <automated>npx vitest run src/lib/aggregation.test.ts; npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>Aggregation tests pass. For a catalog with two stock entries: (packCount=2, quantity=30) and (packCount=null, quantity=10), totalQty === 70. For entries without packCount, behaviour is identical to before.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DB transaction | handleOpenBoxClick writes two records atomically — no partial state possible on failure |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-10-01 | Tampering | handleOpenBoxClick pack decrement | low | mitigate | db.transaction wraps both writes; if packCount was tampered to 0 the guard `packCount > 1` prevents the pack-path from running |
</threat_model>

<verification>
`npx tsc --noEmit` clean. `npx vitest run` green.

Manual: Add a stock entry with Number of boxes = 2, quantity = 30 tablets. Open detail view. Confirm card shows "2 boxes × 30 tablets". Click Open box. Confirm: original entry now shows "1 box × 30 tablets"; new entry shows quantity 30 with Opened status and today's date. Open box button absent on the new (opened) entry.
</verification>

<success_criteria>
Gaps G-05-2 (display half) and G-05-3 fully closed. Open box correctly operates at pack level when packCount is tracked. Aggregate totalQty reflects pack×unit product. TypeScript clean. All tests green.
</success_criteria>

<output>
Create `.planning/phases/05-stock-catalog-management/05-10-SUMMARY.md` when done
</output>
