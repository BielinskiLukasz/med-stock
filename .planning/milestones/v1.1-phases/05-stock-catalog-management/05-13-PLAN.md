---
phase: 05-stock-catalog-management
plan: 13
type: execute
wave: 1
depends_on: []
gap_closure: true
gap_ids: [G-05-10]
files_modified:
  - src/routes/medicines/[id].tsx
autonomous: true
requirements: [STOCK-05]

estimate:
  tokens: 20000
  raw_tokens: 10000
  tasks: 1
  confidence: high

must_haves:
  truths:
    - When status or location filters are active, the detail view stock list shows only entries matching ALL active filters
    - When no filters are active, all stock entries are shown (no regression)
    - The header status badge and catalog-delete guard remain computed from unfiltered stockEntries
    - nearestExpiryStock remains computed from unfiltered stockEntries
  artifacts:
    - src/routes/medicines/[id].tsx (filteredStockEntries useMemo added; render loop uses filteredStockEntries)
  key_links:
    - useUIStore(useShallow) → selectedStatuses + selectedLocations → filteredStockEntries useMemo → render loop at line 261
---

<objective>
G-05-10: The detail view (`src/routes/medicines/[id].tsx`) does not filter its stock entry list by the active list-view filters (selectedStatuses, selectedLocations from Zustand UIStore). This means a user who has set filters on the medicine list sees an unfiltered stock list on the detail screen — inconsistent behaviour.

Purpose: Apply the same status and location filters the list view uses so the detail view stock list is consistent.
Output: `[id].tsx` updated with a `filteredStockEntries` useMemo that gates the stock entry render loop; unfiltered `stockEntries` retained for badge and delete guard.
</objective>

<execution_context>
@C:/my-code/vibe-coding/med-stock/.claude/gsd-core/workflows/execute-plan.md
@C:/my-code/vibe-coding/med-stock/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@C:/my-code/vibe-coding/med-stock/.planning/PROJECT.md
@C:/my-code/vibe-coding/med-stock/.planning/ROADMAP.md
@C:/my-code/vibe-coding/med-stock/.planning/STATE.md
@C:/my-code/vibe-coding/med-stock/src/routes/medicines/[id].tsx
@C:/my-code/vibe-coding/med-stock/src/stores/uiStore.ts
@C:/my-code/vibe-coding/med-stock/src/lib/expiry.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add filteredStockEntries useMemo to detail view — G-05-10</name>
  <files>src/routes/medicines/[id].tsx</files>
  <behavior>
    - When selectedStatuses is empty AND selectedLocations is empty: filteredStockEntries === stockEntries (all entries shown)
    - When selectedStatuses is ['Active'] and an entry has status 'Expired': entry is excluded from filteredStockEntries
    - When selectedLocations is ['Bathroom'] and an entry has location null ('Other'): entry is excluded
    - When selectedLocations is ['Bathroom'] and an entry has location 'Bathroom': entry is included
    - Both filters AND-combined: entry must pass status filter AND location filter
    - stockEntries (unfiltered) still drives nearestExpiryStock and allStockCount (no change to those)
    - The empty-state message "No stock" appears when filteredStockEntries.length === 0 (not stockEntries.length === 0)
  </behavior>
  <action>
    In `src/routes/medicines/[id].tsx`, make these three targeted changes:

    1. IMPORTS — add `useUIStore` and `useShallow` to the existing import from `@/stores/uiStore`. The file currently has no import from that module. Add the import line after the existing `@/lib/expiry` import:
       `import { useUIStore, useShallow } from '@/stores/uiStore'`

    2. STORE READ — add two store reads inside `MedicineDetail`, immediately after the existing `const nearestExpiryStock = useMemo(...)` block (around line 72). These must be unconditional hook calls (React rules of hooks — never inside conditionals or after early returns):
       `const selectedStatuses = useUIStore(useShallow(s => s.selectedStatuses))`
       `const selectedLocations = useUIStore(useShallow(s => s.selectedLocations))`

    3. FILTERED MEMO — add `filteredStockEntries` useMemo immediately after the two store reads, before any handler functions. Dependencies: [stockEntries, selectedStatuses, selectedLocations].
       Logic (per G-05-10 spec):
       - If stockEntries is undefined or null, return an empty array.
       - For each entry in stockEntries, compute `entryStatus = calculateStatus(entry)` (string value).
       - Pass status filter: selectedStatuses.length === 0 OR selectedStatuses.includes(entryStatus).
       - Pass location filter: selectedLocations.length === 0 OR selectedLocations.includes(entry.location ?? 'Other').
       - Entry is included only if BOTH conditions are true.
       - Return the filtered array.

    4. RENDER SUBSTITUTION — in the JSX render section, replace the two references to `stockEntries` that drive the visible stock list:
       a. The empty-state guard at line 257: change `stockEntries.length === 0` to `filteredStockEntries.length === 0`
       b. The map call at line 261: change `stockEntries.map(stock =>` to `filteredStockEntries.map(stock =>`
       Do NOT change: the `nearestExpiryStock` useMemo (still uses `stockEntries`), the `allStockCount` useLiveQuery, or the loading guard (`stockEntries === undefined`).

    calculateStatus is already imported from `@/lib/expiry` — do not add a duplicate import.
    useShallow import syntax follows the project convention in uiStore.ts line 3: `from 'zustand/react/shallow'`.
  </action>
  <verify>
    <automated>npx vitest run src/routes/medicines/\[id\].test.tsx 2>/dev/null || echo "no dedicated test file — manual verify required"</automated>
  </verify>
  <done>
    - `filteredStockEntries` useMemo exists between the `nearestExpiryStock` memo and the first handler function.
    - `selectedStatuses` and `selectedLocations` are read from `useUIStore(useShallow(...))` as unconditional hook calls.
    - The stock entry render loop (line 261) and empty-state guard (line 257) reference `filteredStockEntries`.
    - `nearestExpiryStock` useMemo and the loading guard still reference unfiltered `stockEntries`.
    - `npm run build` exits 0 (no TypeScript errors).
    - Manual smoke test: set a status filter on the medicine list, navigate to a detail view — only matching stock entries appear; the header badge still reflects ALL stock.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UIStore → render | Filter values come from in-memory Zustand state — no external input crossing |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-13-01 | Information Disclosure | filteredStockEntries | low | accept | Filtering is purely client-side in-memory; no data leaves the device. Worst case: a user sees fewer entries than expected — not a security boundary crossing. |
</threat_model>

<verification>
1. `npm run build` passes (TypeScript strict mode, no errors).
2. `npm run lint` passes.
3. With no filters active: detail view shows all stock entries (no regression).
4. With a status filter set (e.g. 'Expired'): detail view shows only entries whose `calculateStatus()` result is 'Expired'.
5. With a location filter set: detail view shows only entries at the selected location.
6. Header status badge and catalog-delete confirmation count are unaffected by filters.
</verification>

<success_criteria>
G-05-10 closed: the detail view stock list respects the active Zustand filter state for selectedStatuses and selectedLocations, using the same AND-combination semantics as the list view, while keeping the header badge and delete guard computed from the full unfiltered stock set.
</success_criteria>

<output>
Create `.planning/phases/05-stock-catalog-management/05-13-SUMMARY.md` when done.
</output>
