---
phase: 05
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/db.ts
  - src/routes/medicines/[id].tsx
  - src/components/MedicineCard.tsx
autonomous: true
requirements:
  - FLOW-01
  - FLOW-02

must_haves:
  truths:
    - "A catalog entry with three stock entries is created and visible in the detail view"
    - "Detail view loads by catalogId (not stock entry ID) per D-11"
    - "Nearest-expiry status computed from active stock entries on detail screen (per FLOW-02)"
    - "Total quantity aggregated across all active stock entries for the catalog (flagged assumption: how ties are broken when multiple stocks have same earliest expiry)"
    - "Schema v4 migration removes `name` and `category` fields from Medicine interface (per D-16, D-17)"
    - "All active stock entries for a catalog are displayed, soft-deleted entries hidden"
    - "No data loss during v3→v4 schema upgrade on existing data"
  artifacts:
    - "src/lib/db.ts contains db.version(4) with `medicines` index missing name/category"
    - "src/routes/medicines/[id].tsx loads catalog by ID, queries stock by catalogId"
    - "src/components/MedicineCard.tsx (or new component) renders catalog name + nearest-expiry status badge"
    - "TypeScript interfaces MedicineCatalog and Medicine updated (Medicine no longer has name/category)"
  key_links:
    - "db.version(4) migration MUST run before any list/detail views execute (unversioned data would fail type check)"
    - "Detail view MUST call calculateStatus() on nearest-expiry stock entry to derive display status"
    - "Detail view MUST filter: deletedAt === null to hide soft-deleted stock entries"

---

<objective>
End-to-end tracer slice: schema v4 migration complete, detail view loads catalog + active stock entries, status computed from nearest-expiry stock, quantity aggregated.

Purpose: Prove the catalog+stock two-layer architecture works end-to-end by implementing the simplest happy path — view a catalog with its stock entries. Schema migration locks in the new data model. Detail view establishes the catalogId-based navigation (D-11) and aggregation pattern (nearest-expiry status, total qty).

Output: Working detail screen for a catalog; schema v4 in place; foundation for all subsequent Phase 5 tasks.
</objective>

<execution_context>
@C:/my-code/vibe-coding/med-stock/.claude/gsd-core/workflows/execute-plan.md
@C:/my-code/vibe-coding/med-stock/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-stock-catalog-management/05-CONTEXT.md
@.planning/phases/04-database-migration-schema-v3/04-01-SUMMARY.md
@.planning/phases/04-database-migration-schema-v3/04-02-SUMMARY.md
@src/lib/db.ts
@src/routes/medicines/[id].tsx
@src/components/MedicineCard.tsx
@src/lib/expiry.ts
</context>

<tasks>

<task type="auto">
  <name>Schema v4: remove name/category from medicines table, update Medicine interface</name>
  <files>src/lib/db.ts</files>
  <read_first>
    @src/lib/db.ts — Current schema (v3), index strings, MedicineCatalog and Medicine interfaces
  </read_first>
  <action>
    Add db.version(4) chaining block in src/lib/db.ts (after db.version(3)). The upgrade does NOT modify existing row data — Dexie ignores unknown fields on reads. Update the schema string for `medicines` table to remove `name` and `category` from the indexed fields list (e.g., `++id, location, expiryDate, manualStatus` — keep remaining fields). Update the TypeScript Medicine interface to remove `name: string` and `category: string | null` fields entirely (per D-17). Keep all other fields: id, catalogId, quantity, quantityUnit, expiryDate, openedDate, pao, location, manualStatus, notes, createdAt, updatedAt, deletedAt. No row migration code needed — rows are auto-upgraded by Dexie on first access.

    Reference: Phase 4 established db.version(3) with the new catalog+stock tables. This task adds the v4 schema refinement that removes denormalized fields no longer needed (D-16). All Phase 5 code reads catalog name from medicine_catalog, never from stock entries.
  </action>
  <verify>
    <automated>npm run build -- --strict 2>&1 | grep -E "(Medicine|schema)" || echo "Build succeeded, types clean"</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/db.ts contains db.version(4).stores() with medicines index string NOT including 'name' or 'category'
    - Medicine interface does NOT contain name or category fields (TypeScript strict check passes)
    - MedicineCatalog interface unchanged (still has name, category, form)
    - Dexie version chain is: version(1) → version(2) → version(3) → version(4)
    - npm run build exits 0 with no type errors
  </acceptance_criteria>
  <done>
    Schema v4 defined in db.ts; Medicine interface cleaned; build passes; all Phase 5 code can assume catalog name comes from medicine_catalog, never from stock entry.
  </done>
  <reversibility rating="one-way">D-16: Schema downgrade would require db.version(5) to restore name/category fields to medicines. All Phase 5 code hardcodes the assumption name lives in catalog only.</reversibility>
</task>

<task type="auto">
  <name>Detail view: load catalog by ID, render stock list with nearest-expiry status</name>
  <files>
    src/routes/medicines/[id].tsx
    src/components/MedicineCard.tsx
  </files>
  <read_first>
    @src/routes/medicines/[id].tsx — Current implementation (loads medicine by stock entry ID)
    @src/lib/db.ts — Dexie query patterns (where, equals, filter, toArray)
    @src/lib/expiry.ts — calculateStatus function signature and usage
    @.planning/phases/05-stock-catalog-management/05-CONTEXT.md — D-11 (catalogId as :id), D-02 (nearest-expiry status), FLOW-02 requirement
  </read_first>
  <action>
    Rewrite src/routes/medicines/[id].tsx to:
    1. Extract :id param from useParams (now represents catalogId, per D-11)
    2. Load catalog by ID: useLiveQuery(() => db.medicine_catalog.get(Number(id)), [id])
    3. Load active stock entries: useLiveQuery(() => db.medicines.where('catalogId').equals(Number(id)).filter(m => m.deletedAt === null).toArray(), [id])
    4. Compute nearest-expiry stock: useMemo that finds the stock entry with soonest expiryDate (compare string dates lexicographically)
    5. Compute status: useMemo that calls calculateStatus(nearestExpiryStock) to get status badge value
    6. Render layout:
       - Header: catalog name (use h1 + text-xl font-semibold per UI-SPEC)
       - Stock list: map over stockEntries, render each as row with quantity, expiryDate, location, status badge
       - If no stock entries, show "No stock" empty state (per UI-SPEC Copywriting)
    7. For each stock entry row, include a delete button (will wire up in later phases)

    Reference: D-11 (catalogId as navigation param), D-02 (nearest-expiry status computation), FLOW-02 (list all stock for one catalog).
  </action>
  <verify>
    <automated>npm run build && npm test -- --include="**/[id].test.ts" 2>&1 | grep -E "(PASS|FAIL|✓|✗)" || echo "Tests not yet created; build passed"</automated>
  </verify>
  <acceptance_criteria>
    - src/routes/medicines/[id].tsx loads catalog by ID (not stock entry ID)
    - Renders catalog.name as h1 with Heading typography (20px, semibold)
    - Queries stock entries: where('catalogId').equals(catalogId).filter(m => m.deletedAt === null)
    - Computes nearestExpiryStock by finding min(expiryDate) among stock entries
    - Calls calculateStatus(nearestExpiryStock) and displays StatusBadge
    - Renders stock list with columns: quantity + unit, expiryDate, location
    - Shows "No stock" empty state when no active stock entries
    - TypeScript strict types: no `any`, all Medicine/MedicineCatalog uses typed
  </acceptance_criteria>
  <done>
    Detail view loads by catalogId, shows catalog name + all active stock entries with nearest-expiry status computed. Clicking a catalog card navigates to /medicines/:catalogId. Foundation ready for edit/delete actions (later tasks).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| IndexedDB ↔ App | Local data, same origin; no untrusted input (user controls only own data) |
| DB schema ↔ Type system | Schema version mismatch could cause type errors; migration tested |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01 | Tampering | db.version(4) upgrade | low | mitigate | Upgrade is deterministic (no row modification); test data survives v3→v4 migration |
| T-05-02 | Information Disclosure | Catalog + stock data rendering | low | accept | All data local-only (PWA, no backend); user owns all data; no cloud transmission |
| T-05-03 | Denial of Service | useLiveQuery on large stock tables | low | mitigate | Dexie indexing on catalogId ensures efficient lookups; no N+1 queries |
</threat_model>

<verification>
Manual flow (after build):
1. Open DevTools → Application → IndexedDB → med-stock
2. Create test catalog entry via SQL insert or phase 04 output
3. Create 2-3 test stock entries (catalogId = catalog.id) via SQL insert
4. Navigate to /medicines/{catalogId}
5. Verify: catalog name displayed, all stock entries visible, status badge shows correct expiry status

Automated gate: npm run build passes with no type errors.
</verification>

<success_criteria>
- Schema v4 migration in place (no row data modified, field index updated)
- Detail route loads catalog by catalogId (not stock entry ID)
- Nearest-expiry status computed and displayed on detail screen
- Stock list shows all active entries, hides soft-deleted
- Empty state ("No stock") shown when catalog has no stock entries
- All TypeScript types strict (no `any`, no implicit `unknown`)
</success_criteria>

<output>
Create `.planning/phases/05-stock-catalog-management/05-01-SUMMARY.md` when done, recording:
- Schema v4 confirmed in db.ts
- Detail view routing change (useParams.id now catalogId)
- Nearest-expiry computation pattern (min date across stock)
- Test data created for manual verification
</output>

## Artifacts This Phase Produces

| Symbol | Type | File | Purpose |
|--------|------|------|---------|
| `db.version(4)` | Schema block | src/lib/db.ts | Removes name/category from medicines index |
| `Medicine` (updated) | TypeScript interface | src/lib/db.ts | No longer contains name, category fields |
| Detail view catalogId routing | Pattern | src/routes/medicines/[id].tsx | :id param is catalogId, not stock entry ID |
| Nearest-expiry computation | Function | src/routes/medicines/[id].tsx | useMemo finds min(expiryDate) among stocks |
