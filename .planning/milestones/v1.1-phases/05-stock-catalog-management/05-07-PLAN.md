---
phase: 05-stock-catalog-management
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - src/routes/medicines/[id].tsx
  - src/routes/medicines/index.tsx
  - src/components/MoveStockSheet.tsx
autonomous: true
gap_closure: true
gap_ids: [G-05-1, G-05-5, G-05-6, G-05-7]
requirements: [STOCK-02, STOCK-03, STOCK-04, FLOW-01]

must_haves:
  truths:
    - "Open box button is absent for stock entries that already have an openedDate set"
    - "Status filter is match-any: catalog appears if ANY stock entry matches a selected status (same semantics as location filter)"
    - "Each stock entry card in the detail view renders its change history below the action row"
    - "Move/Split sheet pre-fills the location picker with the stock entry's current location on open"
  artifacts:
    - src/routes/medicines/[id].tsx (Open box guard + ChangeHistory import and render)
    - src/routes/medicines/index.tsx (status filter using calculateStatus over stockEntries)
    - src/components/MoveStockSheet.tsx (useState initialized to stock.location + useEffect reset)
  key_links:
    - "ChangeHistory component (medicineId: number) wired to each stock.id inside the map callback"
    - "calculateStatus imported in index.tsx and called inside .some() for status filter"
    - "MoveStockSheet useEffect depends on [open, stock] to reset both targetLocation and quantity"
---

<objective>
Close four independent UI-level gaps found in UAT: the Open box button showing on already-opened stock entries (G-05-1), change history being written to DB but never rendered in the detail view (G-05-6), status filter matching only the aggregate status instead of individual stock entries (G-05-5), and the Move/Split sheet always defaulting to "Other" instead of the entry's current location (G-05-7).

Purpose: These fixes correct confusing behaviour that surfaced during manual testing without touching data schema or business logic.
Output: Three patched source files. No new files created.
</objective>

<execution_context>
@C:/my-code/vibe-coding/med-stock/.claude/gsd-core/workflows/execute-plan.md
@C:/my-code/vibe-coding/med-stock/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-stock-catalog-management/05-UAT.md
</context>

<tasks>

<task type="tracer">
  <name>Tracer: detail view — Open box guard (G-05-1) + ChangeHistory render (G-05-6)</name>
  <files>src/routes/medicines/[id].tsx</files>
  <action>
    Read the current file before editing.

    G-05-1 (Open box guard): In the stockEntries.map callback, find the Open box Button element. Its visibility is currently controlled by the condition `(stock.quantity ?? 0) > 1`. Add the additional condition `!stock.openedDate` so the full condition becomes `(stock.quantity ?? 0) > 1 && !stock.openedDate`. This hides the button when the entry is already marked as opened.

    G-05-6 (ChangeHistory render): Add an import for ChangeHistory at the top of the file alongside the existing component imports: import ChangeHistory from '@/components/ChangeHistory'. Then, inside the stockEntries.map callback, add `<ChangeHistory medicineId={stock.id} />` as the last element inside the stock entry card div — place it after the closing tag of the action row div (the `<div className="flex gap-2 mt-2 flex-wrap">` block). Each stock card in the map gets one ChangeHistory instance keyed by stock.id (the outer card key is already on the card div).
  </action>
  <verify>
    <automated>npx vitest run 2>/dev/null; npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>TypeScript compiles clean. A stock entry with openedDate set shows no Open box button. A stock entry without openedDate and quantity > 1 still shows Open box. Each stock card renders a collapsible Change History section at its bottom.</done>
</task>

<task type="auto">
  <name>Fix status filter to match-any over stock entries (G-05-5)</name>
  <files>src/routes/medicines/index.tsx</files>
  <action>
    Read the current file before editing.

    Add calculateStatus to the imports from '@/lib/expiry'. The current import line brings in only type imports from db; add: `import { calculateStatus } from '@/lib/expiry'`.

    Find the status filter block inside the useMemo filtered chain. The current check is:
    `if (selectedStatuses.length > 0 && !selectedStatuses.includes(item.aggregateStatus)) return false`

    Replace it with match-any semantics over all stock entries:
    `if (selectedStatuses.length > 0 && !item.stockEntries.some(e => selectedStatuses.includes(calculateStatus(e)))) return false`

    The variable `item.stockEntries` already exists in the CatalogWithStock shape — it holds all active stock entries for the catalog.
  </action>
  <verify>
    <automated>npx vitest run 2>/dev/null; npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>Status filter now applies match-any semantics identical to the location filter. Selecting "Expired" shows any catalog that has at least one expired stock entry, even if the aggregate status differs.</done>
</task>

<task type="auto">
  <name>Pre-fill MoveStockSheet location from current stock entry (G-05-7)</name>
  <files>src/components/MoveStockSheet.tsx</files>
  <action>
    Read the current file before editing.

    Change the React import from `import { useState } from 'react'` to `import { useState, useEffect } from 'react'`.

    Change the targetLocation state initializer from `useState<string | null>(null)` to `useState<string | null>(stock.location)`. This seeds the initial value from the prop.

    Add a useEffect after the existing state declarations:
    The effect must re-sync both targetLocation and quantity whenever the sheet opens or the stock prop changes. Run `setTargetLocation(stock.location)` and `setQuantity(1)` inside the effect body, guarded by `if (open)`. The dependency array is `[open, stock]`.
  </action>
  <verify>
    <automated>npx vitest run 2>/dev/null; npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>Move/Split sheet opens with the location picker pre-selected to the stock entry's current location. If opened again for a different entry, both location and quantity reset appropriately.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Component props | stock.location is a DB value (string or null) — no unvalidated user input in this flow |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-07-01 | Tampering | MoveStockSheet quantity state | low | accept | quantity is re-validated against maxQty before submit; stale state prevented by useEffect reset |
</threat_model>

<verification>
Run `npx vitest run` and `npx tsc --noEmit` — both must pass with no new errors.

Manual checks in dev server:
1. Open detail view for a medicine with a stock entry that has openedDate set — Open box button must be absent on that entry.
2. Open detail view for a stock entry with quantity > 1 and no openedDate — Open box button must still appear.
3. Edit any stock entry via the edit sheet — confirm Change History section appears in the card and reflects the edit after saving.
4. Click Move/Split on a stock entry stored in "Kitchen Drawer" — confirm the location picker opens pre-selected to "Kitchen Drawer".
5. Apply a status filter in the list view — confirm catalogs appear if any individual stock entry matches the selected status.
</verification>

<success_criteria>
Gaps G-05-1, G-05-5, G-05-6, G-05-7 closed. `npx tsc --noEmit` clean. `npx vitest run` green.
</success_criteria>

<output>
Create `.planning/phases/05-stock-catalog-management/05-07-SUMMARY.md` when done
</output>
