---
phase: 05-stock-catalog-management
plan: 12
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/MedicineCardAggregate.tsx
  - src/routes/medicines/index.tsx
autonomous: true
requirements: [CAT-01, STOCK-01]
gap_closure: true
gap_ids: [G-05-9]

estimate:
  tokens: 18000
  raw_tokens: 12000
  tasks: 1
  confidence: low

must_haves:
  truths:
    - The status badge on every catalog card reflects the worst-case aggregateStatus computed by computeCatalogAggregate (priority: Expired > ExceededOpenPeriod > Opened > Active)
    - A catalog with only PAO-only stock entries (no expiryDate) shows a non-Active badge when those entries are Opened or ExceededOpenPeriod
    - The badge status matches the value used by the status filter in index.tsx so filter and badge are always consistent
  artifacts:
    - src/components/MedicineCardAggregate.tsx — accepts aggregateStatus prop; badge derived from prop, not recalculated
    - src/routes/medicines/index.tsx — passes aggregateStatus={item.aggregateStatus} to MedicineCardAggregate
  key_links:
    - computeCatalogAggregate returns aggregateStatus on each CatalogAggregate item → index.tsx passes it to MedicineCardAggregate → badge renders it directly
---

<objective>
Fix the catalog card status badge so it uses the pre-computed aggregateStatus from computeCatalogAggregate rather than re-deriving status from nearestExpiryStock.

Purpose: The mismatch between badge (nearestExpiryStock-based) and filter (aggregateStatus-based) makes the status filter appear broken for PAO-only entries where nearestExpiryStock is null and badge defaults to Active even when actual status is worse.

Output: MedicineCardAggregate receives aggregateStatus as a prop and renders it directly; index.tsx passes the value; calculateStatus import removed from the card component.
</objective>

<execution_context>
@C:/my-code/vibe-coding/med-stock/.claude/gsd-core/workflows/execute-plan.md
@C:/my-code/vibe-coding/med-stock/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-stock-catalog-management/05-UAT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire aggregateStatus prop through MedicineCardAggregate</name>
  <files>src/components/MedicineCardAggregate.tsx, src/routes/medicines/index.tsx</files>
  <action>
    In src/components/MedicineCardAggregate.tsx:
    - Replace the import line `import { calculateStatus } from '@/lib/expiry'` with `import type { MedicineStatus } from '@/lib/expiry'` — calculateStatus is no longer called in this component.
    - Add `aggregateStatus: MedicineStatus` to MedicineCardAggregateProps (alongside the existing fields catalog, nearestExpiryStock, totalQuantity, stockCount).
    - Add aggregateStatus to the destructuring in the function signature.
    - Replace the status derivation line (`const status = nearestExpiryStock ? calculateStatus(nearestExpiryStock) : 'Active'`) with `const status = aggregateStatus`.
    - No other changes to the component.

    In src/routes/medicines/index.tsx:
    - Add `MedicineStatus` to the existing expiry import: change `import { calculateStatus } from '@/lib/expiry'` to `import { calculateStatus } from '@/lib/expiry'` and add `import type { MedicineStatus } from '@/lib/expiry'` as a separate type import — or combine into one import statement if preferred (calculateStatus is still used for the filter, so do NOT remove it).
    - In the MedicineCardAggregate JSX block (around line 202-206), add the prop `aggregateStatus={item.aggregateStatus as MedicineStatus}` alongside the existing props.
  </action>
  <verify>
    <automated>npx vitest run src/lib/aggregation.test.ts && npx vitest run</automated>
  </verify>
  <done>
    MedicineCardAggregate.tsx: props interface includes aggregateStatus: MedicineStatus; status const equals aggregateStatus directly; calculateStatus is not imported.
    index.tsx: MedicineCardAggregate JSX passes aggregateStatus={item.aggregateStatus as MedicineStatus}.
    All existing tests pass.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| aggregateStatus value | Comes from computeCatalogAggregate over Dexie live query — same trust level as existing data flow |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation |
|-----------|----------|-----------|----------|-------------|------------|
| T-05-12-01 | Tampering | aggregateStatus cast | low | accept | Cast is `as MedicineStatus` from computeCatalogAggregate which already types the field as MedicineStatus — no external input involved |
</threat_model>

<verification>
After applying the fix:
1. Run `npx vitest run` — all tests must pass.
2. Manual smoke-check in dev server: open a catalog with only PAO-only entries (no expiryDate, openedDate set). The badge should now show Opened/ExceededOpenPeriod matching what the status filter selects.
3. Selecting "Opened" in the status filter must surface the same catalogs that show an Opened badge.
</verification>

<success_criteria>
- Status badge on catalog card is driven by aggregateStatus prop, not recalculated from nearestExpiryStock.
- For PAO-only stock entries, badge correctly reflects worst-case status (Opened, ExceededOpenPeriod) instead of defaulting to Active.
- Filter and badge are consistent: a catalog shown by the "Opened" filter always displays an Opened badge.
- All existing Vitest tests pass.
</success_criteria>

<output>
Create `.planning/phases/05-stock-catalog-management/05-12-SUMMARY.md` when done.
</output>
