---
phase: 03-data-household-sync
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - src/components/ui/sonner.tsx
  - src/routes/RootLayout.tsx
  - src/components/BottomTabBar.tsx
  - src/App.tsx
  - src/routes/data/index.tsx
  - src/components/SyncInstructions.tsx
  - src/components/ExportSection.tsx
  - src/components/ImportJSONSection.tsx
  - src/components/ImportCSVSection.tsx
autonomous: true
requirements:
  - DATA-04
must_haves:
  truths:
    - "5th 'Data' tab appears in BottomTabBar after Locations; navigating to /#/data renders DataScreen"
    - "DataScreen renders exactly 3 section cards with h3 headers: 'Export backup', 'Import backup', 'Sync with household' (per D-43)"
    - "SyncInstructions renders 4 numbered steps and Note paragraph — exact copy from UI-SPEC §Sync with Household Section (per D-44)"
    - "npm run build exits 0 with all stub components in place"
    - "Toaster from sonner is mounted in RootLayout — toast() calls from subsequent plans will fire"
  artifacts:
    - src/routes/data/index.tsx
    - src/components/SyncInstructions.tsx
    - src/components/ui/sonner.tsx
    - src/components/ExportSection.tsx
    - src/components/ImportJSONSection.tsx
    - src/components/ImportCSVSection.tsx
  key_links:
    - "App.tsx route children include { path: 'data', element: <DataScreen /> } after trash entry"
    - "BottomTabBar has 5 NavLink elements — Medicines | Dashboard | Trash | Locations | Data (D-42)"
    - "RootLayout.tsx renders <Toaster /> imported from @/components/ui/sonner"
    - "DataScreen imports and renders ExportSection, ImportJSONSection, ImportCSVSection, SyncInstructions"
---

## Phase Goal

**As a** household member, **I want to** export my medicine inventory as a backup file and import it on another device, **so that** two household members can share one inventory without any backend.

<objective>
Lay the navigational foundation for Phase 3: install the two new npm dependencies (sonner toast, papaparse CSV parser), wire the toast infrastructure into RootLayout, add the 5th "Data" tab to the bottom nav, register the /data route, scaffold the DataScreen with stub section components, and deliver the complete static SyncInstructions component.

After this plan, the Data tab is navigable, the DataScreen renders its 3-section structure with placeholder content for Export and Import, and the Sync with household instructions section is fully delivered.

Purpose: Provide the structural shell that Plans 03-02 and 03-03 will fill with real implementations. Every downstream component drops in without touching DataScreen or the nav.

Output: Working Data tab + DataScreen scaffold + SyncInstructions + toast infrastructure
</objective>

<execution_context>
@C:/Users/lukasz.bielinski/OneDrive - Accenture/my-folders/my-projects/vibe-coding/med-stock/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/lukasz.bielinski/OneDrive - Accenture/my-folders/my-projects/vibe-coding/med-stock/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-data-household-sync/03-CONTEXT.md
@.planning/phases/03-data-household-sync/03-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install dependencies and configure sonner toast</name>
  <files>package.json, src/components/ui/sonner.tsx, src/routes/RootLayout.tsx</files>

  <read_first>
    - src/routes/RootLayout.tsx — current structure; Toaster goes as last child of the root div
    - src/components/ui/alert-dialog.tsx — pattern for how existing shadcn ui wrappers are structured (thin re-exports with cn() applied)
  </read_first>

  <action>
  Step 1 — Install packages. Run: npm install sonner papaparse
  Then run: npm install --save-dev @types/papaparse
  Both must succeed before proceeding. sonner is the shadcn-ecosystem toast library (by Emil Kowalski); papaparse is the industry-standard CSV parser listed in CLAUDE.md recommended stack. Neither requires a legitimacy checkpoint — both are explicitly called out in project documentation.

  Step 2 — Create src/components/ui/sonner.tsx. This file is a thin wrapper matching the shadcn/ui pattern for the sonner integration. Import the Toaster component from 'sonner'. Export a named Toaster function that spreads all props through to the sonner Toaster and sets two default props: position="bottom-center" (mobile-friendly) and richColors (enables the success/error/warning color system matching the UI-SPEC color tokens). The component has no additional logic — it is purely a re-export with defaults applied so future configuration is centralized.

  Step 3 — Update src/routes/RootLayout.tsx. Add an import of Toaster from '@/components/ui/sonner'. Inside the returned JSX, add a self-closing Toaster element as the last child of the root div (after the BottomTabBar element). Do NOT add it inside the main element — it must be a sibling of BottomTabBar so portal positioning works correctly across all routes.

  Do NOT import from 'sonner' directly in RootLayout — always import from '@/components/ui/sonner' to keep theming centralized.
  </action>

  <verify>
    <automated>
      node -e "const p=JSON.parse(require('fs').readFileSync('package.json','utf8')); const ok=p.dependencies.sonner &amp;&amp; p.dependencies.papaparse &amp;&amp; p.devDependencies['@types/papaparse']; process.exit(ok?0:1)" &amp;&amp; npm run build
    </automated>
  </verify>

  <acceptance_criteria>
  - package.json dependencies object contains "sonner" key
  - package.json dependencies object contains "papaparse" key
  - package.json devDependencies object contains "@types/papaparse" key
  - src/components/ui/sonner.tsx exists and contains a named export called Toaster
  - src/routes/RootLayout.tsx contains the string "Toaster" (import + render)
  - npm run build exits 0 — TypeScript accepts the new imports
  </acceptance_criteria>

  <done>Sonner and papaparse installed; Toaster wired into RootLayout; build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Add Data tab to navigation and scaffold DataScreen</name>
  <files>
    src/components/BottomTabBar.tsx,
    src/App.tsx,
    src/routes/data/index.tsx,
    src/components/SyncInstructions.tsx,
    src/components/ExportSection.tsx,
    src/components/ImportJSONSection.tsx,
    src/components/ImportCSVSection.tsx
  </files>

  <read_first>
    - src/components/BottomTabBar.tsx — exact NavLink + cn() pattern to replicate for the 5th tab
    - src/App.tsx — exact route child structure and import placement to replicate for the data route
    - .planning/phases/03-data-household-sync/03-UI-SPEC.md — §Copywriting Contract §Sync with Household Section for the 4 step strings and Note text; §Data Screen Layout for section card styling
    - .planning/phases/03-data-household-sync/03-CONTEXT.md — D-42, D-43, D-44 locked decisions
  </read_first>

  <action>
  Step 1 — Update src/components/BottomTabBar.tsx. Append a 5th NavLink immediately after the Locations NavLink. Use the identical className pattern as the existing 4 tabs (cn() with isActive conditional). Set to="/data" and render inner span with text content "Data". The nav comment at the top of the file must be updated from "D-39: 4 tabs: Medicines | Dashboard | Trash | Locations" to "D-42: 5 tabs: Medicines | Dashboard | Trash | Locations | Data". Per D-42, do NOT remove any existing tab.

  Step 2 — Update src/App.tsx. Add a named import of DataScreen from '@/routes/data/index'. Add a new route entry { path: 'data', element: React.createElement(DataScreen) } (or JSX equivalent) in the children array after the trash entry. Follow the identical structure as the existing location/dashboard/trash route entries.

  Step 3 — Create src/routes/data/index.tsx. DataScreen is a named export function component with no props. Import ExportSection from '@/components/ExportSection', ImportJSONSection from '@/components/ImportJSONSection', ImportCSVSection from '@/components/ImportCSVSection', SyncInstructions from '@/components/SyncInstructions'.

  DataScreen renders a single scrollable container div with Tailwind classes: "flex flex-col gap-4 p-4 pb-20" (pb-20 keeps content above the 64px tab bar). Inside, render 3 section cards in order per D-43:

  Section card 1 — className "border border-gray-200 rounded-lg p-4":
    h3 with className "text-base font-medium mb-2" and text "Export backup"
    ExportSection as self-closing element

  Section card 2 — className "border border-gray-200 rounded-lg p-4":
    h3 with className "text-base font-medium mb-2" and text "Import backup"
    ImportJSONSection as self-closing element
    hr with className "my-4 border-gray-100" (visual divider between JSON and CSV import)
    ImportCSVSection as self-closing element

  Section card 3 — className "border border-gray-200 rounded-lg p-4":
    h3 with className "text-base font-medium mb-2" and text "Sync with household"
    SyncInstructions as self-closing element

  Note: Section components do NOT render their own h3 section headers — DataScreen owns the headers per the 3-section contract (D-43). Section components render only their content below the h3.

  Step 4 — Create src/components/SyncInstructions.tsx. Named export function component SyncInstructions with no props. Renders a div with className "space-y-3 text-sm text-muted-foreground". Render 4 paragraph elements with EXACT copy from UI-SPEC §Sync with Household Section:
    Paragraph 1: '1. Use the "Export backup" button above to save a backup file to a shared folder (e.g., OneDrive, Google Drive, Dropbox).'
    Paragraph 2: '2. Share the link to the folder with your household member.'
    Paragraph 3: '3. Your household member opens this app on their device, downloads your backup file, and uses the "Import backup" button to restore it.'
    Paragraph 4: '4. To push your changes to your household member, export a new backup and they import it again. The most recent backup is the source of truth.'
    Final paragraph with className "mt-2 text-xs": 'Note: All changes are manual. There is no automatic sync — each household member must explicitly export and import when sharing updates.'
  No buttons, no state, no interactivity — per D-44 this section is instructions-only.

  Step 5 — Create src/components/ExportSection.tsx. Named export function ExportSection with no props. Stub placeholder: renders a p element with className "text-sm text-muted-foreground" and text "Export functionality will be available soon." This stub will be completely replaced in Plan 03-02. Do not add any imports beyond React (if needed by the runtime).

  Step 6 — Create src/components/ImportJSONSection.tsx. Named export function ImportJSONSection with no props. Stub placeholder: p element, className "text-sm text-muted-foreground", text "JSON import will be available soon." Replaced in Plan 03-02.

  Step 7 — Create src/components/ImportCSVSection.tsx. Named export function ImportCSVSection with no props. Stub placeholder: p element, className "text-sm text-muted-foreground", text "CSV import will be available soon." Replaced in Plan 03-03.
  </action>

  <verify>
    <automated>npm run build</automated>
  </verify>

  <acceptance_criteria>
  - src/components/BottomTabBar.tsx contains exactly 5 NavLink elements (grep -c "NavLink" src/components/BottomTabBar.tsx returns 5 — counting opening tags, adjust grep pattern to "to=")
  - src/components/BottomTabBar.tsx contains 'to="/data"'
  - src/App.tsx contains 'path: .data.' (the route entry)
  - src/App.tsx imports DataScreen from '@/routes/data/index'
  - src/routes/data/index.tsx exists and contains three h3 elements with text "Export backup", "Import backup", "Sync with household"
  - src/components/SyncInstructions.tsx contains the string "household member must explicitly export and import"
  - src/components/ExportSection.tsx, ImportJSONSection.tsx, ImportCSVSection.tsx all exist as stub components
  - npm run build exits 0
  - Navigating to /#/data in a browser renders a page with 3 visible sections (manual verification)
  </acceptance_criteria>

  <done>BottomTabBar has 5 tabs; /data route registered; DataScreen scaffold renders 3 sections; SyncInstructions complete; stubs in place; build passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → local node_modules | Third-party packages installed in this task |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-SC | Tampering | npm install (sonner, papaparse) | high | mitigate | Both packages are explicitly named in CLAUDE.md recommended stack (papaparse) and shadcn ecosystem (sonner). Verify via npmjs.com/package/sonner and npmjs.com/package/papaparse before install — both are [VERIFIED]: sonner by emilkowalski_ (4M weekly downloads), papaparse (18M weekly downloads, MIT). No checkpoint required. |
| T-03-00 | Tampering | App.tsx route registration | low | accept | Adding a new child route cannot shadow existing routes (HashRouter children are matched by path prefix). Risk of accidental breakage is handled by npm run build type check. |
</threat_model>

<verification>
After both tasks complete:
1. npm run build exits 0 — TypeScript compiles all new files
2. npm test exits 0 — no existing tests broken
3. Manual: open app in browser, confirm 5 tabs in bottom bar, tap Data tab, see 3-section scaffold
4. Manual: SyncInstructions text is readable with 4 numbered steps and Note paragraph
</verification>

<success_criteria>
- package.json has sonner + papaparse + @types/papaparse
- src/components/ui/sonner.tsx exports Toaster
- RootLayout.tsx renders Toaster
- BottomTabBar has 5 tabs in Medicines | Dashboard | Trash | Locations | Data order (D-42)
- DataScreen renders 3 section cards with correct h3 headers (D-43)
- SyncInstructions is complete with exact UI-SPEC copy and no buttons (D-44)
- ExportSection, ImportJSONSection, ImportCSVSection exist as non-breaking stubs
- npm run build and npm test both pass
</success_criteria>

<output>
Create .planning/phases/03-data-household-sync/03-01-SUMMARY.md when done.
</output>
